"""관계 그래프 — 마일스톤 2 의 중 1.1.

`class_graph.db`(상속 + 메서드 소유권)와 `dependency_graph.db`(`#include`)를 만든다.
**LLM 을 쓰지 않는다** — tree-sitter 파싱과 정규식뿐이라 커밋 즉시 돌 수 있고, 검증도
결정적 게이트로 끝난다.

`data-schema.md` 기준 이 두 DB 는 **파생 자산**이다 — 지우고 풀 스캔하면 되살아난다.
합성 산출물(컨텍스트 MD·도메인 yaml)과 성격이 다르니 백업 대상으로 취급하지 말 것.
"""
