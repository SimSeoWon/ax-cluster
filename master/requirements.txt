# 마스터 오케스트레이션 의존성.
# AgentTest 에는 requirements.txt 가 없어 `build.bat:29` 의 pip 목록을 기준으로 새로 만들었다.
# `pywinpty` 는 윈도우 전용이라 제외한다 — 리눅스에서는 순수 파이프로 충분하다(리포트 04).

# task_queue HTTP 서비스 (--serve). systemd 주 모드가 요구하는 최소 집합.
fastapi
uvicorn
pydantic

# MCP. `master/projects`(8103) 는 이걸 필수로 쓴다 — streamable-http 로 노출한다.
# ⚠️ 설치본은 mcp 2.0.0 이라 `mcp.server.fastmcp` 가 없다. 고수준 API 는
#    `mcp.server.MCPServer` — task_queue/mcp_server.py 는 아직 옛 경로라 stdio 가 깨져 있다.
mcp

# 프로젝트 레지스트리 설정 파일(projects.yaml · <Name>/config.yaml).
pyyaml

# 추론 브로커 (master/broker) — 노드로 프록시. 스트리밍 통과가 필요해 stdlib 로는 부족하다.
httpx2

# RAG — context_search 1단계 이식 완료 (2026-08-08). 내장 ONNX MiniLM 을 쓰므로
# 별도 임베딩 서버가 없다. BM25 는 stdlib sqlite3 의 FTS5 라 의존성이 없다.
chromadb

# 클래스 그래프 파서 (소 1.1.1). 🔴 없으면 `master/graph/parse.py` 의 require() 가 던진다 —
# 원본은 조용히 빈 그래프를 만들었고, 그게 "돌았는데 결과 0" 의 원인이었다.
# 리눅스에서 UE 매크로 전처리 결과 검증됨: 1,654 파일 → 1,806 클래스 (2026-08-08).
tree-sitter
tree-sitter-cpp
