# LLM 적합도 — **실측 기록** (작업별 유리함 · 환각 유무)

> 모델 적합도는 **머신의 속성이 아니라 모델의 속성**이라 여기 한 곳에 모은다. 각 머신에
> **무엇이 설치돼 있는지**는 그 머신 문서에 있다 — [`win-worker-2.md`](win-worker-2.md) ·
> [`win-main-33.md`](win-main-33.md) · [`bc250-1.md`](bc250-1.md).
>
> 🔴 **여기 없는 모델은 "좋다/나쁘다" 를 모른다는 뜻이다** — 안 재봤다는 것이지 나쁘다는
> 뜻이 아니다. 추측으로 칸을 채우지 말 것.

## 왜 이 문서가 있나

같은 측정을 세션마다 다시 하지 않기 위해서다. 그리고 **환각은 모델을 바꿀 때만 재는 게
아니라 기록해 둬야 한다** — 우리가 겪은 환각은 전부 *"에러 없이 그럴듯한 문서"* 형태라,
숫자로 남기지 않으면 다음 세션이 같은 걸 다시 발견한다.

**판정 기준은 결정적 게이트다** — `master/context_synth/verify.py` 가 소스의 주석을 벗겨
**주석 안에만 있는 식별자**를 특정하고, 문서가 그것을 사실로 주장하면 거부한다. LLM 에게
자기 검증을 맡기지 않는다(설계 규칙: 결정적 게이트 > LLM 판단).

---

## 로컬 (Ollama)

| 모델 | 어디 | 유리한 작업 | 🔴 환각·실패 (실측) |
|---|---|---|---|
| **`gemma4:e4b`** | bc250 · `.2` · `.33` | ✅ **컨텍스트 MD 합성 — 채택** (`SYNTH_MODEL`) | 사실 게이트 **3/3**. 12~32초/그룹. **4B급인데 35B 와 동률** — 지금 실패 유형이 문장력이 아니라 **지시 준수**라는 뜻 |
| **`gemma3:12b`** | `.2` | ✅ 합성 **대안** (`FALLBACK_MODEL`) | 사실 게이트 **3/3**. 16~46초/그룹. 전용 VRAM 이라 메모리 문제 없음 |
| **`qwen2.5-coder:14b`** | bc250 · **`.2` 핀** · `.33` | ✅ **UE5 C++ 코드 생성** (§4.4 확정, `CODER`) | 🔴 **문서 작업엔 부적합** — frontmatter 를 닫는 두 번째 `---` 를 빼먹는다(실측). 형식 게이트가 전부 거부했다. `md.repair_frontmatter` 로 구제하지만 다른 모델엔 필요 없는 보정이다 |
| **Qwen3.5-35B-A3B `IQ2_M`** | **bc250 핀** | 🟡 합성 가능하지만 **권하지 않는다** (`HEAVY_MODEL`) | 🔴 **주석 처리된 코드를 살아 있는 기능으로 서술** — 실측 2건: ⑴ `FPlayerMontageLoadInfo`·`FMonsterMontageLoadInfo`·`RequestLoad_MontageList` ⑵ `UMissionTask_Spawn`. 특이한 것은 **같은 응답의 「개선 필요 사항」 절은 정확히 짚는다** — 사실을 모르는 게 아니라 절마다 다르게 쓴다.<br>🔴 **요청당 ~170MB 씩 노드 메모리를 먹고 안 돌려준다** → BC-250 커널 정지(리포트 10 §8). `num_ctx` 로 한 요청은 묶이지만 누적은 안 막힌다 |
| `devstral:24b` | bc250 | ⬜ **미측정** | — |
| `gemma2:2b` · `qwen3:8b` · `llama3` · `llama3.1` | `.2` | ⬜ **미측정** | — |

### 임베딩 (검색용, 마스터 로컬)

| 모델 | 상태 | 근거 |
|---|---|---|
| **`paraphrase-multilingual-MiniLM-L12-v2`** (fastembed/ONNX) | ✅ **채택** | **384차원** — 원본이 기록한 *"384차원짜리는 저장 증가 0"* 조건에 부합. PyTorch 불필요(ONNX). 「전역 이벤트 시스템」↔`global event system` 코사인 **0.817** |
| `all-MiniLM-L6-v2` (chromadb 내장) | ⬜ 되돌릴 때만 | 🔴 **영어 전용.** 한글 질의가 벡터·BM25 **두 채널 모두 실패**했다 — 「전역 이벤트 시스템」이 전부 오답, `global event system` 은 정답 |

## 상용 CLI

| | 어디 | 유리한 작업 | 🔴 주의 |
|---|---|---|---|
| **Claude Code** | `.2` · `.33` | 코드 작성·리뷰 (원본이 층2 검증에 쓴 백엔드) | 한도 신호 감지 시 **5시간 차단**이 원본 정책. 차단 중엔 폴백도 건너뛴다(다른 백엔드 과부하 방지) |
| **`agy` (Antigravity)** | 마스터 1.1.10 · `.33` 1.0.16 | 큰 파일 단발 분석 (토큰 절감) | 🔴 **웹 검색을 시키면 출처를 지어낸다** — 파일을 `--add-dir` 로 쥐여줄 것.<br>🔴 **작업 디렉토리에서 자율 실행 금지** — `--dangerously-skip-permissions` 를 쓰며 동의 없이 파일을 고친 적이 있다.<br>🔴 `.33` 은 **1.0.16 으로 구버전** — 마스터가 겪은 PTY·ANSI·argv 함정이 거기서 고쳐졌는지 미검증 |
| Gemini CLI | `.2` | — | 원본은 **2026-06-18 개인티어 종료로 실행 스택을 통째 제거**하고 `agy` 로 갔다. 새로 배선하지 말 것 |

---

## 이 표를 갱신하는 규칙

1. **재본 것만 적는다.** 미측정은 `⬜` 로 남긴다 — 빈칸을 추측으로 채우면 이 문서가 쓸모없어진다
2. **환각은 재현 조건까지 적는다** — "환각이 있었다" 만 적으면 다음 세션이 판단을 못 한다.
   어느 파일·어느 식별자였는지가 근거다
3. **모델을 바꿀 때 통과율을 같이 잰다** — `python -m master.context_synth sample N` 이
   받아온 스냅샷을 건드리지 않고 통과율만 낸다
4. 🔴 **표본 수를 적는다.** 지금 합성 판정은 **그룹 3~4개 표본**이다. 얇다 —
   실제 커밋이 돌면 자연히 쌓이므로 그때 갱신한다
