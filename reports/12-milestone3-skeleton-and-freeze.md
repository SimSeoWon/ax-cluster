# 리포트 12 — 마일스톤 3: 골조 생성 + 인터페이스 동결 (중 1.1)

## §1. 무엇을 만들었나

`master/work/skeleton.py` — §4.5 의 **빈 첫 칸**이었던 골조 생성. 소 1.1.1~1.1.4.

    frozen_decls(header)     동결 선언 키 집합    🔴 결정적 — LLM 0
    violation(old, new)      위반 사유            추가는 허용, 변경·삭제만 위반
    relations(paths, cls)    부모/자식·include    🔴 그래프에서 읽는다 (LLM 0)
    build_prompt(...)        규범 → 관계 → 선언부 → 지시  (뒤로 갈수록 베낄 것)
    build(paths, spec)       골조 **텍스트**      🔴 파일을 쓰지 않는다
    Skeleton.contracts()     워커가 읽는 계약 텍스트

## §2. 🔴 사용자 지적으로 고친 것 둘

기억하신 원전 흐름(*"각 클래스별 관계(부모/자식, 포함관계 등)와 호출하는 인터페이스등 기본
골조를 만들고 [클로드 오푸스 모델을 이용해 처리하고]"*)과 대조해 **내가 만든 것에 구멍이 둘**
있었다.

### ① 모델을 잘못 골랐다

기본값을 로컬 `qwen2.5-coder:14b` 로 뒀었다. **틀렸다.** 모토(*작성은 무료 로컬*)는 **본문
대량 생산** 이야기인데, 골조는 **작업당 1회**이면서 **모든 병렬 조각의 계약**이 된다 —
틀린 골조는 N개 조각을 전부 오염시킨다. 배칭 실측이 *"호출당 고정비가 지배한다"* 로 말한 것과
같은 논리로 **여기가 값을 쓸 자리**다. → `DEFAULT_MODEL = "claude:opus"`.

레인 문법을 `synth.generate` 에 확장했다 — `claude:opus` 처럼 **레인 뒤에 모델**을 붙인다.
`claude -p --model opus` 실측 통과. 🔴 층2 는 모델을 지정하지 않는다(소 2.2.1 이 정할 일을
코드에 박지 않는다).

### ② 관계 그래프를 안 쓰고 있었다

중 1.1(상속 + `#include`, LLM 0)에서 **이미 만들어 둔 그래프**를 골조 프롬프트가 안 읽었다.
규범과 선언부만 싣고 관계를 뺐다 — 부모를 모르면 `: public X` 를 지어내고, include 를 모르면
헤더를 잘못 문다. 실측:

    UMSViewBase   parents: UMSWidgetBase <- UCommonActivatableWidget · children (3)
                  includes: Source/ModularStage/UI/Base/MSWidgetBase.h
    UMissionTaskExecutor   parents: UObject <- FTickableGameObject

프롬프트 순서를 **규범 → 관계 → 선언부 → 지시**로 뒀다. 규범은 *무엇을 지킬지*, 관계는
*어디에 붙을지*, 선언부는 *정확한 철자*다 — 뒤로 갈수록 베껴야 하는 것이라 지시에 가깝게 둔다
(#26 실측: 베낄 텍스트가 지시에서 멀면 모델이 기억에서 꺼낸다).

## §3. 🔴 원전 동결 목록에 구멍이 있었다 (실측)

원전 `_frozen_decls` 를 그대로 이식했는데, 이 프로젝트 헤더 **857개**로 재보니 매크로 목록이
모자랐다:

    GENERATED_USTRUCT_BODY          97회   ← 원전 목록에 없다
    DECLARE_LOG_CATEGORY_EXTERN     25회   ← 없다
    DECLARE_DYNAMIC_MULTICAST_…     19회   ← 없다
    UE_DECLARE_GAMEPLAY_TAG_EXTERN         ← 없다 (태그 헤더의 동결이 0개였던 원인)

→ `GENERATED_*BODY` 와 `DECLARE_`/`UE_DECLARE_` 계열을 접두어로 받는다.

⚠️ **대문자 매크로를 통째로 받지는 않았다.** 같은 실측에서 `UE_LOG` 가 헤더의 **인라인 본문에
23회** 나왔다 — 동결하면 워커가 로그 문구만 고쳐도 위반이 뜬다. *정상 작업을 막는 게이트가
통과시키는 게이트보다 나쁘다*(사실 게이트에서 이미 내린 판단). 프로젝트 고유 매크로
(`PODS_SAFE_CALL` 130회)도 선언인지 문장인지 몰라 넣지 않았다.

### 회귀 (합성 데이터가 아니라 실제 소스로)

    헤더 857개 · 동결 선언 12,719개 (구멍 메우기 전 12,519) · 선언 0개 헤더 16 (이전 19)
    🔴 자기 자신 대비 위반 0            ← 정규화가 안정적이다
    🔴 본문 채움 시뮬 714건 · 오탐 0     ← `;` → `{…}` 가 키를 안 깬다 (설계의 핵심 성질)

남은 0건 헤더 16개는 정당하다 — 의도적 빈 껍데기(`MSActivatableWidget.h`, "삭제 금지" 룰)와
`#define`·태그 전용 파일들이다.

## §4. 등록 배선 (소 1.1.4) — 🔴 과했던 것을 되돌렸다

처음엔 골조를 **필수**로 만들었다가 기존 테스트가 전부 깨졌다. 골조 없는 등록은 **원래부터
정상 경로**다(작업장이 골조를 쓰는 흐름). 되돌리고 조건을 하나로 좁혔다:

    instruction 있고 skeleton 없음  →  생성한다
    skeleton 이미 있음              →  🔴 손대지 않는다 (사람이 쓴 골조가 이긴다)
    둘 다 없음                      →  건너뛴다 (기존 동작 유지)

🔴 **골조 생성은 큐를 만지기 전에 끝낸다** — 등록 도중 실패하면 반쪽 work 가 남고 그건 사람이
치운다. 그리고 **생성한 골조가 골조가 아니면 등록하지 않는다**(`[PSEUDO]` 0개 = 완성 파일,
동결 0개 = 계약 불가). 원전이 값을 치르고 배운 실패다 — 글루 파일이 워커 태스크로 등재되던 버그.

테스트 `master/test_skeleton.py` **61건**, 전체 실패 0.

## §5. 🔴 토폴로지가 바뀌었다 — 쓰는 주체는 하나다 (사용자 확정 2026-08-11)

사용자가 원전 흐름을 기억해 짚었고, 이어서 이렇게 제안했다:

> *"워커들이 이 브랜치에 등록된 내용을 기준으로 추론을 하고 응답하면 메인 작업 컴퓨터가
> 빌드 되는지 적합한지 판단하고 원격 브랜치에 직접 커밋하는게 좋을꺼같아"*

    옛것   워커 N대가 각자 클론에서 수정 → 각자 커밋 → attempt push → 마커로 판정
    새것   워커는 **추론만** → 통합자가 **적용 → UE5 빌드 판정 → durable 커밋**

### 5.1 왜 이게 맞는가

옛 구조는 태스크를 **워커의 자기 신고**(`RESULT: DONE`)로 판정하고 *"컴파일되나"* 를 아무도
묻지 않았다. 🔴 **원전이 정확히 그 구조에서 사고를 냈다** — 빌드를 등재 *뒤*에 두었더니 깨진
골조가 `origin` 에 박히고 **워커 전원이 그 위에서 작업**했다. 빌드하는 주체를 커밋 주체로
만들면 그것이 **규율이 아니라 구조**가 된다.

내가 소 1.1.5(골조 빌드 게이트)와 소 2.3.5(통합 빌드)를 **따로 칸으로 만든 것 자체가** 옛
구조에서는 그것이 규율로만 지켜진다는 증거였다.

한 무리의 문제가 통째로 사라진다 — 워커 git 인증 · `attempt/` 찌꺼기 · *"워커는 브랜치를 못
지운다"* · 더티 트리 파견 차단 · CAS/펜싱의 절반. **전부 여러 대가 쓰기 때문에 있던 것**이다.
§5.2-D 가 이미 같은 방향으로 갔다(*"durable merge CAS·모델이 머지 권한은 1인 구도에선 불필요"*).

🔴 **포기하는 병렬은 이미 의심스러웠다**: 실측 2026-08-09 워커 2대는 **12% 빠르고 90% 비쌌다**
(캐시 생성이 워커별 고정비). 그리고 **층3 은 이미 `.2` 전용**이라 직렬이었다 — 직렬 병목은
**새로 생기는 게 아니라 위치가 옮겨진다.**

### 5.2 🔴 내가 다르게 본 한 가지 — 그 역할을 `.33` 이 아니라 `.2` 에

사용자의 첫 스케치는 적용·커밋을 **메인 작업 PC(`.33`)** 에 뒀다. 비교표를 만들어 제시했고
`.2` 안(B′)이 채택됐다.

| | `.33` 요청자 | `.2` 통합자 |
|---|---|---|
| UE5 | ✅ | ✅ 5.8 |
| 무인 구동 | ❌ 사람이 쓰는 기계 | ✅ **유일** |
| 가용성 | 🔴 낮다 — **이 세션 중에도 꺼져 있었다**(실측) | ✅ 전용·상시 |
| 게스트 규칙 | 🔴 읽기 전용 선호·worktree 강제·미커밋 `.uasset` 복구 불가 | 해당 없음 |

`.33` 은 원래 역할(등록·최종 리뷰·`[FEEDBACK]`)에 남는다 — **사람이 눈으로 볼 자리이지
파이프라인이 커밋을 밀어넣을 자리가 아니다.**

### 5.3 받아들이는 비용 두 개 (기록해 둔다)

1. **`.2` 가 적용+빌드의 단일 장애점이 된다.** 층3 이 이미 그랬으니 새 위험은 아니지만 더 많은
   일이 얹힌다.
2. **`.2` 의 추론 트리와 적용 트리가 겹치면 안 된다** → `git worktree` 로 가른다(소 1.4.1).
   ⚠️ **UE5 worktree 의 DDC/Binaries 재생성 비용은 여전히 미측정이다** — `.2` 에서 기존 체크아웃
   재사용을 택한 이유가 그 비용이었다(콜드 빌드 수십 분 예상). **강제 전에 잰다.**

### 5.4 무엇을 고쳤나 — 문서와 레드마인

    docs/settled-decisions.md      「Write authority — one writer」 절 신설
    docs/8-git-authority.md §8.4   「누가 무엇을 만드나」 표를 **대체**. 통합자 역할 추가
    docs/milestones/3-*.md         중 1.3 재정의(추론만) · **중 1.4 통합자 신설** · 사슬 갱신

레드마인: **중 1.4 + 소 4개**(#111~#115) 신설. 영향받은 이슈에 결정 노트 — #75 중 1.3 제목까지
바꿨고(「병렬 파견·순차 적용」 → 「병렬 추론 파견 — 커밋하지 않는다」), #77 소 1.3.2 는
「적용 풀」 → 「응답 인계」로 재정의, #107·#110 은 성격 변경을 적었다.

🔴 **닫힌 마일스톤 2 의 #38·#61·#63 에는 「유휴」 표시만 하고 닫힌 상태를 유지했다** — 당시
실제로 끝났고 왕복 1분 29초가 실측됐다. **소급해서 열면 기록이 거짓이 된다.** 유휴화는
*다음* 마일스톤의 사실이다.

⚠️ **코드는 지우지 않았다** — 워커 Gitea 키 · `attempt/` 브랜치 · `work/cleanup.py` 328줄.
측정으로 뒤집힐 여지가 있는 동안은 되돌릴 수 있어야 한다.

## §6. 골조 빌드 게이트 (소 1.1.5) — 🔴 **게이트가 세 번 일했다**

`master/work/skeleton_gate.py`. 마스터가 골조 텍스트를 **통합자 `.2`** 의 격리 트리에 놓고
UE5 를 세운 뒤, **통과해야만 등재**한다(`register_work(gate=…)`).

### 6.1 노는 기계를 썼다 (사용자 지적)

*"지금 다른 컴퓨터들 놀고 있는데 활용 안해?"* → 미측정으로 **막혀 있던 것 둘**을 그 기계들에
던졌다. 둘 다 결론이 나왔다.

### 6.2 🔴 UE5 worktree 비용 — 문서의 예상이 틀렸다

문서가 *"콜드 빌드 수십 분 예상"* 이라 적어 두고 그 때문에 `.2` 에서 **기존 체크아웃 재사용**을
택했었다(§8.4 ⚠️ 미측정). 실측:

    worktree 체크아웃   10초 · 2,229 파일 · 1.08 GB    (원본은 6,629 파일 · 9.91 GB)
    🔴 콜드 빌드        144초 · 11 actions · Succeeded
    빌드 후             2,529 파일 · 1.77 GB · 디스크 -1.8 GB
    이후 증분 빌드       35~65초

**엔진은 설치본을 그대로 쓰고 프로젝트 모듈 11개만 짓기 때문**이다(로그: `Output binary:
C:\Program Files\UE_5.8\...\UnrealEditor.exe`). worktree 에 없는 것(Binaries·Intermediate·
DDC·Saved)은 전부 gitignore 대상이라 애초에 복제 비용이 아니었다.

→ **소 1.4.1(워크트리 격리)의 선결 미결이 풀렸다.** 격리를 기본으로 둘 수 있다.
⚠️ 다만 **이것은 코드 빌드만이다.** 층3 `RunTests` 는 에디터를 띄우므로 DDC(셰이더)가 필요하고
**그건 아직 안 쟀다.**

### 6.3 🔴 게이트가 잡은 것 셋 — 전부 실재하는 결함

| 회차 | 판정 | 무엇이었나 |
|---|---|---|
| 1 | 🔴 크래시 | **층3 자체의 버그** — `UnicodeDecodeError` |
| 2 | 🔴 `BUILD FAILED` | 골조 **계약의 구멍** — 반환값 있는 함수에 return 이 없다 |
| 3 | 🔴 `BUILD FAILED` | **환각** — `EMissionType::None` (실재하지 않는 열거값) |
| 4 | ✅ `BUILD OK(61.6s)` | 통과 |

**① 층3 이 이 기계의 빌드 출력을 못 읽었다.** `_run` 이 `text=True`(strict UTF-8)라 한국어
윈도우의 UBT 출력에서 **디코딩이 먼저 죽었다** — 링커가 CP949 로 *"라이브러리 … 만들고
있습니다"* 를 뱉는다. 판정 문자열 `Result:` 는 ASCII 라 살아 있었는데 **거기 도달하지 못했다.**
🔴 `errors="replace"` 로 때우지 않았다 — 그러면 실패 사유의 한국어가 깨져 사람이 못 읽는다.
소스 44%가 CP949 라 이미 만들어 둔 `source_text.decode` 를 그대로 썼다.
**이 버그는 층3 을 실제로 돌려야만 나온다** — 단위 테스트 63건은 다 통과하고 있었다.

**② 골조는 "본문 없음" 이 아니라 "컴파일되는 최소 본문" 이다.**
`error C4716: 'IsSnapshotValid': 값을 반환해야 합니다`. `// [PSEUDO] …` 주석 하나만 두면
non-void 함수는 컴파일되지 않는다 — **모델의 실패가 아니라 계약의 구멍**이었다. 원전도 골조를
빌드해 통과시켰으니(Step 6) 같은 전제를 암묵적으로 갖고 있었다. 프롬프트가 이제 못박는다.

**③ 🔴 grounding 이 이름만 대고 내용을 안 줬다.**

    error C2065: 'None': 선언되지 않은 식별자입니다
    EMissionType MissionType = EMissionType::None;

`EMissionType` 은 실재하고 멤버는 `Main`/`Sub` 다. `None` 은 **다른 열거형**
(`EMissionExecutorState`)의 멤버 — 모델이 둘을 섞었다. `declarations.py` 가 고치려던
*"한 글자 차이"* 와 같은 부류다.

**왜 안 잡혔나**: 선언부는 `spec.classes` 만 실었는데 그 열거형은 **관계 블록이 include 로
이름만 댄 헤더**(`Table/TableEnum.h`)에 있었다. *"이 헤더를 문다"* 고 말하고 **내용은 안 준**
것이다. → `include_decls()` 신설: 관계가 이름 댄 헤더의 내용을 함께 싣는다(1,484자).
그 다음 구동에서 **`BUILD OK`**.

🔴 **셋 다 게이트가 없었으면 첫 분산 작업에서 N개 조각을 헛일로 만들고서야 드러났을 것이다.**

## §7. 골조 레인 A/B — 🔴 `DEFAULT_MODEL` 을 측정으로 확인했다

같은 과제·같은 grounding, 네 레인. 채점은 **결정적 지표만**(LLM 에게 우열을 묻지 않는다).

| 레인 | 동결 | `[PSEUDO]` | 글자 | 시간 | 결함 |
|---|---|---|---|---|---|
| **`claude:opus`** | **30** | 15 | 9,766 | 133초 | — |
| `agy` | 20 | 12 | 5,581 | **39초** | — |
| 35B (BC-250) | 10 | 12 | 2,455 | 83초 | — |
| `qwen2.5-coder:14b` | **6** | 9 | 2,009 | **28초** | 🔴 펜스 잔재 2건 |

**동결 = 계약이 될 선언 수**이고, 그것이 곧 *"워커가 못 바꾸는 것"* 의 크기다. opus 30 vs
14b **6 — 5배**. 동결이 6개면 워커가 인터페이스를 바꿔도 안 걸리는 부분이 훨씬 많다는 뜻이라,
**골조를 로컬로 만들면 병렬의 전제 자체가 헐거워진다.** 넷 다 요청 파일을 정확히 주고 그래프가
말한 부모를 지켰다 — 차이는 **계약의 촘촘함**에서 났다.

⚠️ 14b 의 펜스 잔재는 `generate.py` 주석이 이미 *"14b 의 유일한 결함은 펜스"* 라 적어 둔 것의
재현이다. **`agy` 는 실질 대안이다** — opus 의 2/3 계약을 3.4배 빠르게 낸다.

🔴 **추론으로 정한 `DEFAULT_MODEL = "claude:opus"` 가 측정으로 뒷받침됐다.** 이 저장소는
가중치를 눈으로 만지다 한 번 나빠진 전례가 있어 *"추측 대신 잰다"* 가 규칙이고, 여기도 지켰다.

## §8. 분해 (중 1.2) — §3 의 미결이 닫혔다

`master/work/decompose.py`. `docs/3-open-items.md` 가 남긴 *"적용 순서(의존 순서 vs 임의) ·
분할 입도"* 의 답:

    분할 입도   **파일 불가분 단위**(모듈+경로+stem). 원전의 "8~20개 파일" 은 안 쓴다 —
                개수가 아니라 **충돌 방지**가 기준이다
    적용 순서   🔴 **의존이 먼저**. `#include` 위상 정렬
    파일 내     `[PSEUDO:N]` 뎁스

⚠️ §4.5 가 *"동결돼 있으면 컴파일상 순서 무관이어야 하지만 확인 필요"* 라 적어 뒀던 예상은
**채택하지 않았다** — 선언이 없으면 컴파일이 깨지므로 순서는 실재한다.

### 8.1 🔴 두 종류의 "쪼갬" 을 구분해야 했다

    묶음 (동시)   서로 **다른 파일** → 병렬 파견 가능
    뎁스 (순차)   **같은 파일**을 단계로 → `depends_on` 으로 줄을 선다

이 구분이 없으면 겹침 검사가 **뎁스를 위반으로 잡는다**(같은 파일이 여러 조각에 나오니까).
그래서 검사를 **물결(wave) 단위**로 바꿨다 — *동시에 파견될 수 있는 것들*끼리만 본다.

### 8.2 🔴 테스트가 내가 넣은 결함을 잡았다

그래프 조회 실패를 예외로 삼켜 `deps = []` 로 두고 있었다. 그러면 **그래프가 통째로 죽어도
순서가 임의로 정해지고 아무도 모른다** — 이 저장소가 반복해 물린 *조용히 틀리는* 유형이다.
실패를 세어 보고하게 고쳤고, **"그래프가 아예 없다"** 와 **"조회가 실패했다"** 를 다른 메시지로
갈랐다(조치가 다르다: 앞은 `graph build`, 뒤는 조사).

⚠️ 순환은 **막지 않는다.** include 역추적이 basename 근사라 가짜 순환을 만들 수 있고,
*정상 작업을 막는 게이트가 통과시키는 게이트보다 나쁘다*(사실 게이트에서 이미 내린 판단).

## §9. 🔴 골조도 전자동이 아니다 — 대화형 휴리스틱 (소 1.1.6, 사용자 지시)

> *"사용자가 명령을 내렸고 컴퓨터 앞에 있으니 분산 작업에 대한 골조를 생성할 때 애매한 것이나
> 더 좋은 구조등에 대한 내용을 대화형으로 주고 받으면서 휴리스틱을 수집해"*

마일스톤 2 목표 ①이 온톨로지에 대해 못박은 원칙(*"수동 명령과 대화형" · 자동 승급 영구
비활성*)이 **골조에도 적용된다.** 오히려 더하다 — 골조는 모든 병렬 조각의 계약이라 애매한
선택을 모델이 조용히 하면 그 선택이 N개에 박히고, 그때는 이미 늦다.

🔴 **모델의 질문은 휴리스틱이 아니다. 사람의 답이 휴리스틱이다.** 모델 제안을 적립하면 다음
골조가 **자기가 한 말을 근거로** 같은 선택을 반복한다 — 근거 없는 확신이 쌓인다.

**두 되먹임 고리는 짝이다:**

    소 2.3.4  실패 카탈로그   층3 실패 → 다음 프롬프트   **기계가 모은다**
    소 1.1.6  휴리스틱        사람의 판단 → 다음 프롬프트  🔴 **사람이 답한 것만**

원전은 앞쪽만 있었다. 뒤쪽이 없으면 *"컴파일은 되지만 구조가 나쁜"* 선택은 **영원히 교정되지
않는다** — 빌드가 통과하니 카탈로그에 안 남는다.

**실전 첫 바퀴** — 골조가 물어 온 6개(전부 골조에 박히면 N개가 따라가는 선택):

    ① 컴포넌트 / 서브시스템 / 순수 UObject 중 무엇인가
    ② executor 를 BeginPlay 에서 찾을까, 주입받을까
    ③ 진행 상태가 정말 (MissionId, StepIndex, CompletedStepIds, Progress) 모양인가
    ④ 신선도 검사용 step serial 을 executor 가 노출해야 하나
    ⑤ 캡처가 미션을 넘어 살아남아야 하나
    ⑥ 상한 도달 시 가장 오래된 것을 버릴까, 거부할까

⚠️ **파싱 버그를 실전에서 잡았다** — 첫 `=== QUESTIONS ===` 기준으로 잘랐더니 모델이 본문
중간에 흘린 낱말에 걸려 **`.cpp` 를 통째로 잃었다**(파일 1개만 남았다). 원문을 저장해 확인한 뒤
**마지막 것 기준 + 줄 머리 앵커**로 바꿨다. 🔴 추측으로 고치지 않고 **원문을 봤다.**

## §10. 🔴 분산의 값이 재정의됐다 — 처리량이 아니라 증분 (사용자 2026-08-11)

> *"분산 작업의 장점은 비용이나 시간이라고 생각했는데, 어찌보면 증분이 아닐까해"* /
> *"물론 클로드를 통한 검증은 필수겠지만 말이야"*

**우리 실측이 처리량 쪽을 부정한다:**

    워커 2대 병렬   12% 빠르고 **90% 비쌌다**    캐시 생성이 워커별 고정비
    4파일 묶음      파일당 **3.2배 쌌다**        ← 묶음의 이득이지 병렬의 이득이 아니다
    층3            어차피 `.2` 한 대            검증은 원래 직렬

**병렬로 얻은 것은 거의 없다.** 그런데 이 마일스톤이 만든 장치들은 전부 다른 쪽을 향해 있었다:

| 장치 | 실제로 무엇을 위한 것인가 |
|---|---|
| 인터페이스 동결 | 조각들이 서로를 안 깨뜨린다 = **증분이 성립하는 조건** |
| 골조 빌드 게이트 | base 가 서 있어야 그 위에 쌓인다 |
| 의존 순서 | 증분이 쌓이는 순서 |
| 통합자가 통과분만 커밋 | 🔴 durable 이 **항상 컴파일되는 상태**로 누적된다 |

🔴 **그래서 검증이 선택이 아니라 전제다.** 검증 없이 쌓으면 증분이 아니라 부채다. 무료 로컬
대량 생산이 성립하는 이유는 *"싸서"* 가 아니라 **"틀린 것이 안 쌓여서"** 다. 원전이 *"reject 는
전진·수정"* 으로 뒤집은 것도 같은 이야기다 — 맞게 짠 코드를 버리면 증분이 아니게 된다.

⚠️ **경계는 안 움직인다**: 골조 A/B 가 보인 것(동결 30 vs 6)은 **골조=계약**이라 값을 쓸
자리라는 뜻이지, 본문까지 상용으로 하라는 뜻이 아니다.

반영: `docs/4-work-loop.md` §4.5(미결 절을 닫고 값의 정의를 바꿈) · `docs/settled-decisions.md` ·
마일스톤 3 성격 절 · `PLAN.md`.

### 9.1 🔴 범위가 없으면 오염이다 (사용자 지적, 같은 날 정정)

> *"내가 일단 요청한게 아니잖아 무엇에 대한 질문이야?"* /
> *"저 질문들은 답변하면 모든 분산 작업에 적용되는거야?"*

**셋 다 맞는 지적이었고 하나는 설계 버그였다.**

**① 그 질문들은 사용자가 요청한 것이 아니다.** `MissionTaskSnapshot` 은 **내가 레인 A/B 를
재려고 지어낸 고정 입력**인데, 그 골조가 낸 질문을 답이 필요한 것처럼 내밀었다. 벤치마크
산출물과 실제 작업을 섞어 보고한 것이다.

**② 흐름의 시작점은 요청이다.**

    보고한 것   (가상 과제) → 골조 → 질문 → 사람
    실제 흐름   🔴 사람이 "이런 기능이 필요하다" → 골조 초안 + 질문 → 답 → 확정 → 파견

**③ 🔴 설계 버그** — 답을 **전부 한 파일에 쌓고 모든 골조 프롬프트에 실었다.** *"이 스냅샷은
컴포넌트로 한다"* 가 **관계없는 다음 작업에도** 실린다는 뜻이다.

→ **범위를 넣었다. 범위도 사람이 정한다**(모델이 *"이건 전역 규약입니다"* 라고 판단하게 두면
적립을 사람에게 맡긴 이유가 사라진다):

    project      "액터 수명에 묶이는 상태는 컴포넌트로 둔다"   → 계속 적용
    domain:<D>   "MissionRuntime 에서는 …"                   → 그 도메인 작업에만
    once         "이번 스냅샷은 컴포넌트로"                    → 🔴 **적립하지 않는다**

골조 생성 시 **결정적 도메인 추론**(소 2.2.2)으로 이 작업의 도메인을 뽑아 거른다. 도메인을
모르면 `project` 만 싣는다. `once` 는 `SkeletonSpec.answers` 로 그 골조에만 실리고 버려진다 —
**재생성 라운드에서 앞 질문에 답한 것을 넣는 자리**다.

🔴 우리 온톨로지가 이미 도메인별 규범과 전역을 갈라 뒀고 원전도 작업유형 템플릿과 전역 실패
카탈로그를 갈라 뒀는데, 내가 그 구분을 뭉갰다. 테스트 40건(범위 필터·`once` 미적립 포함).

## §11. 세션 마감 — 문서 최신화

`README.md` 와 `master/README.md` 가 **오늘 만든 것을 통째로 모르고 있었다**(`skeleton` ·
`skeleton_gate` · `decompose` · `heuristics` 0건 언급). 채웠다:

    README.md              저장소 구조에 work/ 신규 4종 · 흐름도에 골조/게이트/통합자 ·
                           테스트 표 4행. 🔴 흐름도가 "워커가 커밋한다" 로 낡아 있었다
    master/README.md       모듈 4행 + work/ 요약을 새 토폴로지로
    CLAUDE.md              "M3 has not started building yet" → 중 1.1·1.2 닫힘.
                           🔴 **2026-08-11 에 바뀐 두 틀**을 못박았다(옛 근거로 논증하지 말 것):
                           쓰는 주체는 하나 · 분산의 값은 증분
    machines/sim-desktop.md  같음 + 게이트가 잡은 결함 셋

검사: 깨진 링크 **0** · 테스트 **실패 0** · `~/CLAUDE.md` 심볼릭 링크 정상 ·
레드마인 마일스톤 3 **13/51**(중분류 10, 제목에 분자 0, 버전 미지정 0).

### 오늘 남긴 상태

    .2   측정용 worktree `E:\trunk\ms-wt-probe` (detached, 빌드 캐시 따뜻함 — 다음 게이트 60초대)
         🔴 지우는 것은 사람이 판단한다:
         ssh janus@192.168.0.2 'cd /d E:\trunk\ModularStage && git worktree remove --force E:\trunk\ms-wt-probe'
    프로젝트  `skeleton-heuristics.md` 는 **아직 없다** — 실제 요청이 들어와야 첫 항목이 쌓인다

**다음**: 대 1 의 남은 둘 — 중 1.3(추론 파견) · 중 1.4(통합자). 🔴 **소 1.4.1 의 선결이던
worktree 비용은 오늘 재서 풀렸다**(콜드 144초·증분 35~65초·1.8GB) — 다만 `RunTests`(DDC)는
여전히 미측정이다.
