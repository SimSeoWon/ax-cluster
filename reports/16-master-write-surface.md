# 16. 마스터 쓰기 표면 — Flow Y 배선의 선결 (2026-08-14)

> 마일스톤 4 · 대 1 토폴로지 원복 · **중 1.2 2-tier 복원** (`#121`) — 잔여 `#123`·`#125`.
> [중요] **이 절은 실행 *전에* 썼다** (CLAUDE.md § 작업 일지 규칙). Gitea 는 공유 인프라다.

## §1 세션 시작 상태 (실측)

    ax-cluster 세 클론     ee65203 동일 · 작업트리 청결 (마스터·GitHub·.43)
    마스터 서비스          ax-task-queue · ax-broker · ax-projects · ax-indexer.path/.timer · ax-status.timer  전부 정상
    큐                     works 0 · 색인 게이트 paused=False("진행 중 작업 없음")
    기계                   .57 [완료] · .43 [완료](부팅 9분) · .2 [완료] · [중요] .33 꺼짐
    레드마인 M4            22/75 완료 (version *마일스톤 4* = id 3)

[주의] **리포트 15 의 종료 시퀀스는 ① 만 실행됐다** — `.2`·`.33`·`.57` 은 안 껐고, 마스터는 6일째
가동 중이다. `.43` 은 이 세션 직전에 물리 버튼으로 다시 켜졌다.

## §2 `#123` 의 정체 — 이식이 아니라 **배선**이 남아 있었다

`master/work/coordinator.py`(217줄)는 원전 `cluster_coordinator.py`(200줄) 함수 6개를 전부
갖고 있고 실 Gitea 실증도 통과했다(리포트 14 §15). [중요] **그런데 그것을 `import` 하는 곳은
`test_coordinator.py` 와 `selftest.py` 둘뿐이다** — 실 파이프라인(`runner`→`infer`→`integrate`)은
2-tier 브랜치를 **한 번도 쓰지 않는다.**

    실측: grep -rn "from .coordinator\|work.coordinator" --include=*.py .
          → master/test_coordinator.py:25 · master/work/selftest.py:51   (끝)

## §3 [중요] 갈림길 — Flow Y 는 마스터의 push 봉인과 정면으로 부딪힌다

Flow Y(사용자 확정 2026-08-14)는 *"마스터가 받은 diff 를 task/attempt 브랜치에 **commit**"* 이다.
그런데 마스터 정본의 push 는 **의도적으로 봉인**돼 있다:

    /home/sim/ax-cluster/ModularStage/repo
      origin  /var/lib/gitea/gitea-repositories/sim/modularstage.git   (fetch)
      origin  DISABLED://마스터는-소스저장소에-push하지-않는다-PLAN.md-2.1  (push)

근거는 `docs/2-architecture.md` §2.1·§2.2-1 *"마스터는 여전히 파일을 소유하지 않는다"* 이고,
`selftest.py` 가 1단계를 **임시 저장소**에서 돌린 이유도 이 봉인이었다.

**사용자 결정 (2026-08-14): 「별도 쓰기 표면 추가」.** 정본 `origin` 의 push 봉인은 **그대로
두고**(오발 push 를 시끄럽게 죽이는 가드로 남긴다), 같은 클론에 push 가능한 두 번째 remote 와
attempt 전용 worktree 를 둔다. [중요] 정본 트리는 계속 `main` 에 앉아 있으므로 §5.5 가 기대는
*"정본은 fetch 전용"* 불변식을 건드리지 않는다.

## §4 전송 경로 — 파일시스템 push 는 **불가능**하다 (실측)

    sg gitea -c 'ls -ld /var/lib/gitea/gitea-repositories/sim/modularstage.git'
    → drwxr-xr-x 8 gitea gitea      [중요] 그룹은 r-x, **쓰기 없음**

[중요] **그래서 bare 에 직접 push 할 수 없다.** 억지로 `g+w` 를 주면 Gitea 의 receive 훅·브랜치
보호·DB 갱신을 **우회**해 쓰게 된다 — 그건 가드를 여는 것이 아니라 없애는 것이다.
→ 쓰기는 **Gitea 정규 경로(SSH)** 로 간다. 작업장 `.43` 이 이미 그 경로를 쓴다(실측):

    origin  gitea@192.168.0.57:Sim/ModularStage.git   (fetch·push)

[주의] 그리고 `sim` 은 `/etc/group` 상 `gitea` 그룹인데 **이 대화형 셸 프로세스에는 그 그룹이 없다**
— §5.5 가 적어 둔 *"그룹 변경은 떠 있는 프로세스에 소급되지 않는다"* 함정. 읽기 확인은
`sg gitea -c` 로만 된다. 서비스는 `SupplementaryGroups=gitea` 로 이미 갖고 있다.

## §5 [중요] 별개 발견 — `main` 은 서버측 보호가 **없고**, 넣을 수도 없다

    GET /api/v1/repos/Sim/ModularStage/branch_protections  →  []
    GET /api/v1/user/keys  →  ax-worker-2 · ax-worker-43 · ax-requester-33   (마스터 키 없음)

[중요] **네 대가 전부 같은 Gitea 계정 `Sim` 으로 인증한다.** 등록된 것은 저장소별 배포키가 아니라
계정 키(`/user/keys`)다. Gitea 의 브랜치 보호 화이트리스트는 **사용자 단위**이므로, 같은 계정을
쓰는 기계들 사이를 **원리적으로 구분할 수 없다.** 즉 *"마스터는 `main` 에 못 쓴다"* 를 서버에
넣는 방법이 지금 구조에는 없다.

→ **가드는 클라이언트측에 둔다**(마스터 클론의 `pre-push` 훅). [중요] 이것은 이식 위에 얹는 개선이
아니라 **봉인을 여는 비용**이다 — 원전 인프라 PC 는 push 권한을 그냥 갖고 있었고, 우리는 §2.1
때문에 봉인을 뒀으므로, 봉인을 좁히려면 대체 가드가 같은 커밋에 있어야 한다
(마일스톤 4 「가드를 만들면 깨울 주체를 같은 커밋에 넣는다」의 같은 결).

[주의] **기계별 Gitea 신원 분리는 이 세션의 범위 밖이다** — 사용자 결정이 필요한 별개 항목이라
아래 「남은 것」에 적는다.

## §6 실행할 명령 (이 절은 실행 전에 적었다)

    ① 마스터 키 등록 — 워커에 한 것과 같은 방식 (§8.5)
       POST /api/v1/user/keys   title=ax-master-write   key=~/.ssh/id_rsa.pub
       되돌리기: DELETE /api/v1/user/keys/<id>

    ② 인증 확인
       ssh -T gitea@192.168.0.57                        → "authenticated with the key named ax-master-write"
       git ls-remote gitea@192.168.0.57:Sim/ModularStage.git

    ③ 쓰기 표면 추가 (정본 clone 안, origin 은 손대지 않는다)
       git -C ModularStage/repo remote add gitea-write gitea@192.168.0.57:Sim/ModularStage.git
       되돌리기: git remote remove gitea-write

    ④ [중요] 대체 가드 — pre-push 훅 (attempt/* · task/* 만 통과)
       ModularStage/repo/.git/hooks/pre-push
       되돌리기: rm 그 파일

    ⑤ 실증 — 훅이 막는 것과 통과시키는 것을 **둘 다** 잰다
       push --dry-run gitea-write HEAD:refs/heads/main            → [중요] 훅이 거부해야 한다
       push --dry-run gitea-write HEAD:refs/heads/attempt/probe/… → 통과해야 한다

## §7 실행 기록

    ① 키 등록      [완료] id=4 `ax-master-write` (SHA256:nJK1+FYa…)
    ② 인증 확인    [완료] "authenticated with the key named ax-master-write" · ls-remote 통과
    ③ 쓰기 표면    [완료] remote `gitea-write` 추가 · origin push 봉인은 **그대로**
    ④ pre-push     [완료] 원본 `master/work/pre_push_hook.sh` → 설치 `…/repo/.git/hooks/pre-push`
    ⑤ 실증         [완료] 아래 §8

### [중요] 스스로 잡은 규약 위반 하나 — 훅을 저장소 밖에만 뒀다

처음엔 훅을 `.git/hooks/pre-push` **에만** 썼다. [중요] **그 디렉토리는 어느 저장소도 추적하지
않는다** — 클론을 다시 만드는 순간 **가드가 조용히 사라진다.** 가드에는 최악의 실패다.
그리고 이 저장소는 그 함정을 **이미 문서로 못박아 뒀다**: `events/post_receive_hook.py` 머리말
*"이 파일이 원본이고 `install_hook.py` 가 복사한다. 훅을 저장소 밖에서만 고치면 다음 사람이
원본을 못 찾는다."*

→ 같은 규약으로 정정했다: 원본은 `master/work/pre_push_hook.sh`(SSOT), 설치·검증은

    python -m master.work.attempt status        # 설치됐나 · 원본과 같나 · 실행 가능한가
    python -m master.work.attempt install-hook  # 원본에서 설치 (덮어쓴다 — 원본이 SSOT)

[주의] 검증기가 **첫 실행에서 불일치를 잡았다**(설치본에 SSOT 머리말이 없었다) — 즉 *"설치돼 있다"*
와 *"원본과 같다"* 를 가르는 것이 실제로 일한다.

## §8 가드 실증 — [중요] **허용과 거부를 둘 다 쟀다**

| | 결과 |
|---|---|
| `attempt/guardprobe/master/t1` (실 dry-run) | [완료] `[new branch]` — 훅 통과 |
| `probe-guard-check` (실 dry-run) | [중요] 훅 거부 |
| `main` (훅 직접 입력) | [중요] 훅 거부 |
| `main` 되감기 (실 dry-run) | [주의] **훅이 아니라 git 의 non-fast-forward 가 막았다** |
| 삭제 — 미병합 attempt | [중요] 거부 (병합 안 됨 = 증거) |
| 삭제 — 병합된 durable | [완료] 통과 |
| 삭제 — tracking ref 없음 | [중요] 거부 (판정 불가 → **보존**) |
| dry-run 이 만든 ref | **0건** (`ls-remote` 로 확인) · 정본 청결·`main` 유지 |

[중요] **측정 도구가 조용히 틀린 자리 하나 — 스스로 잡았다.** `main` push 를 dry-run 으로 재려
했는데 정본 HEAD == 원격 `main` 이라 *"Everything up-to-date"* 로 **훅이 아예 안 불렸다.**
되감기(`HEAD~1`)로 바꿨더니 이번엔 **git 의 ff 검사**가 먼저 막아 역시 훅이 안 불렸다.
→ 훅에 **직접 입력을 넣어** 판정했다. [주의] 마일스톤 4 의 *"측정 도구 자신이 조용히 틀린다"* 가
이 세션에도 나왔다. **`rc` 가 0/1 인 것만 보고 「가드가 동작했다」로 접으면 틀린다 — 누가
막았는지를 봐야 한다.**

[주의] **삭제 검증은 git 을 부르지 않고 훅에 직접 입력했다.** `CLAUDE.md` 가 대화형 세션의 브랜치
삭제를 승인이 있어도 금지하므로, `--dry-run` 이어도 그 명령을 만들지 않았다.

## §9 소 1.2.2 (`#123`) — 마스터측 attempt 계층을 **배선**했다

신규 `master/work/attempt.py`. [중요] 이식본 `coordinator.py` 의 함수를 **그대로 쓴다** —
`assign_attempt` · `push_attempt` · `verify_and_merge`, 그리고 원전이 만들어 둔 **주입 seam**
(`work_fn`)에 실전용 적용 함수를 끼운다. 드라이런(`fake_workshop`)과 실전이 **같은 경로**를 탄다.

    ensure_attempt_tree   <프로젝트>/ax-wt-<work_id> — 정본 **옆**. 정본은 main 에 남는다
    apply_responses       Response.files({경로: 완성 본문}) 를 쓰고 **되읽어 확인**
    push_attempt_for      [중요] 성공·실패 **무관** push. 이 계층은 판정하지 않는다
    merge_verified        epoch 게이트 → 통과분만 durable

**원전과 다르게 둔 것 하나 추가** (`coordinator.py` 머리말 ④): `remote` 가 인자다. 원전 인프라
PC 는 `origin` 에 push 할 수 있었고 우리는 봉인돼 있으니, **그 인자 하나가 환경 차이를 전부
흡수한다.** 기본값은 원전 동작(`origin`) — `selftest` 의 임시 저장소가 그 경로다.

### [중요] `#115` 와 §8.4 의 모순을 정리했다

`integrate.py` 의 *"실패는 커밋하지 않는다"* 는 근거가 *"쓰는 주체가 하나라 **실패 attempt 를
남길 브랜치가 없다**"* 였다. **그 전제가 사라졌다** — attempt 계층이 돌아왔으므로 §8.4
*"실패도 커밋한다"* 가 맞다. [주의] **문장을 지우지 않고** 그 파일에 *"이 토폴로지에서만"* 이라는
교차 참조를 달았다(§8.4 규약: 두 토폴로지를 나란히 두고 측정으로 고른다).

### 실 구동 (임시 저장소 아님 — 실제 Gitea)

    attempt push        cfc419fd → attempt/probe-flowy/master/20260814-live1  [완료] 원격 확인
    본문 왕복           .ax/probe/flowy.md 내용 일치 [완료]
    [중요] 정본 불변        b1ba6af9 → b1ba6af9 · branch main · 청결 [완료]
    좀비 epoch (1<2)    [중요] 거부 · durable **안 생김** (부작용 0) [완료]
    epoch 통과 (2==2)   durable task/probe-flowy 신규 생성 [완료] 원격 확인
    색인기 반응         깨어남 → `b1ba6af9→b1ba6af9 · 재색인 건너뜀` [완료] 스풀 잔량 0

[중요] **색인기 반응은 예측을 실측으로 확인한 것이다.** `post_receive_hook` 은 **ref 를 안 가리고**
전부 스풀에 넣지만, `consumer.process_event` 는 `ev.ref` 를 **보지 않고** 정본에서
`merge --ff-only FETCH_HEAD` 를 하고 그 for-merge 항목은 `main` 의 업스트림뿐이다 →
attempt push 는 `from == to`. 리포트 14 §17 이 이미 잰 경로와 같다.

### [중요] 이식본이 잡은 우리 결함 — locale

테스트 ⑧(같은 내용 재제출)이 실패했다. *"변경이 없다"* 를 `"nothing to commit" in str(e)` 로
판정했는데 **이 기계는 `ko_KR.UTF-8` 이라 git 이 한국어로 말한다.** 트리 상태(`status
--porcelain`)로 판정하도록 고쳤다. [주의] **`integrate.py` 는 아직 문구 대조를 쓴다** — 거기는 git 이
영어인 윈도우 작업장에서 도는 코드라 지금은 맞지만, **마스터로 옮겨오면 같은 자리에서 깨진다.**
같은 부류를 `plan_remote_cleanup` 에서도 미리 없앴다(종료코드 대신 `merge-base` **값** 비교).

## §10 소 1.2.4 (`#125`) — 정리 시점: **도달 가능한 것만** (사용자 결정 2026-08-14)

결정을 부르기 전에 실측했고, 그 실측이 선택을 바꿨다:

    원격 attempt 4건    [중요] **전부** main 에서 도달 불가 (그 커밋이 거기만 있다)
    원격 durable 5건    4건은 병합됨(bc4b38f4) · 1건은 이 세션 probe
    응답 스풀           **1건뿐** — 2026-08-09 태스크는 **하나도 없다**

[중요] **원전대로 work 종료 시 `delete=True` 를 돌렸다면 실패 시도 3건의 유일한 사본이 사라졌다.**
[주의] 단서: 그 셋은 스풀 설계(소 1.3.2) *이전*의 산물이다. 이후 작업은 스풀이 유료 LLM 출력을
들고 있어 조건이 다르다 — 그래도 채택한 기준이 더 안전하다. **병합되면 자동으로 삭제 대상이
되므로 영구 누적이 아니다**(테스트로 그 성질을 고정했다).

기준은 `work/cleanup.py` 가 로컬 브랜치에 쓰는 것과 **같은 것 하나**이고, **두 겹**으로 넣었다:

    호출자   attempt.plan_remote_cleanup / apply_remote_cleanup — 계획을 내고 그것만 지운다
    최후방   클론의 pre-push 훅 — 같은 기준을 **한 번 더** 판정한다

[주의] **범위는 ephemeral 뿐 — durable 은 원전대로 보존**하고 세어서 보고만 한다.
[중요] **판정 불가는 언제나 보존**이다(tracking ref 없음·fetch 실패·git 실패).

**실 저장소 계획 실행(삭제 없음)** — 손 측정과 정확히 일치:

    ephemeral 삭제 대상 0 · 보존 4 · durable 5건(건드리지 않음)

## §11 중 4.1 (`#172`/`#173`) — `AGENTS.md` 5조 대조·이식

원전 `.agents/AGENTS.md` 21줄. [중요] **조항마다 판정이 갈렸다** — 다섯을 뭉텅이로 옮기지 않았다.

| 원전 조항 | 우리 상태 | 판정 |
|---|---|---|
| **3조** 모호성 제거 | [중요] **부분 누락** — 우리 건 *"설계 갈림길"* 만 다뤘다 | **이식** |
| **4조** 상세한 사전 설명 (검토 비용) | [중요] 없다 | **이식** |
| **5조** 직접 빌드·커밋 금지 | 커밋은 있다(의도적 완화) · [중요] **빌드 조항 없음** | **부분 이식** |
| **1조** 임의 수정 금지 | 없다 | **범위 좁혀 이식** (사용자 결정) |
| **2조** 툴 사용 사전 동의 | 없다 | [중요] **안 옮긴다** (근거 아래) |

### [중요] 3조가 이 세션에서 실제로 걸렸다 — 그래서 값이 측정된 이식이다

원전: *"사용자 입력이 **작업 지시인지 단순 질문인지** 모호한 경우, 임의로 해석하여 행동에 나서지
말고 반드시 질문을 던져 의도를 재확인하십시오."* 우리 규칙은 *"설계에 갈림길이 있으면 묻는다"*
뿐이었다 — **입력 자체의 모호성**은 안 덮었다.

실측: 같은 세션에서 `응` 이 **두 번** 왔다. 첫 번은 커밋 승인이었고 **두 번은 단순 수긍**이었다
(앞에 물음이 없었다). 두 번째를 승인으로 읽었으면 **다음 마일스톤 작업을 임의로 시작**했을 것이다.
[중요] 이것은 원전이 *"1인칭 서술(「중간 커밋하고 올게」)을 나에게 내린 지시로 오독"* 해 사고를 낸 것과
**같은 계열**이고, 원전은 그 규칙을 **이미 문서로 갖고 있었다.**

### 1조 — 범위는 사용자가 정했다 (2026-08-14)

원전 1조는 *모든* 파일을 덮지만, 그 조항이 겨눈 조건은 **사람의 UE5 프로젝트를 에이전트가 직접
만지는 상황**이었다. 우리에게 그 조건이 실재하는 자리는 둘이고, 사용자가 그 범위를 골랐다:

    ① 자동 로드 문서   CLAUDE.md 전부 — [중요] 한 줄이 **모든 다음 세션의 행동**을 바꾼다
    ② 게임 소스        미러·워크트리·원격 체크아웃 (미커밋 .uasset 은 복구 불가)

도구 코드(`master/`·`docs/`·`reports/`)는 **커밋 승인 게이트**가 그 역할을 한다 — 커밋 전에는
아무것도 영구화되지 않는다. [주의] 이번 세션에 제가 자동 로드 문서를 **사전 동의 없이** 고쳤고
(§9 의 문서 정정 4곳), 그 사실이 이 범위를 고르는 근거로 쓰였다.

### [중요] 2조는 안 옮긴다 — 근거

*"단순 파일 읽기라도 몰래 툴을 호출해서는 안 된다."* 그 조항이 겨눈 조건은 **사람이 툴 호출을 볼
수 없는 에이전트**다. Claude Code 는 모든 툴 호출을 터미널에 띄우므로 *"몰래 호출"* 이 **구조적으로
불가능**하다 — 옮기면 카고 컬트다. [주의] 조항의 나머지 절반(*"무엇을 하려는지 사전에 간략히 설명"*)은
이미 관례이고 **4조 이식이 그것을 문서화**한다. [중요] **재범위는 닫지 않는다** — 무인 잡(cron·타이머)
세션은 사람이 보지 않으므로 전제가 다르다.

### [중요] 이식 후 분리 — 자동 로드 문서의 길이는 세금이다 (사용자 지시 *"기능에 따라 분리해"*)

5조를 인라인으로 넣자 `CLAUDE.md` 가 **+32줄**이 됐다. 그 파일은 스스로 *"이 파일은 색인이고 길이는
매 작업의 세금"* 이라 적어 뒀으므로, 사용자 지시로 **기능별 문서**를 신설해 근거를 옮겼다:

    docs/11-agent-conduct.md   §11.1 묻기 · §11.2 만들기 전에 읽기 · §11.3 수정 권한
                               §11.4 승인과 검토 비용 · §11.5 실행 권한 · [중요] §11.6 원전 5조 대조표

`CLAUDE.md` 양쪽에는 **한 줄 규칙 + 포인터**만 남겼다. [중요] **규칙을 지운 것이 아니라 근거를 옮긴
것이다** — 규칙 자체는 인라인에 남아야 한다(*"놓치면 안 되는 것만 남긴다"*).

    CLAUDE.md                 214 → 253 → **226줄**  (세션 순증 +12)
    machines/sim-desktop.md   219 → 228 → **225줄**  (세션 순증 +6)

[주의] 순증 +12 의 대부분은 5조가 아니라 **§9 의 토폴로지 정정**이다(틀린 하드 규칙을 고친 것이라
줄일 수 없다). `PLAN.md` 의 `docs/` 색인에 11번 행을 추가했다 — [중요] **색인에 안 올리면 다음 사람이
그 문서를 못 찾는다**(원전 훅 규약과 같은 결).

## §12 와처 서버 기능 복각 — 착수 (사용자 지시 2026-08-14)

> *"개선보단 일단 복각이 먼저야. 개선할 내용은 기록만 해두고, 리눅스 환경에서 기존 와처가 하던
> 서버 기능을 복각 먼저해! 작동하는 상태를 만들어야 실증 로그를 심어서 문제가 될 것이 있는지
> 보고 개선할 게 있는지 찾지."*

### §12.1 무엇이 미복각인가 (실측)

`watch.py` 가 실제로 `import` 해서 구동하는 것이 **서버 기능의 정의**다. 능력 단위로 맞춰 봤다:

    planner · plan_generator · plan_dedup · review_collector · issue_classifier
    recipes_synthesizer · history_harvest · rag_report · search_log_analyzer
    context_audit · client_index · domain_health · ontology_declared
    ontology_invalidation · ontology_descriptions · ontology_pkg_audit
    → [중요] **16개 전부 미복각** (`recipes` 히트는 실패 카탈로그·웹UI 문구로 무관)

[중요] **그리고 얹힐 자리가 없다.** 원전은 한 루프 안의 **유휴 사이클** + **시간 게이트**
(`watch_state._should_run_*` / `_mark_*_done`)로 배치를 돌렸다. 우리는 `watch_state` 이식 때
**`in_progress` 일시정지만**(`#164`) 가져왔다. 주기 구동 주체는 지금 **둘뿐**(색인 따라잡기 ·
상태 스냅샷). **이식해도 아무것도 돌지 않는다** → `#192` 신설(분모 75 → 76).

### §12.2 [중요] `PLAN.md`·`3-open-items.md` 를 안 읽고 만들려 했다 (사용자 지적)

사용자가 *"레드마인과 플랜은 읽었어?"* 로 물어 드러났다. **레드마인은 읽었고(이슈 75건 목록 +
손댄 것 본문), `PLAN.md` 와 `docs/3-open-items.md` 는 안 읽었다.** 후자는 `CLAUDE.md`·`PLAN.md`
둘 다 *"start here before working"* 로 가리키는 문서다. 읽었더니 **내가 만들려던 것에 직접 걸리는
확정 사항 둘**이 있었다:

    §5.4.2 (확정 2026-08-07)  [중요] watch.py 의 `while True` 폴링 루프는 **이식 대상이 아니다**
                              (Gitea 이벤트 구동). 단 "상주 프로세스 자체는 여전히 필요"
    §5.4.3 (확정 2026-08-07)  process_lifecycle.py · network_firewall.py 는 **폐기 확정** —
                              리눅스 등가물로 고쳐 쓰지 않고 **systemd 에 넘긴다**

→ 배치의 자리는 **반드시 systemd timer** 다. 원전처럼 루프 안 유휴 사이클로 복각하면 **이미
기각된 폴링 루프를 되살리는 것**이 된다. [중요] **방향은 맞았지만 근거 없이 맞았을 뿐이었다** —
이것이 *"만들기 전에 읽는다"* 가 규칙인 이유다.

[주의] **그 김에 낡은 것 둘을 발견했다 (기록만 — 사용자 지시대로 개선은 진행하지 않는다)**:
`PLAN.md` 「Currently running」 표에 서비스 **3종만**(유닛 6종 누락) · `docs/3-open-items.md` 의
[진행] 표시들이 **`locked` 된 마일스톤 3** 을 가리킨다.

### §12.3 복각 1번은 **기록기**였다 — 분석기의 상류 (`#193`)

`#161`(원전 `search_log_analyzer` 696줄)을 첫 배치로 잡았다가, 이식 전에 **입력이 실재하는지**
쟀다. [중요] **우리는 검색 로그를 아예 쓰지 않는다** — 파일 0건, 코드 히트 0건. 유일한 히트가
우리 `bm25.py:256` 의 주석이었다:

> *"이 규칙은 질의 3개로 정했다. 표본이 얇다 — `search_log.jsonl` 이 쌓이면…"*

[주의] **누락이 아니라 의도적 보류였다** — `search.py:16` 이 이식 당시 *"검색 로그(`_log_search`)
→ §5.2-D 로 별도 판단 (감사 계층)"* 로 적어 뒀다. 그 보류를 푸는 근거가 셋 생겼다: ① 우리
코드가 이미 요청 ② `#161` 의 입력 ③ 사용자가 말한 *"실증 로그를 심어서"* 가 정확히 이것이다.
→ `#193` 신설(분모 76 → 77).

**이식분** (`master/context_search/search_log.py`, 원전 `cs_common` 179~245·314~323·150~173):

    detect_lang            5배 규칙까지 원전 그대로 (바꾸면 과거 리포트와 비교 불가)
    build_result_details   [중요] `Hit` **필드를 직접** 읽는다 (아래 함정)
    log_search             원전 스키마 그대로 · [중요] 검색을 절대 막지 않는다
    rotate                 상한 2000건, **최신**을 남긴다
    read_entries           `#161` 의 입력 · 깨진 줄은 건너뛴다

**자리**: `search_context_tool`(*"검색 입구는 이것 하나다"*) — 원전도 MCP/HTTP **표면**에서
기록한다. [주의] 내부 소비자(`context_synth`)는 기록하지 않는다 — 원전 `context.py` 도 안 한다.
합성이 스스로 만든 질의를 섞으면 분포가 오염된다.

#### [주의] 함정 — `Hit.to_dict()` 로 적으면 채널 분해가 조용히 전부 빈다

`to_dict()` 는 응답 축소용이라 `bm25_score`·`vector_rank`·`bm25_rank` 를 **버린다.** 그런데
원전 `result_details` 는 **그 셋이 본체**다(채널 기여도 비교가 목적). 필드를 직접 읽었고,
테스트로 고정했다(`rank 0` 이 사라지지 않는 것까지 — 0 은 1위다).

#### [중요] 원전과 다르게 둔 것 — 조용히 실패하지 않는다

원전은 `_log_search`·`_rotate_search_log` 둘 다 `except Exception: pass` 다. **fail-open 자체는
옮겼다**(로그가 검색을 막으면 더 나쁘다). 다만 **소리 없이는 안 된다** — 중 4.2 에서 이식한
σ 감사의 기준이 *"조용한 실패는 결함"* 이고 *"예외를 값으로 돌려주면 조용하지 않다"* 다.
그래서 `{"ok": False, "reason": …}` 를 돌리고 응답에 `logged`/`log_error` 로 실어 보낸다.

#### 실 구동 (실제 색인·실제 검색)

    질의        [미션시스템]에서 [완료]시 [연출] 부분이 어디야?
    확장 후     미션시스템 완료 연출 UAlphaManager_Mission
    hits 5 · logged True · 파일 1,421B 생성
    기록 확인   query(받은 그대로) · lang=ko · caller=mcp_direct · zero_hit 없음(2건 이상)
    [중요] 채널 필드 살아 있음 — vec_rank 1 · bm25_rank 1 · similarity 0.6001 · bm25_score 10.6428

[주의] **관측 하나 (기록만)**: 1위가 `_archive/project_alpha_md/…/AlphaUIHUD_Mission.md` 였다 —
**아카이브된 Project_Alpha 문서가 현행 소스보다 앞선다.** 분석기(`#161`)가 잡아낼 부류이고,
지금 고치지 않는다(개선은 기록만).

[주의] **돌고 있는 `ax-projects` 서비스는 아직 옛 코드를 물고 있다** — 실운영 수집은 재기동 후에
시작된다. [중요] 재기동은 방금 이식한 원전 5조에 걸리므로 **승인을 받고 한다.**

### §12.4 복각 2번 — 자리(`#192`) + 첫 배치(`#161`)를 같은 커밋에

**자리** `master/batch/` + `ax-batch.timer`. [중요] **폴링 루프로 복각하지 않았다** — §12.2 가 찾은
확정(§5.4.2·§5.4.3)이 그것을 금지한다.

    gates.py   원전 `watch_state` 84~233 이식 — 조건 다섯을 **원전 순서 그대로**:
               ① 활성 ② 입력 최소치 ③ 시각 ④ 요일 ⑤ 최소 간격(daily 12h · weekly 6d)
               + `_last_<key>` 마커
    run.py     구동 + CLI. [중요] **`plan` 이 기본**(부작용 0) · `run` · `status` · `--force`

**원전과 다르게 둔 것 둘**

**① 게이트를 하나로 접었다.** 원전은 배치마다 함수를 따로 뒀지만 스스로 *"recipe 합성 게이트와
**동일 구조**"* 라 적어 뒀다 — 조건이 같고 상수만 다르다. `Schedule` 하나로 접고 배치별 값을
준다. **재설계가 아니라 같은 것을 한 번만 쓰는 것**이다.

**② `now` 주입 가능.** 원전은 `datetime.now()` 를 직접 부른다. [중요] **원전 자신이 같은 이유로
`attempt_branch(ts=…)` 를 인자로 뒀다**(*"테스트에서 시간을 고정할 수 있어야 한다"*).

[주의] **`rag_analysis` 게이트만 이식했다.** `weekly_plan`·`recipe_synthesis` 는 그 배치(`#155`·
`#157`)가 없다 — 게이트만 옮기면 §12.3 에서 한 번 피한 *"입력 없는 배치"* 와 같은 실수다.

#### [중요] 순서가 설계다 — 일시 중지 게이트가 **밖**에 있다

    ① events.gate.check()   작업 중이면 **전부 미룬다** (원전: git lock 경쟁) · 종료코드 4
    ② 배치별 시간 게이트     때가 됐나
    ③ 실행 → [중요] **성공했을 때만** 마커

뒤집으면 작업 중에도 시각만 맞으면 도는 창이 생긴다. 종료코드 **4 = 일시 중지**는 색인기와
**같은 규약**이다(`SuccessExitStatus=4` · *"늘 빨간 유닛은 유닛이 아니다"*).

#### [주의] `--force` 의 문서와 코드가 어긋나 있었다 (스스로 잡음)

독스트링은 *"시간 게이트만 넘는다"* 였는데 코드는 **활성 플래그까지** 넘고 있었다.
[중요] **운영자가 명시적으로 끈 것을 도구가 되살리면 안 된다** → 코드를 문서에 맞췄다:

    넘는다     시각 · 요일 · 최소 간격 · 입력 최소치 (얇아도 안전 — 분석기가 판단을 보류한다)
    못 넘는다  [중요] `batch.<key>=false` · [중요] 일시 중지 게이트

**첫 배치** `context_search/search_log_analyzer.py` — 원전 696줄 이식, **stdlib·LLM 0**.
임계값·판정 문구는 **원전 그대로**(바꾸면 과거 리포트와 비교 불가). [주의] `tag` 집계는 우리에게
늘 0 이다 — 융합이 2채널이라 그렇고, **그 0 이 리포트에서 그 사실을 말해 준다**(지우지 않았다).

#### 실 구동 — 게이트가 막는 것과 통과시키는 것을 **둘 다** 쟀다

    plan (게이트 실측)   · 안 돈다 rag_analysis (입력 2건) — 입력 부족 2 < 10건
    status               weekly sunday 23시 · 최소 10건 · 마지막 없음
    run --force          ▶ 실행 · 엔트리 2건 · 파일 2개 · 마커 2026-08-14T23:28:03

산출물 확인 — `rag_channel_report.md` · `language_gap_report.md`:

    [중요] 표본 가드가 작동했다   "표본 2건 < 최소 30건 — 자동 판정 보류 (수치는 참고용)"
    채널 기여도             vector+bm25 4 (50%) · vector 3 (37.5%) · bm25 1 (12.5%)
    RRF k=40/60/80          overlap 1.000 · top-1 변경 0% (표본 2건이니 당연하다)
    언어 버킷               ko 1 · en 1 — 두 표면이 서로 다른 언어를 물어 왔다
    도메인 빈도             Source 7 · [중요] **_archive 1**

[중요] **첫 리포트가 「판단 보류」로 나온 것이 정상이고 옳다** — 로그를 오늘 심었으므로(§12.3)
표본이 한 자릿수다. 이 가드가 없으면 2건으로 채널 결론을 내렸을 것이다.

#### 유닛 설치 (사용자 승인 2026-08-14)

`systemd-analyze verify` 통과 후 **승인을 받고** 설치했다 — 타이머를 켜는 것은 원전 5조
(빌드·배포·데몬)에 걸린다. 설치는 기존 유닛과 **같은 방식**(`/etc/systemd/system` + `pkexec`,
규약: *"마스터가 직접 처리, 사용자에게 명령을 떠넘기지 않는다"*).

    enable --now  → timers.target.wants 심볼릭 링크 생성 · 즉시 1회 발화
    실제 발화 2회  23:35:58(enable) · 23:36:08(수동) — 둘 다 `입력 부족 2 < 10건` · 종료 0
    다음 발화     00:38:36 (1시간 뒤)

[중요] **실 systemd 경로로 게이트가 막는 것을 확인했다** — 유닛이 도는 것과 **배치가 도는 것**은
다르다. 타이머는 「확인할 때」를 정하고 실행 여부는 게이트가 정한다. 로그가 그 판정을 그대로
남기므로 *"왜 안 돌았나"* 가 항상 보인다(고장과 구별되는 조건).

[주의] 유닛이 이제 **7종**이다(HTTP 3 + path 1 + timer 3). `PLAN.md` 표를 그에 맞게 정정했다.

### §12.5 복각 3번 — 컨텍스트 MD 진단 (`#160`) · [중요] **첫 감사가 곧 실측을 냈다**

**추천을 측정으로 골랐다.** 후보 다섯의 **입력이 실재하는지** 먼저 쟀다:

    #155 planner · #156 review_collector    `.claude/reviews/`   [중요] 0건
    #157 recipes_synthesizer                `_signals.jsonl`     [중요] 0건
    #158 history_harvest                    `.claude/history/`   [중요] 0건
    #160 context_audit                      컨텍스트 MD          [완료] **1,057건**

[중요] **중 3.3(되먹임 기계 7종)이 그룹째로 막혀 있다** — 넷이 0건인 이유가 같다. **우리는 그 입력을
만드는 쪽(코드 리뷰 생성 · code-writer 신호 · frontmatter 있는 history)을 안 옮겼다.** 지금
이식하면 §12.3 에서 피한 *"입력 없는 배치"* 를 **네 번 반복**한다. 이 의존은 마일스톤 문서에
없었다 — 읽어서 나온 것이라 적어 둔다.

#### [중요] 이식 형태를 정정했다 — 배치가 아니라 **요청 시** 도는 것이다

처음엔 `master/batch/` 에 얹으려 했는데, `watch.py` 는 `context_audit` 을 유휴 사이클에서
**부르지 않는다**(`import` 목록에 없다). 원전 통로는 셋이었다: MCP `audit_context_md` ·
HTTP · 스킬 `/audit-context`. **배치화는 원전에 없는 내 발명**이므로 하지 않고 **개선 후보로만
기록**했다(사용자 지시: 개선은 기록만).

→ 이식: `context_search/context_audit.py` + MCP 도구 **둘**. 두 번째는 `audit_tools.py` 에 있던
`analyze_search_log` 로, **§12.4 에서 이식한 분석기의 사람 입구**다(리포트를 손으로 뽑는 자리).

#### [중요] 분류 순서가 판정을 바꾼다 — 실측으로 증명됐다

원전 순서는 `dir_named` → `orphan_stem` → 내용 분류다. 이식 전 사전 스캔은 *"본문 50자 미만
147건"* 이었는데, 감사 결과 `shell_only` 는 **39건**이다. 차이 108건은 `orphan_stem` 으로
**먼저** 잡혔다 — 즉 순서를 바꿨으면 **재생성할 소스가 없는 108건이 「재생성 대상」**이 됐다.
원전이 그 순서를 고른 이유가 값으로 나왔다.

#### 실 구동 (트윈 1,037건 전수)

    valid          886  (85.4%)  [완료]
    shell_only      39  ( 3.8%)  [주의] **현행 소스**의 문서가 껍데기다 — 재생성 대상
    orphan_stem    112  (10.8%)  [주의] 대응 소스가 없다
    나머지 5분류      0           (펜스 손상 0 — 그 유형은 우리에게 없다)

#### [중요] 감사가 찾은 것 — 좀비 11건, 그중 8건이 **미전파된 개명**

`orphan_stem` 112건 중 101건은 `_archive/` 다(예상대로). [중요] **나머지 11건은 현행 경로**였고,
**거짓 발견을 피하려고 전수 확인**했다 — 동명·prefix 형태 둘 다 저장소 전체에 없었다:

    .claude/mcp/uelog_analyzer.md
    Source/ModularStage/UI/Base/UIWidgetBase.md · UI/Subsystems/UISubsystem.md
    Source/ModularStageEditor/{Private,Public}/Widget/EditorView_MissionTask{Editor,_Annihilation,_Destination,_Wait}.md   ← 8건

**그 8건의 정체를 확인했다**: 저장소에는 `EditorView_MissionTask_Wait_**Renewal**.h` 가 있다.
개명이 일어났고 그 문서의 `related_classes` 는 아직 죽은 경로를 가리킨다.

> [중요] **정정 (같은 세션) — 나는 이것을 `#167`(개명 cascade)의 실증이라고 적었고, 그것은 틀렸다.**
>
> 사용자가 그 근거로 `#167` 을 다음 작업으로 골랐고, 착수 전에 확인하다가 드러났다.
> stale 을 셀 때 SQL 패턴을 `%EditorView_MissionTask%` 로 잡아 **현존하는 정상 클래스**
> `UEditorView_MissionTask**DetailBase**` 를 「옛이름」으로 셌다. 정확한 패턴으로 다시 재니:
>
>     class_graph.db · dependency_graph.db · bm25*.db · 온톨로지 YAML  →  [중요] stale **0건**
>
> 전부 `_Renewal` 로 정확히 갱신돼 있다 — **개명 cascade 는 이 사례에서 제대로 작동했다.**
>
> **진짜 기제는 다르다**: 소스 6개 vs 컨텍스트 MD 10개. 개명 전 이름의 MD 4종(Private/Public
> 합쳐 8파일)이 **그냥 남아 있다** — [중요] **색인기가 사라진 소스의 문서를 지우지 않는다.**
> 그것은 `context_audit` 의 `orphan_stem` + **아카이브 이동 옵션**이 다루는 자리이고, 이번에
> **일부러 이식하지 않은** 부분이다(트윈 문서 이동은 사람의 판단).
>
> [주의] **마일스톤 4 의 「측정 도구 자신이 조용히 틀린다」의 재발이고, 이번 도구는 내 LIKE
> 패턴이었다.** 이 세션에서 같은 부류가 두 번째다(§8 의 *"누가 막았는지를 봐야 한다"*).
> [중요] **교훈: 부분 일치로 「없어야 할 것」을 세면, 있어야 할 것이 그 패턴에 걸린다.**
> 경계(`\b`)와 **제외 조건**(`_Renewal` 접미)을 같이 적어야 한다.

[주의] **`_archive` 신호가 이 세션에서 세 번째다** — 검색 로그 1위(§12.3) · 도메인 빈도(§12.4) ·
그리고 orphan 101건. 세 신호가 같은 곳을 가리킨다. [중요] **그러나 옮기지 않았다** — 트윈 문서를
이동하는 것은 사람의 판단이고(`work/cleanup.py` 와 같은 결), `_archive` 를 어떻게 할지는 **열린
질문**이다. 복구 경로는 `dir_named`·`orphan_stem` 을 **후보에 아예 넣지 않는다**(테스트로 고정).

#### 회귀 하나를 냈고 그 테스트가 잡았다

`test_mcp_servers.py` 가 **도구 수를 못박아 둔 계약 테스트**라 26 → 28 로 깨졌다. 우연한 등록·
삭제를 잡는 자리이므로 숫자를 올리고 **새 도구 둘을 이름으로도 검사**하게 했다(그 파일이 이미
*"개수만 세면 다음에 또 깨진다"* 고 적어 둔 규약을 따랐다).

### §12.6 [중요] 우리에게는 원전에 없는 층이 있다 — 「한글 베이스」 (사용자 지적)

`#160` 2단계로 아카이브 이동을 이식하고 **112건 이동을 제안했다.** 사용자가 승인하지 않고
*"데이터를 실제로 봐. 기존에 단순 영어로만 태그와 RAG 구성된 걸 한글 베이스로 바꾸는 작업이
있었어"* 라고 했다. 전수로 재니 **내 진단이 틀렸다.**

    컨텍스트 MD 태그   936문서 / 5,446태그 → 한글 242개 / 69문서 (7.4%)
    온톨로지 aliases   240항목 → 10개, **전부 한글** (4.2%)
    시소러스 폴백      _thesaurus.tsv **1줄** (헤더: "온톨로지 yaml 이 없는 클래스용")
    BM25 KR 라우팅     bm25kr_* 테이블 존재 · [중요] AX_BM25_KR **꺼짐**

한글 별칭 10개는 **현행 클래스에 정확히** 붙어 있다(`UManager_Mission`=미션 매니저 ·
`UMissionTaskExecutor`=태스크 실행 …). [중요] **기제는 작동한다** — `ax-ontology` 스킬 실측:
*"MissionSystemTools 는 본문에 「태스크」가 0회인데 한글 태그 4개만으로 상위권"*.
커버리지가 4~7% 에서 멈춘 것이 상태다.

#### [중요] 내 진단이 두 번 틀렸다

    [실패] "아카이브가 검색을 오염시킨다"
       → 아카이브 문서 101건은 **한글 태그가 0개**다. 한글 질의로 뜰 수단이 없다
    [실패] 그래서 처방이 "문서 112건 이동" 이었다
       → 실제 원인은 **시소러스 폴백 한 줄**: 「미션시스템」 → UAlphaManager_Mission(레거시).
         온톨로지에는 「미션 매니저」 → UManager_Mission(현행)이 제대로 있다.
         [중요] 이동했다면 별칭은 그대로 남아 여전히 레거시로 확장되고, 대상 문서만 사라져
         **검색이 더 나빠졌을 것**이다

[중요] **그 TSV 한 줄을 「낡았다」고 판정하지 않았다** — `add_alias_tool` 은 **사람이 답한 것만**
저장하고, 포팅 원본을 가리키는 것이 의도였을 수 있다. 사용자 확인 사항으로 남겼다.

#### 순서 결정 (사용자 2026-08-15)

> *"지금은 순수하게 복각 먼저 하고 그 후 저거와 병합할래?"*

[중요] **순수 복각 먼저, 한글 베이스와의 정합은 그 뒤** → `#194`(중 3.7) 신설, 분모 77 → 78.
맞춰야 할 지점 넷을 그 이슈에 적었다(분석기 언어 갭 처방 · `has_ue_prefix`/`query_shape` 의
영문 전제 · 시소러스 두 경로의 우선순위 · `AX_BM25_KR` 판단).

[주의] **이 순서를 택하면 `#160` 2단계는 이미 그 형태로 끝나 있다** — 기제는 이식됐고(테스트 41/41)
**적용은 안 했다.** [중요] **이식(기제)과 적용(정책)을 가른 것이 이 경우에 값을 냈다**: 기제는
남았고 잘못된 적용은 일어나지 않았다.

### §12.7 σ.9.0 4분기 (`#160` 3단계) — [중요] 원전 신호원이 우리 환경에서 무의미했다

빈 문서를 소스 활동 기준으로 4분기한다. [중요] **「빈 문서 N건」을 「고쳐야 할 N건」으로 읽으면
틀린다** — 그것을 가려 주는 장치다.

    recent_but_empty  [중요] 소스가 최근 바뀌었는데 문서가 비었다 — 원전이 Level 5 라 부른 것
    old_inactive      소스가 오래 조용하다 — 트리거 정책의 **정상 산물**
    orphan_md         링크는 있는데 그 파일이 없다 (좀비)
    no_source_link    링크 자체가 없다

#### [중요] 첫 판은 39건 **전부** 결함이라고 했다 — 계측 오류였다

    원전 신호원   소스 파일의 **mtime** — 팀원 PC 의 **오래 살아 있는 작업 복사본** 전제
    우리          갓 클론한 미러 — 실측: 소스 400개 mtime 이 **전부 2026-08-08 한 날짜**
                  같은 파일의 git 마지막 변경: 2025-10-12 · 2026-05-16

→ 신호원만 **git 커밋 시각**으로 바꿨다(mtime 은 폴백으로 남긴다 — git 없는 트리도 있다).

    첫 판 (mtime)   recent_but_empty 39 · old_inactive  0
    정정 후 (git)   recent_but_empty 14 · old_inactive 25   ← **없는 결함 25건을 만들고 있었다**

[주의] 이것은 `#194`(한글 베이스 정합)가 아니라 **이식이 성립하기 위한 조건**이다 — 신호원이
틀리면 이식분이 **거짓말을 한다.** 마일스톤 4 함정 그대로: *"그 수정이 겨눈 조건이 우리에게
없으면 옮기면 카고 컬트이거나 퇴행"*.

[주의] **알고 남긴 것**: 판정 근거가 문서 자신의 소스가 아니라 `related_classes` 의 **남의 헤더**일
수 있다(원전 로직 = 존재하는 후보 중 **가장 최근**). 실측 — `task_service.md` 도 `Beacon.md` 도
`MissionPrefab.h` 로 판정됐다. 원전 의도가 *"관련 소스가 움직였으면 문서가 낡았다"* 이므로
그대로 뒀다.

#### [중요] 14건의 원인 — **LLM 한 번도 안 쓰고** 확정했다

세 갈래를 예상했다(ⓐ 우리 합성이 조용히 실패 · ⓑ 트리거 누락 · ⓒ 문서화할 내용 없음).
**전부 틀렸다.** 무료 신호 둘이 갈랐다:

    source_commit 스탬프   recent_but_empty 14건 → **0/14** · valid 표본 200건 → 45/200
    우리 합성의 저장 게이트  synth.py:296·306 — `verdict`/`report` 가 ok 아니면 **저장하지 않는다**

    ⓒ 배제   Beacon.h 3,271B + .cpp 6,155B 에 실제 클래스가 있다. frontmatter 는 정확하고
             (`ABeacon`·`AMissionPrefab`) **본문만 통째로 없다**
    ⓐ 배제   우리에겐 껍데기를 저장할 경로가 **없다**(게이트)
    ⓑ 배제   애초에 **우리가 만든 문서가 아니다**(스탬프 0/14)
    [중요] ⓓ    `infra_state_20260808.zip` 로 실려 온 **원전 환경의 σ.7 손상 유산**

→ **원전 문서의 처방이 그대로 맞다**: *"소스가 다시 커밋되면 자연 회복한다."* 그 파일이 다시
커밋되면 우리 색인기가 합성하고, **게이트를 통과한 것만** 저장되며 `source_commit` 이 찍힌다.
지금 14건을 강제 재생성하는 것은 그 회복을 앞당기는 것뿐이고 급한 이유가 없다(비용 LLM 14회).

[중요] **교훈 — 출처 스탬프가 LLM 1회보다 싸고 확실하다.** *"우리 결함인가, 물려받은 손상인가"*
는 실행해 보지 않고 **스탬프 유무**로 갈린다. 이 저장소는 `source_commit`·`[ax-index]` 트레일러·
`twin_commit` 을 이미 그 목적으로 갖고 있다 — **먼저 그것을 보라.**

### §12.8 소 3.4.3 (`#162`) — 클라이언트 폴백 캐시 · [중요] 내 저평가를 사용자가 정정했다

나는 이 칸을 *"원격 클라이언트 캐시라 팀 전용일 가능성"* 으로 낮게 봤다. **틀렸다.**

> *"클라이언트 캐시는 오히려 가져와야 해. 기존과 달리 모든 윈도우 PC 는 온톨로지 정보가 없이
> 리눅스 서버에 저장된 내용을 활용하게 바뀌었으니까."* (사용자 2026-08-15)

실측이 확인했다:

    .2 · .43 의 `.claude/vector_db`   [중요] **디렉토리 자체가 없다** (온톨로지·벡터 0건)
    작업장 MCP 등록                    `mcpServers: {}` — 마스터 8101/8102/8103 등록 0건
    현재 전달 경로                     마스터가 **매니페스트 파일**에 담아 scp

원전은 팀원 PC 가 로컬 데이터를 가질 수도 있는 3모드 구조였다. [중요] **우리는 온톨로지가 마스터에만
있다** — 소비자가 닿지 못하면 대안이 없다. 그래서 이 캐시는 **덜 필요한 게 아니라 더 필요하다.**

**소비자도 갈렸다**: 작업장은 `ax-infer` 가 *"큐 401 이 정상 — 우회로를 찾지 않는다"* 로 **의도적
차단**이고, 진짜 클라이언트 모드는 **요청자 `.33`**(스킬 `ax-ontology` 의 MCP 조회)이다. 마스터가
멈추면 **사람이 멈추는** 자리다.

#### [중요] 3분기 `outcome` 을 합치면 지워진 개념이 되살아난다

    ok           캐시 워밍 후 응답 그대로
    absent       [중요] 서버가 도달돼 「없다」고 답함 → **폴백·캐시 안 한다**
    unreachable  stale 폴백 (`_stale`·`_cached_at` 주입) · 없으면 miss

실 구동(실제 HTTP): 실 마스터 `/livez` → `server`(캐시 1건) · 실제 404 → `absent`(**캐시 안
늘어남**) · 닫힌 포트 → `stale` · 미스 → `miss`.

#### [중요] `mtime` 과 같은 부류가 또 있었다 — 임베더

원전 `client_index` 는 Chroma **기본** 임베더(`all-MiniLM-L6-v2`)를 쓴다. 우리
`context_search/embedding.py` 가 **이미 그 모델을 기각**해 뒀다: *"영어 전용 … 영어 전용
모델에서는 **한글 질의가 두 채널 모두 실패했다**"*. 그대로 옮기면 **폴백이 한글에 무력해진다** —
마스터가 멈춰 폴백으로 내려간 바로 그 순간에. 기제는 옮기고 **구성요소는 우리 것**을 썼다.

### §12.9 소 2.3.1 (`#142`) — 대조: [중요] 우리가 더 큰데 능력 5개가 미이식이었다

원전 `cluster_selftest.py` **416줄** vs 우리 `work/selftest.py` **595줄**. [중요] **줄 수가 완성도를
말하지 않는다** — 능력 단위로 맞추니:

    [완료] 이식됨      worktree 격리 · base1/base2 2단 커밋 · work 등록·종결
    [완료] 더 촘촘함    NONCE 왕복(우리 50 : 원전 33) · PSEUDO 제거 확인
    [중요] 미이식 5    폴링 중 워커 수(`last_seen`) · `force_backend` 태깅 ·
                   백엔드별 격리 검증 · 진짜 병렬 판정(구간 겹침) · C.7 리더 통합 머지

**미이식 5개의 내용**

① **폴링 워커 수 + 진짜 병렬 판정.** 원전 근거(사용자 2026-07-06): *"워커 1대여도 통과해야 한다.
병렬 단정은 폴링 ≥2 일 때만."* 등록 수가 아니라 **`last_seen` 이 있는 폴링 중 워커**로 판정하고,
병렬은 서로 다른 워커의 `[claimed_at, submitted_at]` **구간 겹침**으로 잰다 — 워커 분포만 보면
같은 워커의 순차 처리를 병렬로 **오판한다.** [중요] 우리 실측 *"워커 2대 = 12% 빠르고 90% 비쌈"* 을
이 장치가 **자동으로 재는** 것이다.

② **`force_backend` + 백엔드별 격리 검증.** 원전 근거(사용자 2026-07-05): 로컬 LLM / agy 가
**각각** 실제 파일 편집을 내는가. [중요] 실측 주석까지 달려 있다: *"`task_data` 안에 넣는다 —
top-level 은 모델이 drop 한다."* 우리 체인은 `agy`→`claude` 이고 노드는 35B/14b 인데 그 격리를
**아무도 자동으로 안 잰다.**

③ **C.7 리더 통합 머지.** 실제 finalize 헬퍼를 태워 feature+durable 머지를 검증한다. **push
없음**(실 main 무접촉)이고, probe 가 trivial python 이라 UE 빌드 대신 **머지 정합성만** 본다.
[중요] C.7 이 우리 「통합자」의 원본이다(마일스톤 4 「가장 큰 발견」).

#### [중요] 그런데 이 5개는 미이식이 아니라 **의도된 단계 분리**였다

우리 `selftest.py` 머리말이 스스로 적어 뒀다: *"1단계(여기) 격리 저장소 + `fake_workshop` →
NONCE 라운드트립 기제 증명 · 2단계(`#141`) 실 큐·실 작업장·실 백엔드 e2e."* [중요] **5개 전부
2단계 능력이다.** 이 대조가 그 경계를 정확히 확인했고, **근거 없이 「미이식」으로 세지 않았다.**

#### [중요] 그 단계 분리의 **근거가 오늘 만료됐다**

*"의도된 분리"* 는 맞지만 **원전의 설계가 아니다** — 원전은 `run_selftest` **하나**로 실 워커까지
간다. 나눈 것은 우리이고, 모듈에 근거가 적혀 있다:

> *"[중요] **1단계를 먼저 하는 것은 게으름이 아니라 강제다.** 마스터 정본은 push 가 봉인돼 있다
> (`DISABLED://…`). 원전은 인프라 PC 가 push 할 수 있어서 라이브 저장소에 worktree 격리로
> 셀프테스트 브랜치를 올렸지만, 우리는 그 자리에 **임시 저장소**를 쓴다."*

[중요] **그 봉인을 이 세션에 열었다**(`#123` — `gitea-write` + `pre-push` 가드). 즉 *"강제였다"* 는
더 이상 사실이 아니고, 2단계를 막던 것이 사라졌다.

[주의] **그런데 제약이 봉인에서 가드로 옮겨졌을 뿐이다.** 실측:

    refs/heads/selftest/<slug>        [중요] 가드 거부 — 허용은 attempt/* · task/* 뿐
    refs/heads/selftest_integ/<slug>  [중요] 가드 거부

→ 2단계는 **막힌 것이 아니라 결정이 필요한 것**이 됐다: ⓐ 가드에 `selftest/*` 를 더한다(가드
표면이 넓어진다) · ⓑ 셀프테스트를 `attempt/selftest-<slug>/…` 아래로 둔다(가드 무수정, 원전
이름과 달라진다) · ⓒ 계속 미룬다 — [중요] **다만 근거를 새로 써야 한다.** *"봉인 때문"* 은 이제
사유가 아니다. **만료된 근거를 그대로 두면 다음 세션이 그것을 사실로 읽는다.**

#### [주의] 대조 도구가 또 조용히 틀렸다 (오늘 세 번째)

첫 판은 능력 12개 중 8개를 「없음」으로 냈다 — **`NONCE` 조차 없다고 했다.** 원인은 내 grep:
`-E` 에 `\|` 를 써서 `NONCE|nonce` 를 **리터럴로** 찾았다. [중요] **ERE 의 대안은 `|` 다.**
경계·이스케이프를 틀리면 「없어야 할 것」과 「있어야 할 것」이 **함께** 사라진다 —
§12.5 의 `%EditorView_MissionTask%` 와 같은 계열이고, 이번엔 반대 방향으로 틀렸다.

### §12.10 소 3.1.8·3.1.5·3.1.7 — 버전 핸드셰이크 · 온톨로지 감사 · 스테이징 세션

세 칸을 이었다. [중요] **셋 다 「기제는 옮기고 신호원·구성요소는 우리 것」이 반복됐다.**

**`#191` 버전 핸드셰이크 (κ.9).** 원전이 이 도구를 만든 계기가 우리에게 그대로 있었다 —
*"그 전엔 어느 빌드인지 확인할 방법이 전혀 없었음"*. 실측: 버전 상수 0건 · 배달 config 에
`generated_by` 뿐. 신호원을 **git 커밋**으로 바꿨고, [중요] **트윈 커밋을 함께** 낸다(§8.4 세 값
동일 규약 — 코드만 말하면 *"같은 코드가 다른 트윈을 본다"* 를 못 본다. 실측: 코드 `fdbe4f3` /
트윈 `b1ba6af`). 판정은 **3분기**(`same`/`drifted`/**`unknown`**)이고 모르는 값에 `"unknown"`
문자열을 **넣지 않는다** — 넣으면 두 「모름」이 같다고 판정된다.

**`#188` 온톨로지 일관성 감사.** read-only 감사 둘. 실 구동: 도메인 MD 직속 7·archive 7 →
갭 1건 · 패키지 7도메인 obj112 act72 inv56. [중요] 그 갭이 **이전 마일스톤의 한글 지원과 만나는
자리**였고, 나는 그것을 두 번 잘못 틀 잡았다(*"규약 위반"* → *"어느 쪽이 맞는지 정할 충돌"*).
사용자 정정으로 **보관·메모 후 머지**로 바로잡았다(§12.11).

**`#190` 스테이징 세션 (η.9).** 원전 표현 그대로 **`DoubleBufferedIndex` 패턴의 파일시스템
버전**. 원전 판단 넷이 전부 「과설계 방지」다 — 세션 강제 안 함 · [중요] 메타는 staging **밖**
(안이면 rename 때 pkg 오염) · 라이브 변경을 **막지 않고 알림** · 백업 1세대.
[주의] **여기서 `mtime` 을 쓰는 것은 σ.9.0 과 다르다** — 비교 대상이 `domain.yaml`, **우리 도구가
직접 쓰는 파일**이고 git checkout 이 아니다. [중요] **같은 도구라도 어디에 쓰는지가 판정을 바꾼다.**

### §12.11 [중요] 한글 지원은 「충돌」이 아니다 — 보관하고 복각 후 머지 (사용자 확정)

`#188` 감사가 `_archive` 안 `status: active` 한글 도메인 1건을 잡았다. 나는 그것을
*"규약 위반"* 으로 읽었고, 사용자 정정 후에는 *"어느 쪽이 맞는지 정해야 하는 충돌"* 로 적었다.
[중요] **둘 다 틀렸다.**

> *"지금은 복각을 우선하고 있고, **한글 지원은 이전 마일스톤에서 만든 거야.** 일단 **보관하고
> 메모를 남겨뒀다가, 복각이 끝난 뒤 머지**하자."* (사용자 2026-08-15)

    ontology/create.py   [중요] 한글 도메인명 **거부** — "경로·URL·검색 식별자에서 깨진다"
    webui/board.py       [완료] 한글 도메인명 **허용**(의도). 실측 기록: "[A-Za-z0-9_-] 로 좁혔더니
                         한글 도메인명이 통째로 날아가 파일명이 `domain` 이 됐다(2026-08-13)"
                         + LLM 프롬프트가 "도메인명(한글)을 제안하고" 라고 시킨다

**복각 중 원칙 셋** (모듈·`#194`·마일스톤 함정 세 곳에 못박았다):

    ① 한글 지원 쪽을 **결함으로 고치지 않는다** — 이전 마일스톤이 값을 치른 것이다
    ② 원전 기준 감사가 그것을 「갭」으로 표시하는 것도 **그대로 둔다**
       [중요] 그 표시가 곧 **머지 지점의 목록**이 된다
    ③ 규칙을 한쪽으로 **통일하지 않는다** — 통일하면 머지할 때 무엇이 달랐는지 알 수 없다

### §12.12 소 3.1.6 (`#189`) — 대조만 하고 이식하지 않았다

능력 단위로 맞추니 **이식할 것이 하나로 줄었고, 그것도 머지 후 결정**이 됐다(사용자 지시).

    edit_ontology_item / remove_ontology_item   [완료] **이미 있다** (잠금 보호까지)
    remove_domain_members 의 **DB 절반**          [중요] **대상이 없다** (아래)
    edit_domain_field                            [중요] 없다 — 유일한 후보, **머지 후 결정**

[중요] **DB 절반을 옮기면 우리가 없앤 드리프트를 되살린다.** 원전은 `class_ontology` 표에
`DELETE` 를 치는데 실측하니 그 표는 **행 0 · 쓰는 코드 0건**이다. 우리 `graph/db.py` 가 의도를
적어 뒀다: *"스키마를 처음부터 완성형으로 둔다 … 빈 `class_ontology` 는 원본의 구 DB 호환
경로와 같은 결과(빈 추론)를 낸다."*

[주의] **그리고 마일스톤 문구를 정정했다** — *"DB 온톨로지 표는 **안 옮겼다**"* 는 부정확하다.
정확히는 **스키마는 옮겼고 채우지 않았다.** [중요] **「안 옮겼다」와 「옮겼지만 안 쓴다」는 다르다** —
전자로 적어 두면 다음 세션이 스키마가 없다고 믿고 다시 만들거나, 있는 것을 보고 *"기록이
틀렸다"* 로 결론한다.

`edit_domain_field` 는 도메인 MD 의 `tags`(= 한글 태그 채널, 가중치 3.0)를 건드리므로
**머지 지점 7번**으로 올렸다(`#194`).

### §12.13 남은 것 — 복각 4번 이후

    #192  유휴 배치 구동 주체 (ax-batch.timer + 시간 게이트 이식)  ← 자리
    #161  search_log_analyzer 696 + rag_report 56                 ← 그 자리에서 돌 첫 배치
    [중요] 둘을 **같은 커밋에** 넣는다 — 자리만 만들면 「깨울 주체 없는 가드」의 역방향이 된다

## §13 테스트

    신규   master/test_attempt.py                 40/40   (실제 git · 실제 worktree)
    전체   2379/2379 통과 · 실패 파일 0개

## 남은 것

- **`#128` 빌드 게이트를 큐 잡으로** — 마스터가 attempt 를 올리면 작업장이 그것을 **claim →
  pull → UE 빌드/RunTests → 보고** 하고, 마스터가 `merge_verified` 를 부른다. [중요] 지금은
  `merge_verified` 를 부르는 **파이프라인 호출자가 없다** — 라이브러리와 실증만 있다
- [주의] **`integrate.py` 의 문구 대조**(`"nothing to commit"`) — 지금 자리에서는 맞지만 마스터로
  옮기면 깨진다. `#129`(경로 정리)에서 같이 볼 것
- [주의] **기계별 Gitea 신원 분리** — 네 대가 계정 `Sim` 하나라 서버측 브랜치 보호로 마스터를
  제한할 수 없다. 그것이 되면 가드를 클라이언트에서 서버로 옮길 수 있다. **사용자 결정 사항**
- [중요] **`main` 에 서버측 보호가 없다**(`branch_protections` = `[]`). 위 항목과 묶여 있다
- 정리 대상으로 남긴 것: `ax-wt-probe-flowy` 워크트리 · 로컬/원격 probe 브랜치
  ([중요] **대화형 세션이 지우지 않는다** — 명령만 남긴다)

      git -C ~/ax-cluster/ModularStage/repo worktree remove ax-wt-probe-flowy   # 사람이 판단
      # 원격 probe 브랜치는 병합 안 됐으므로 훅이 거부한다 — 지우려면 사람이 Gitea UI 에서

---

## §13 세션 마감 (2026-08-15)

    M4          22/75 → **36/78** (분모가 셋 늘었다 — 전부 읽어서 나왔다: #192·#193·#194)
    닫힌 중분류   7개 — 중 1.1 · 1.2 · 1.4 · 2.3 · 3.4 · 4.1 · 4.2
    커밋         16개 (87f9717 … 이 커밋) · 세 클론 동기 유지
    테스트       2268 → **2626/2626 · 실패 0**
    신규 유닛     ax-batch.timer (설치·가동) — 유닛 7종 전부 active

### [중요] 이 세션의 성격 — 측정이 계획을 계속 바꿨다

| 바뀐 것 | 무엇이 바꿨나 |
|---|---|
| 분석기 → **기록기 먼저** | 검색 로그 입력이 0건이었다 |
| 배치 이식 → **자리 먼저** | 구동 주체가 없었다(폴링 루프를 버릴 때 스케줄링 권한도 버렸다) |
| `mtime` → **git 커밋 시각** | 없는 결함 25건을 만들고 있었다 |
| 강제 재생성 → **자연 회복** | 출처 스탬프 0/14 = 물려받은 손상 |
| 아카이브 이동 → **하지 않음** | 원인이 시소러스 한 줄이었다 |
| 캐시 「팀 전용」 → **더 필요** | 작업장에 온톨로지가 0건이었다 |
| Chroma 기본 임베더 → **우리 다국어** | 우리 코드가 이미 기각해 뒀다 |
| `#189` 이식 → **기록만** | 이식할 것이 하나로 줄고 그것도 머지 대상이었다 |

[중요] **반복된 형태 하나: 「기제는 옮기고 신호원·구성요소는 우리 것」.** σ.9.0 의 `mtime`→git ·
폴백 임베더 · 버전 핸드셰이크의 exe 빌드번호→git 커밋 — 셋 다 같은 판단이다. [주의] 그리고
스테이징 세션에서 **그 반대**가 나왔다(`mtime` 을 그대로 써도 되는 자리) — **같은 도구라도
어디에 쓰는지가 판정을 바꾼다.**

### [중요] 내가 틀렸다가 잡힌 것 여섯 (전부 함정 목록에 등재)

    ① 가드 동작을 `rc` 로 오판        — 두 번 다 훅이 아니라 git 이 막았다
    ② `#167` 「실증」 노트            — `%...%` 부분 일치가 현존 클래스를 옛이름으로 셌다
    ③ 아카이브 오염 진단             — 한글 베이스 층을 몰랐다 (사용자 지적)
    ④ 클라이언트 캐시 저평가          — 작업장에 온톨로지가 0건인 것을 안 봤다 (사용자 지적)
    ⑤ selftest 대조 grep             — `-E` 에 `\|` 를 써서 우리 핵심 NONCE 를 「없음」으로 냈다
    ⑥ 한글 도메인명 「규약 위반」      — 이전 마일스톤 산물이었다 (사용자 지적 ×2)

[중요] **셋은 사용자 지적이었다.** 그리고 ②③⑥ 은 전부 *"우리 것을 모르고 원전 기준으로만 봤다"* 는
같은 뿌리다 — 그래서 `#194`(머지 지점 7개)가 생겼다.

### 다음 세션의 결정 대기 (작업이 아니라 판단)

    #141 2단계   가드에 `selftest/*` 추가 · `attempt/` 아래로 · 계속 미루기(근거 새로 쓰기)
    중 3.3       생산자(코드 리뷰 생성·신호·history) 복각 여부 — 그룹째 막혀 있다
    #194         한글 베이스 정합 — [중요] **복각이 끝난 뒤** 머지 (지점 7개 보관)
    #152         `@ms-contract` 태그가 소스에 0건 — 재범위 후보(닫지 않음)
    열린 질문     `_archive` 처리 · 시소러스 「미션시스템」이 레거시를 가리키는 것이 의도인지 ·
                 코드 배달 경로(번들은 텍스트만 나른다)

**바로 할 수 있는 작업**: 중 3.1 마지막 `#187`(`ontology_descriptions` 270줄) — [주의] LLM 비용을
먼저 재야 한다. 그 외 중 2.1·2.2·2.4 · 중 3.2·3.3·3.6 은 전부 미착수다.
