# 11 — 온톨로지 LLM 재합성(소 1.3.3) · 🔴 워커 배선 구멍 발견

> 2026-08-09 · 커밋 `49093f3`…`3ca4ad8` · 이전 맥락: 리포트 10
>
> 모든 수치는 이 리포트 작성 중 실측한 값이다. 세션 기억이 아니다.

---

## 1. 세션 시작 — 클러스터 상태

BC-250 이 방금 부팅돼 있었다(uptime 3분). 확인한 것:

| | |
|---|---|
| BC-250 (.43) | SSH ✅ · ollama active · 35B 상주 12.28GiB · GPU 350MHz/52°C · TTM `pages_limit` 유지(5회 연속) |
| `.2` (RTX 3060) | 14b 상주 9,470MB · 여유 2,569 MiB |
| 마스터 서비스 4종 | `ax-task-queue` · `ax-broker` · `ax-projects` · `ax-indexer.path` 전부 active |

🔴 **부팅 직후 두 노드 모두 상주 모델의 `context_length` 가 4096 이었다.** 브로커는 미지정
요청에 `num_ctx=8192` 를 주입하므로, 첫 요청이 **재적재 60.5초**를 문다(리포트 07 §5.1-(2)의
61.6초 재현). 사용자 지시로 미리 태워 뒀다 — 2차 요청은 1.8초.

## 2. 🔴 리포트가 저장소 안으로 옮겨진 것을 늦게 알았다

`docs/3-open-items.md` 가 **리포트 10 §8** 을 인용하는데 `~/claude-workspace/reports/` 에는
09 까지밖에 없었다. 찾아보니 리포트가 **`~/ax-cluster/reports/` 로 이관**돼 있었고(2026-08-08),
10번은 그쪽에만 있었다. 세션 시작 때 못 읽은 것이다.

⚠️ **규약이 두 곳에서 충돌한다** — `~/CLAUDE.md` 는 *"세션 시작 시 리포트 전체 정독"* 인데
`reports/README.md` 는 *"전부 정독하지 말 것, 먼저 `docs/` 를 보라"* 다. 후자가 나중이고
근거(리포트 08 이 82KB, 정독이 지켜진 적 없음)도 있다. **머신 가이드를 저장소 쪽 규약에
맞추는 것이 맞다** — 이번 세션에서는 고치지 않았다(사용자 결정 사항).

## 3. 🔴 워커 배선 — "미구현" 이 아니라 "잘못 배선됨" (사용자 지시로 필수 명기)

사용자 질문에서 시작했다: *"각 워커들이 동일하게 동작하도록 제어하려면 워커에도 … MCP나
스킬 등으로 통제해야 하는거 아니야?"* 실측했더니 상태가 예상보다 나빴다.

### 3.1 실측 (`janus@192.168.0.2`, `E:\trunk\ModularStage`)

| 확인 | 결과 |
|---|---|
| `.claude/skills/` | **전임(AgentTest) 스킬 12종이 그대로 살아 있다** — `distribute`·`review-work`·`cluster-selftest`·`refresh-ontology`·`manage-domain`·`audit-context` … |
| `.claude/agents/` | 전임 에이전트 12종 |
| `.mcp.json` 의 `C:\Users\USER\Documents\ModularStage` | 🔴 **MISSING** — 계정이 `USER` 다. 이 머신은 `janus` |
| `.claude/mcp/*.exe` | 🔴 **하나도 없다** (context-search · log-analyzer · crash-analyzer) |
| `~/.claude.json` 의 `mcpServers` | 🔴 **비어 있다** — 마스터 8101·8102·8103 이 등록조차 안 돼 있다 |

→ 그 폴더에서 Claude Code 세션을 열면 **전임 프로젝트의 스킬 12종을 로드하고, 죽은 MCP
6종을 물고, 우리 마스터는 모르는 채로** 시작한다. 🔴 **에러 없이 조용히 틀리는** 유형이다.

### 3.2 소스 최신화도 같은 항목이다 (사용자 지적)

*"워커들도 프로젝트 소스코드는 업데이트 받기만 하고 최신화만 해두고 있어야 … 온톨로지
데이터를 기반으로 코드 분석하거나 수정할 부분을 찾을 수 있을 것 같은데"* — 정확하다.
트윈은 미러의 **특정 커밋** 기준으로 지어진다(워터마크가 커밋 해시).

| | 커밋 |
|---|---|
| Gitea bare · 마스터 미러 · `last_indexed_commit` · `.2` 체크아웃 | 넷 다 `bc4b38f` — **지금은 우연히 맞아 있다** |
| `master/work/manifest.py` 의 커밋 필드 | 🔴 **0건** — 어긋나도 감지할 수단이 없다 |

고칠 방향: 매니페스트에 **`twin_commit`** 을 싣고, 워커는 시작 전 `fetch` + **ff-only** 로만
따라잡는다(🔴 `reset --hard`·`checkout`·`git clean` 금지, 추적 파일이 더러우면 차단,
못 따라잡으면 **fail-closed**). 🔴 **`.33` 은 예외 — 사람의 메인 작업 PC라 드리프트 보고만.**

### 3.3 처리

`docs/3-open-items.md` 에 🔴🔴 **필수** 항목으로 전문 기록 + 마일스톤 「🕓 예정」 절에
*"「예정」이 아니라 「필수」"* 명기. 커밋 `49093f3`.
**착수는 중 1.3 이후** — 사용자 지시(*"지금은 마스터 서버의 작업을 하니까 1.3을 진행하자"*).

## 4. 자동승급 폐기 확정 (사용자 결정)

*"저 정규 섹션 왜 안만든건데?"* 에서 시작해 원본을 뒤졌다. **도메인 MD 생산자가 둘**이다:

| 생산자 | 쓰는 섹션 | frontmatter | 우리 7개 중 |
|---|---|---|---|
| 자동 승급 `watcher/domain.py:577` | `## 시스템 개요` **하나뿐** | `status: draft`, `created_by` 없음 | **6** |
| `ontology-register` 스킬 `domain_seed.py:299` | 개요 + 핵심 책임 · 도메인 경계 · 핵심 invariants · 협력 도메인 · 의도 메모 | `status: active`, `created_by: ontology-register` | 1 (`MissionEditor`) |

사용자: *"자동승급 오동작이 심했어! 불필요한거였으면 이참에 양쪽 다 제거하면 되겠다."*

**실측 결과 지울 게 없었다** — 우리 저장소에 자동승급 코드가 **0줄**이다(애초에 이식 안 됨).
AgentTest 는 참조 전용이라 손댈 수 없다. 그래서 사용자 선택대로 **`docs/settled-decisions.md`
에 폐기 확정만 명기**했다 — 다음 세션이 "빠진 기능" 으로 오해해 되살리지 않도록.

**그 결과 6섹션은 곧 소 1.3.1/1.3.2 의 산출물이다.** 저 결손은 버그가 아니라 **아직 안 한
대화형 작업의 자리**다.

## 5. 소 1.3.3 — 온톨로지 LLM 재합성 (중 1.3 의 마지막 LLM 조각)

`master/ontology/` 에 6모듈 신설. 결정적 부분(쓰기·검증·레이어·stale·수집)은 이미 있었고
**LLM 한 겹만** 비어 있었다.

    domain_md   도메인 MD 파싱        contexts  응집 조각화 + .h/.cpp 발췌
    prompt      actions/invariants    parse     응답 파서 + 결정적 필터
    synth       오케스트레이션         __main__  CLI (status·plan·verify·dry·refresh)

### 5.1 🔴 원본 파서를 그대로 옮기면 6/7 도메인이 조용히 빈다

원본 `_SECTION_PATTERNS` 는 6섹션 스키마만 안다. 실물은 **7개 중 1개만** 그 스키마다.
나머지 6개는 `시스템 개요` + `핵심 구현 패턴` + `확장 포인트` 라는 **제3의 사실상 표준**
(원본 `agent_defs.py:132` 가 에이전트에게 그 셋을 읽으라고 지시한다).

그대로 옮겼으면 6개 도메인이 `responsibilities=[]`·`boundary=[]`·`invariants=[]` 로 나오고
**파싱은 "성공" 으로 보고된다.** 실측 대조:

    GlobalEventSystem 서술량 — 원본 파서 322자  →  우리 1,225자 (3.8배)

**섹션을 전부 읽고 아는 것만 골라 쓰되 모르는 섹션은 남긴다.** 그리고 🔴 **없는 정규 섹션을
결과에 적는다**(`missing`) — 결손을 숨기면 다음 세션이 "원래 없나 보다" 로 읽는다.

### 5.2 🔴 프롬프트 예산을 실측으로 정했다

원본 상수(클래스 10 × `.h`3,000 + `.cpp`2,500 = **55KB**)는 **상용 API 전제**다. 우리 노드로
그대로 보내면 리포트 10 §8 의 사고를 반복한다. 그래서 `plan` 명령을 **LLM 0** 으로 먼저 만들고
`/api/generate` 의 `prompt_eval_count` 로 환산비를 쟀다:

| 프롬프트 17,315자 | 토큰 | 환산 | `num_ctx` 8192 여유 |
|---|---|---|---|
| 35B-A3B | 6,070 | 2.85자/tok | 2,122 tok |
| qwen2.5-coder:14b | 6,200 | 2.79자/tok | 1,992 tok |

**출력 여유가 2,000 토큰뿐이라 잘릴 자리였다.** JSON 5~15건에 2,500tok 을 확보하기로 하고
발췌 예산을 **11,000 → 7,000자**로 낮췄다. 전 조각이 **출력여유 3,459tok 이상**으로 들어간다.

### 5.3 🔴 페이로드를 조각내 두 노드에 나눠 던진다 (사용자 지시)

> *"컨텍스트 용량을 줄이진 않지만 페이로드를 분리해서 보내고 그걸 재합성할 수 있는 물리
> 성능이 있잖아."*

정확한 지적이었고 내 설계의 실제 결함을 짚었다. 단일 요청이면
`ProjectAlpha_UiViewManagement` 는 **67개 중 8개만 보이고 59개가 영영 안 보인다.**
**창을 못 늘리는 것과 데이터를 못 보는 것은 다른 문제**이고, 후자는 요청을 나누면 풀린다.

- **응집 순서로 자른다** — `#include` 그래프로 BFS. 이름순으로 자르면 한 액션의 참여자가
  서로 다른 조각으로 흩어져 **어느 조각에서도 안 보인다.** 실제로 `*_Renewal` 위젯 패밀리와
  `UWNDUIPage_*` 계열이 같은 조각에 모였다
- **`allowed` 는 도메인 전체 멤버**로 둔다 — 조각 단위로 좁히면 조각을 가로지르는 액션이
  통째로 버려진다. cross-domain 차단이라는 원래 목적은 그대로 지켜진다
- **레인당 in-flight 1** — 모델 이름이 곧 노드 선택이라, 모델당 스레드 하나면 노드당 1건이다

🔴 **다른 모델을 섞으면 핀이 깨진다.** 두 노드 다 `MAX_LOADED_MODELS=1` 이라 `.2` 에
`gemma3:12b` 를 요청하면 상주 14b 를 쫓아내고 브로커가 다시 올린다(~60초) — **왕복 스래싱**.
그래서 기본값은 **두 노드의 상주 모델**이다.

### 5.4 실측 — 로컬 2노드 vs 클로드 (같은 프롬프트·같은 결정적 게이트)

사용자 지시: *"노드 둘 다 클로드도 로그인 되어있으니 클로드를 통한 오케스트레이션도
시도해보고"*. 세 대 모두 설치돼 있다(마스터 2.1.226 · `.2` 2.1.225 · BC-250 2.1.222).
층2 가 이미 같은 방식으로 CLI 를 부르므로(`layer2_verify.call_claude`) 그 호출부를 재사용해
**모델 이름이 `claude`/`agy` 면 상용 레인**이 되게 했다.

| 경로 | actions | invariants | 파싱 거부 | **사실 게이트** | 소요 |
|---|---|---|---|---|---|
| 로컬 2노드 (35B + 14b) | 7 | 9 | 2 | **0** | **68s** |
| `claude` 2.1.226 | **10** | **19** | **0** | **0** | 182s |

🔴 **둘 다 사실 게이트를 100% 통과했다** — 로컬 모델도 **없는 호출을 지어내지는 않았다.**
차이는 밀도와 구체성이다:

    로컬  : "FGlobalEventSystem 인스턴스는 반드시 PrivateHandler 멤버를 통해 유효한 싱글톤으로 존재해야 한다"
    클로드: "모든 등록·해제 경로는 정적 TSharedPtr인 PrivateHandler를 통해서만 수행되며 …"
            "리스너를 담는 키는 항상 인터페이스 UClass의 FName이다. AddEventListenerAll은 InterfaceClass->GetFName()을 …"

로컬의 파싱 거부 2건은 `source_signal` 이 허용 6종 밖으로 나온 것이다(프롬프트 손질거리).

### 5.5 결정 3가지

- **objects 는 LLM 이 만들지 않는다** — 멤버·파일·레이어는 전부 결정적으로 나온다.
  물으면 있는 답을 지어내게 하는 것이고 틀릴 여지만 는다
- **actions 와 invariants 를 합쳐 부르지 않는다** — 프리필이 절반이 되지만 금지 규칙이
  서로 달라 지시 준수가 나빠진다. 우리의 실측된 실패 모드가 문장력이 아니라 형식 준수다
- **병합은 이름 기준이고, 그게 의미 종합이 아님을 명시한다** — 두 조각이 같은 흐름의
  앞뒤를 각각 보고 다른 이름을 붙이면 **둘 다 남는다.** 그 한계를 아는 채로 두는 것이
  조용히 하나를 버리는 것보다 낫다. 🔴 **대화형 에이전트가 나은 자리가 정확히 여기다**

### 5.6 검증

테스트 **42건 신규**(`test_ontology_synth.py`, **LLM 0**) — 두 스키마 파싱 · 결손 보고 ·
모르는 섹션 보존 · 조각 고지 · 도메인 밖 차단(구현/actor/objects_affected/related) ·
펜스 관용 · 응답 형식 실패 4종 · 결정적 해시명 · 병합 규칙. 기존 127건 회귀 없음.

## 6. 사용자가 제시한 아키텍처 — 워커가 자기 자원을 오케스트레이션

> *"각 워커들이 클로드가 오케스트레이션해서 설치된 로컬 LLM 과 동작하도록 MCP나 스킬이 있고,
> 이걸 사용해 워커들이 각자 자신의 컴퓨터에 있는 클로드, 안티그래비티, 로컬 LLM을 써서
> 답변을 만들도록 해야겠지?"*

리포트 08 §6.4 가 이미 도달한 결론과 같다 — *"에이전틱 실행은 그쪽 Claude 에 맡기고
마스터는 컨텍스트·큐·라우팅만 댄다."* **오늘 만든 것이 그대로 그 MCP 표면이 된다:**

| 오늘 만든 것 | 워커 구조에서 |
|---|---|
| `chunk_domain` | `get_ontology_chunk(domain, part)` — 워커가 받아갈 페이로드 |
| `prompt.*` | 스킬 본문 (워커의 Claude 가 자기 백엔드에 맞춰 씀) |
| `parse` + `verify_facts` | `submit_ontology_items(...)` — 🔴 **워커를 믿지 않는다** |
| `package.write` | 마스터가 쓴다 (검수 잠금 보존) |

**갈리는 것은 「누가 추론하나」 한 줄뿐이다.** 그리고 이 구조도 §3 의 워커 배선을 선결로
공유한다 — 이 세션에서 **세 번** 같은 선결조건에 도달했다.

용량 관점에서도 이쪽이 넓다: `.2` 는 로컬 모델 7종 + Claude + Gemini CLI 를 갖고 있다.
⚠️ 단 `.33` 은 여유 VRAM 7,651 MiB 라 로컬 LLM 상주가 금지된 머신이다 — 거긴 Claude·agy 가 자산.

## 7. 🔴 분모가 틀려 있었다

마일스톤이 중 1.3 을 `7/10` 이라 적었는데 **완료 7 + 남은 목록 6 = 13** 이었다. 목록이
사실이므로 분모를 목록에 맞췄다(총계 `14/33` → **`15/36`**). **작업이 는 게 아니라 세는
법이 틀렸던 것이다.** 마일스톤은 완료된 소분류를 지워 180줄 상한을 지켰다(197 → 180).

`CLAUDE.md` 의 *"합성기는 만들었지만 배선 안 됨"* 서술도 정정했다 — 배선은 어제 됐고,
지금의 진짜 공백은 **온톨로지 247 yaml 이 0건 색인**(소 2.1.1)이라는 것이다.

## 8. 남은 것

**바로 집을 수 있는 것 (순서는 사용자가 정한다):**

- [ ] 🔴🔴 **워커 통제 배선** — 전임 `.claude/` 아카이브 → 마스터 MCP 등록 → `client/` 번들
      배달 + 드리프트 게이트 → 매니페스트 `twin_commit` + ff-only 따라잡기. 전문 = `docs/3-open-items.md`
- [ ] **소 2.1.1 도메인 색인** — 합성기가 생겼는데 **247 yaml 이 0건 색인**이라 검색·매니페스트가
      여전히 못 본다. 종점(2.3.1)까지의 사슬에서 지금 가장 상류
- [ ] **소 1.3.1/1.3.2 대화형 등록·편집** — 6개 도메인에 `도메인 경계` 가 없다. 지금은
      결정적 게이트가 막고 있지만, 경계가 있으면 합성 품질이 오른다
- [ ] `refresh` 실전 1회 — 지금까지 **dry-run 만** 했다. 받아온 스냅샷을 덮는 첫 실행이라
      도메인 하나로 시작할 것
- [ ] 로컬 프롬프트 손질 — `source_signal` 이 6종 밖으로 나오는 건 2/11

**규약 정리:**

- [ ] `~/CLAUDE.md` 의 *"리포트 전체 정독"* 을 `reports/README.md` 규약에 맞출 것 (§2)
✅ **레드마인에 이슈로 올렸다** (사용자 지시 *"레드마인도 있으니까 필요하면 할당하던지"*).
      키는 디스크에 없었지만 **레드마인이 이 마스터의 컨테이너**라(사용자 지적) DB 의
      `tokens` 테이블에서 admin API 토큰을 꺼내 썼다. `rest_api_enabled=1` 확인.

      | | 이슈 | 우선순위 |
      |---|---|---|
      | #21 | 🔴 워커 통제 배선 — 워커가 전임 시스템에 잘못 배선돼 있다 | 높음 |
      | #22 | 소 2.1.1 온톨로지 도메인 색인 — 247 yaml 이 0건 색인 | 높음 |
      | #23 | 소 1.3.1/1.3.2 대화형 도메인 등록·편집 — 6개 도메인에 경계 없음 | 보통 |
      | #24 | 온톨로지 refresh 실전 1회 + 로컬 프롬프트 손질 | 보통 |

      ⚠️ **트래커가 `코드리뷰` 1종뿐이고 프로젝트도 `ModularStage` 하나다.** AX 클러스터
      인프라 작업을 게임 프로젝트의 코드리뷰 트래커에 넣은 셈이라 `[AX]` 접두어로 구분만
      해 뒀다. **별도 프로젝트/트래커 분리는 구조 변경이라 사용자 결정 사항으로 남긴다.**

**이월 (변동 없음):** BC-250 장시간 안정성 · 35B 컨텍스트 상한 · 콜드 빌드 실측 ·
`.33` `PasswordAuthentication no` · `gjc` 설치 · `sim` 재로그인(gitea 그룹).

---

*작성: Claude (마스터 세션) · 커밋 `49093f3`·`02d11b8`·`3ca4ad8` · 이전 맥락: 리포트 10*
