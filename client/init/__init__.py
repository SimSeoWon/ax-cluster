"""클라이언트 초기화 — 체크아웃 부트스트랩 등 (#254).

[중요] 이 패키지는 **마스터에서 돈다**(SSH 를 쏘는 쪽). `client/` 에 두는 기준은
§12.5-e 의 「어디서 도는가」가 아니라 **무엇을 설치하는가**다 — 클라이언트 설치물의
선언·절차가 여기 모인다. `master/client/bundle.py` 는 그것을 실어 보내는 배달기다.
"""
