# LLM 적합도 — **실측 기록** (작업별 유리함 · 환각 유무)

> 모델 적합도는 **머신의 속성이 아니라 모델의 속성**이라 여기 한 곳에 모은다. 각 머신에
> **무엇이 설치돼 있는지**는 그 머신 문서에 있다 — [`win-worker-2.md`](win-worker-2.md) ·
> [`win-main-33.md`](win-main-33.md) · [`bc250-1.md`](bc250-1.md).
>
> [중요] **여기 없는 모델은 "좋다/나쁘다" 를 모른다는 뜻이다** — 안 재봤다는 것이지 나쁘다는
> 뜻이 아니다. 추측으로 칸을 채우지 말 것.

## 왜 이 문서가 있나

같은 측정을 세션마다 다시 하지 않기 위해서다. 그리고 **환각은 모델을 바꿀 때만 재는 게
아니라 기록해 둬야 한다** — 우리가 겪은 환각은 전부 *"에러 없이 그럴듯한 문서"* 형태라,
숫자로 남기지 않으면 다음 세션이 같은 걸 다시 발견한다.

**판정 기준은 결정적 게이트다** — `master/context_synth/verify.py` 가 소스의 주석을 벗겨
**주석 안에만 있는 식별자**를 특정하고, 문서가 그것을 사실로 주장하면 거부한다. LLM 에게
자기 검증을 맡기지 않는다(설계 규칙: 결정적 게이트 > LLM 판단).

---

## 로컬 (Ollama)

| 모델 | 어디 | 유리한 작업 | [중요] 환각·실패 (실측) |
|---|---|---|---|
| **`gemma4:e4b`** | bc250 · `.2` · `.33` | [완료] **컨텍스트 MD 합성 — 채택** (`SYNTH_MODEL`) | 사실 게이트 **3/3**. 12~32초/그룹. **4B급인데 35B 와 동률** — 지금 실패 유형이 문장력이 아니라 **지시 준수**라는 뜻 |
| **`gemma3:12b`** | `.2` | [완료] 합성 **대안** (`FALLBACK_MODEL`) | 사실 게이트 **3/3**. 16~46초/그룹. 전용 VRAM 이라 메모리 문제 없음 |
| **`qwen2.5-coder:14b`** | bc250 · **`.2` 핀** · `.33` | [완료] **UE5 C++ 코드 생성** (§4.4 확정, `CODER`) | [중요] **문서 작업엔 부적합** — frontmatter 를 닫는 두 번째 `---` 를 빼먹는다(실측). 형식 게이트가 전부 거부했다. `md.repair_frontmatter` 로 구제하지만 다른 모델엔 필요 없는 보정이다 |
| **Qwen3.5-35B-A3B `IQ2_M`** | **bc250 핀** |  합성 가능하지만 **권하지 않는다** (`HEAVY_MODEL`) | [중요] **주석 처리된 코드를 살아 있는 기능으로 서술** — 실측 2건: ⑴ `FPlayerMontageLoadInfo`·`FMonsterMontageLoadInfo`·`RequestLoad_MontageList` ⑵ `UMissionTask_Spawn`. 특이한 것은 **같은 응답의 「개선 필요 사항」 절은 정확히 짚는다** — 사실을 모르는 게 아니라 절마다 다르게 쓴다.<br>[중요] **요청당 노드 메모리를 먹고 안 돌려준다** → BC-250 정지 **두 번**(리포트 10 §8 · **17 §16**). 재실측 2026-08-15: 작은 요청 **~60MB**씩(옛 ~170MB 는 큰 프롬프트 기준) · 8192 상주 시 여유 **138~300MB** 뿐. [주의] `num_ctx` 는 한 요청을 묶을 뿐 **누적은 못 막고**, 그러라고 있는 `UNLOAD_EVERY` 는 [중요] **한 번도 안 돈다**(레인 호출마다 카운터 리셋 — 실측 요청 14회·회수 0회) |
| **Qwen3.6-27B-A3B-Coder `Q2_K`** | (지웠다 — `.2` 에서 트라이얼만) | [실패] **코드 레인 교체 근거 없음** (트라이얼 2026-08-21, 사용자 요청) | **속도는 이겼고 판정은 못 이겼다.** 지연 **0.73s**(14b 3.4s) · **64~72 tok/s**(14b 30.6) · 상주 **+0.4MB/사이클**(30/30, 누수 없음) · 10GB **100% GPU**. 그런데 층2 에서 **놓침 2 = 14b 와 동률**이고 **하필 같은 표본**(`P3`·**선언부를 준 `P3g` 까지**) — [중요] **모델을 키워도 우리가 실제로 겪은 실패는 안 잡힌다**(`gemma4:e4b` 4B급이 35B 와 동률이던 것과 같은 방향). 게다가 **계약위반 0 → 3**(판정 형식 미준수 → 게이트에 못 물린다). 층2 총 **19s → 493s**.<br>[주의] **사고(thinking) 기본 켜짐** — 같은 프롬프트에 **8161토큰·207초**. 우리 `call_broker` 가 이미 `think:false` 를 싣고 있어 파이프라인은 무영향(그 주석의 근거가 여기서 실증됐다).<br>[주의] `Q3_K_M`(12.7GB)은 3060 12GB 에 **안 들어간다** — 이 카드에서 올릴 수 있는 상한이 Q2_K 다. ollama 라이브러리엔 qwen3.6 이 **없다**(실측 404) — HF GGUF 경유 |
| `devstral:24b` | bc250 | ⬜ **미측정** | — |
| `gemma2:2b` · `qwen3:8b` · `llama3` · `llama3.1` | `.2` | ⬜ **미측정** | — |

### Tool-calling (에이전틱 드라이버 관문) — 실측 2026-08-17 (#106)

하네스 `~/claude-workspace/bin/bench_toolcall.py` (관문 4종: 단발·선택·멀티턴·억제,
`temperature:0`·`num_ctx:8192`) + 한글 변형 3종. [중요] **`/api/show` 의 tools 광고는 믿지
않는다** — 14b 가 광고와 달리 구조화 방출을 못 했다 (#106 의 출발점).

| 모델 | 영문 4관문 | 한글 3관문 | 비고 |
|---|---|---|---|
| **`gemma4:e4b`** | [완료] **4/4 (3회 반복 동일)** | [완료] **3/3** | **에이전틱 드라이버 채택 (#106, 사용자 확정)** — 이미 bc250 상주라 추가 비용 0. 지연 3~9s. 연속 30사이클 30/30, 메모리 ~-5MB/사이클 (35B 의 -120 과 다른 차원). [중요] 가짜 도시명("city number 7")에 인자를 지어내지 않고 되묻는다 — 35B 환각과 정반대 성향. 인자 한글(`'서울'`) 정확 |
| `qwen3:8b` | [완료] 4/4 | [완료] 3/3 | **폴백** (`.2` 설치분으로 측정). 첫 호출 56s(적재), 상주 후 3~4s |
| `llama3.1:8b` | 3/4 | ⬜ | 억제 관문 오답 — 도구 헛호출은 없으나 산수를 거부 |
| `gemma3:12b` | [실패] 0/4 | — | **tool 미지원** (서버 400 거부). gemma3→gemma4 사이에 생긴 능력 |
| `35B-A3B IQ2_M` | [완료] 4/4 (2026-08-08) | ⬜ | 능력은 되나 **상주 불가** (#105 GTT 누적) — 드라이버로 못 쓴다 |
| `devstral:24b` | 3/4 (2026-08-08) | ⬜ | 프롬프트에 형식 지시가 붙으면 호출을 빠뜨림 (리포트 06 §4.5.1) |
| `qwen2.5-coder:14b` | [실패] 1/4 (2026-08-08) | — | 구조화 방출 불가 — 호출 JSON 이 content 로 샘 |

### 임베딩 (검색용, 마스터 로컬)

| 모델 | 상태 | 근거 |
|---|---|---|
| **`paraphrase-multilingual-MiniLM-L12-v2`** (fastembed/ONNX) | [완료] **채택** | **384차원** — 원본이 기록한 *"384차원짜리는 저장 증가 0"* 조건에 부합. PyTorch 불필요(ONNX). 「전역 이벤트 시스템」↔`global event system` 코사인 **0.817** |
| `all-MiniLM-L6-v2` (chromadb 내장) | ⬜ 되돌릴 때만 | [중요] **영어 전용.** 한글 질의가 벡터·BM25 **두 채널 모두 실패**했다 — 「전역 이벤트 시스템」이 전부 오답, `global event system` 은 정답 |

## 상용 CLI

| | 어디 | 유리한 작업 | [중요] 주의 |
|---|---|---|---|
| **Claude Code** | `.2` · `.33` | 코드 작성·리뷰 (원본이 층2 검증에 쓴 백엔드) | 한도 신호 감지 시 **5시간 차단**이 원본 정책. 차단 중엔 폴백도 건너뛴다(다른 백엔드 과부하 방지) |
| **`agy` (Antigravity)** | 마스터 1.1.10 · `.33` 1.0.16 | 큰 파일 단발 분석 (토큰 절감) | [중요] **웹 검색을 시키면 출처를 지어낸다** — 파일을 `--add-dir` 로 쥐여줄 것.<br>[중요] **작업 디렉토리에서 자율 실행 금지** — `--dangerously-skip-permissions` 를 쓰며 동의 없이 파일을 고친 적이 있다.<br>[중요] `.33` 은 **1.0.16 으로 구버전** — 마스터가 겪은 PTY·ANSI·argv 함정이 거기서 고쳐졌는지 미검증 |
| **Codex CLI** (`gpt-6-astra`) | **네 대 전부** — `.57` `.43`(설치 스크립트·node 불필요) · `.33` `.2`(npm 전역) · 2026-09-05 | 본문 작성(층1 동등) · 기존 클래스 수정 골조 | 🔴 **기본 추론이 `none` 이다** — 명시 안 하면 추론 없이 돈다(`-c model_reasoning_effort="…"`). 그 상태로 Opus 와 비교해 오판할 뻔했다.<br>[중요] **`--json -o` 로 부른다** — 텍스트 출력엔 세션 머리말·추론 요약·`tokens used` 가 섞여 본문 길이를 **23,856자로 잘못 읽었다**(실제 8,058자).<br>[중요] **구독 한도가 네 대 공용**이다 — 워커들이 각자 돌리면 `claude` 를 레인에서 뺀 것과 같은 위험(429 하나로 조각 여럿 사망).<br>[주의] `.2` 는 **PowerShell 경로로** 불러야 한다 — cmd 에서는 CP949 로 깨진다 |
| Gemini CLI | `.2` | — | 원본은 **2026-06-18 개인티어 종료로 실행 스택을 통째 제거**하고 `agy` 로 갔다. 새로 배선하지 말 것 |

---

## 이 표를 갱신하는 규칙

### 골조·본문 A/B — 실측 2026-09-05 (Codex 도입 판단)

하네스 `master/bench_skeleton_codex.py`(골조) · `master/bench_lane_codex.py`(본문).
판정은 **골조=동결 선언 수·공개 시그니처 보존**, **본문=층1(`layer1.check`)** — 둘 다 결정적이다.

**본문 작성** (같은 골조 · 같은 프롬프트)

| 레인 | 초 | 층1 | `[PSEUDO]` 잔재 | 산출 |
|---|---|---|---|---|
| `agy` | **32.9** | 통과 | 0 | `.h` 4,501자 · `.cpp` 3,303자 |
| `codex` | 98.6 | 통과 | 0 | `.h` 4,501자 · `.cpp` 3,397자 |
| `qwen2.5-coder:14b` | 117.6 | 통과 | 0 | (2026-09-04 측정) |

🔴 [중요] **두 레인의 헤더가 바이트 단위로 동일했다** — 동결이 실제로 지켜진다는 뜻이다.
품질이 같으므로 **빠른 순**으로 세웠다: `agy → codex → 로컬`.

**골조 생성**

| 대상 | Claude(Opus) | Codex | 비고 |
|---|---|---|---|
| 신규 클래스 | 150.6초 · 동결 **20** | 61.6초 · 동결 **5** | Codex 가 *"제공되지 않았다 … 추측하지 않기 위해 보류"* 로 선언을 안 냈다 |
| 기존 수정 (예산 1,500) | (생성됨) | **거절** | 🔴 **프롬프트가 잘려 있었다** — 아래 |
| 기존 수정 (예산 20,000) | 160.7초 · 동결 **29** · 시그니처 4/4 | 93.3초 · 동결 **23** · 시그니처 4/4 | 대등해졌다 |

🔴 [중요] **모델을 재기 전에 프롬프트를 의심하라.** 기존 클래스 수정에서 Codex 가 거절한 이유는
선언부 grounding 이 `PER_CLASS=1,500자` 예산에 잘려 `SetDetected` 중간에서 끊겼기 때문이다
(실물 헤더 3,915자 · NS 헤더 47개 중 **21개가 예산 초과**). **모델의 옳은 행동이었고 결함은
우리 쪽이었다** — `#369`. [주의] **Claude 는 같은 입력에서도 만들어 냈다** — 이 부류는
모델에 따라 조용히 지나간다.

[주의] **표본은 각 1이다.** 그리고 위 Codex 수치는 **추론 `none`** 에서 나온 하한선이다.

### 토큰 계측 — `work/usage.py` (2026-09-05)

세 CLI 가 형식이 다 다르다. **집계 축이 달라 숫자를 그대로 비교하면 안 된다.**

    claude   --output-format json → usage{input, output, cache_read_input} + total_cost_usd
    agy      --output-format json → usage{input, output, cache_read, total}   ← 키가 다르다
    codex    --json 이벤트 turn.completed.usage{input, cached_input, cache_write, output,
             reasoning_output}   ← 항목별. 텍스트의 `tokens used` 는 합계 하나뿐이라 안 쓴다

[중요] **모르는 값은 `None` 이다 — 0 으로 적지 않는다.**

### 층2 검출력 — 실측 2026-08-21 (코드 레인 교체 트라이얼)

하네스 `master/bench_layer2.py` (표본 7: 양성 5·음성 2 · 채점은 `parse_verdict` 만 보는 **결정적** 방식).
[중요] 고르는 순서는 하네스가 적어 둔 그대로 — **놓침 0 먼저 → 오탐 0 → 속도**.

| 백엔드 | 검출 | 놓침 | 오탐 | 정상통과 | 계약위반 | 초 |
|---|---|---|---|---|---|---|
| `qwen2.5-coder:14b` | 3 | **2** | 0 | 2 | **0** | **19** |
| `Qwen3.6-27B-A3B-Coder:Q2_K` | 3 | **2** | 0 | 2 | **3** | 493 |

[중요] **놓친 표본이 두 모델 모두 `P3`·`P3g` 다** — `P3g` 는 **선언부를 함께 준** 변형이다.
grounding 을 줘도 못 잡았다는 것은 이 축에서 모델 크기가 답이 아니라는 뜻이다.
[주의] 후보의 493초 중 **239초는 `P4` 한 표본** — 8192 상주 중 하네스가 `num_ctx=16384` 로 불러
**모델을 통째로 재적재**했다(이 저장소가 이미 아는 함정이고, 10GB 모델에서 더 비싸다).

1. **재본 것만 적는다.** 미측정은 `⬜` 로 남긴다 — 빈칸을 추측으로 채우면 이 문서가 쓸모없어진다
2. **환각은 재현 조건까지 적는다** — "환각이 있었다" 만 적으면 다음 세션이 판단을 못 한다.
   어느 파일·어느 식별자였는지가 근거다
3. **모델을 바꿀 때 통과율을 같이 잰다** — `python -m master.context_synth sample N` 이
   받아온 스냅샷을 건드리지 않고 통과율만 낸다
4. [중요] **표본 수를 적는다.** 지금 합성 판정은 **그룹 3~4개 표본**이다. 얇다 —
   실제 커밋이 돌면 자연히 쌓이므로 그때 갱신한다
