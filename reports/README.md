# 작업 리포트 — 저장소 작업분

**저장소 코드에 대한 결정·실측·함정** 기록. 3자 동기(마스터·GitHub·BC-250)에 포함된다.

🔴 **왜 여기로 옮겼나 (2026-08-08).** 원래는 `~/claude-workspace/reports/` 에만 있었다.
그러면 두 가지가 깨진다:

1. **동기에서 빠진다** — 마스터 로컬에만 있어 이 기계가 죽으면 사라진다
2. **안 읽힌다** — 실측된 사실이다. 리포트 08 은 82KB 인데 "세션 시작 시 정독" 이 지켜진
   적이 없다. 2026-08-08 세션에서 리포트 07 을 `tail` 만 하고 넘어간 것이 드러났다

하드웨어·인프라 로그(01·03·04·05)는 저장소와 무관하므로 `~/claude-workspace/reports/` 에 남는다.

## 읽는 법 — 전부 정독하지 말 것

분량 때문에 정독은 실패한다. **먼저 `docs/` 를 보라** — 리포트의 결론은 이미 계획 문서로
승격돼 있다:

| 알고 싶은 것 | 보는 곳 |
|---|---|
| 지금 어느 마일스톤인가 | 🔴 **`docs/milestones/4-origin-reconciliation.md`** (M1·M2 닫힘, **M3 는 `locked`=진행 정지** — 전부 참조용이고 로드하지 말 것) |
| 몇 개 중 몇 개인가 | 🔴 **레드마인** (version *마일스톤 4*) — 분자도 분모도 문서에 없다. 손으로 적은 분자가 M2 에서 두 번 어긋났다 |
| 무엇이 되고 무엇이 안 됐나 | 마일스톤 문서 본문 — 대 3 · 중 10 |
| 닫힌 결정 | `docs/settled-decisions.md` |

리포트는 **"왜 그렇게 결정했나" 를 되짚을 때** 펼친다. 목차(`## ` 헤더)로 해당 절만 읽으면 된다.

| | 내용 |
|---|---|
| `06` | 마스터 실행 모델 확정 · task_queue 이식 |
| `07` | 능력 라우팅 · 2번째 추론 엔드포인트 · 브로커 |
| `08` | 프로젝트 레지스트리 · 문서 분리 · 층3 게이트 · 인증 · 이벤트 큐 · 검색 코어 · 워크플로우 (§1~§16) |
| `09` | 전체 분석 · 개선점 · 🔴 **AgentTest 백포트 후보**(협업 관점) |
| `11` | 온톨로지 **LLM 재합성**(소 1.3.3 — 조각화·2노드 병렬·로컬 vs claude 실측) · 🔴 **§3 워커 배선 구멍** · §4 자동승급 폐기 · §7 분모 정정 · **§12 도메인 색인(2.1)·도메인 규범(2.3)** — 🔴 §12.4 *규범이 생성 결과를 바꿨지만 철자 환각은 남았다* |
| `10` | 목표 확정(시소러스 누락 정정) · 마일스톤 대중소 재구성 · **관계 그래프 이식**(중 1.1, 원본 버그 1건 수정) · 컨텍스트 합성기(중 1.2) · 🔴 **§8 BC-250 먹통 사고** · **§9 중 1.2 완료·검색 재설계·중 1.3 착수** |
| `12` | **마일스톤 3 착수** — 골조 생성 + 🔴 **인터페이스 동결**(중 1.1) · 분해(중 1.2) · 🔴 골조는 **등재 전에 빌드**한다 · 🔴 *"분산이 사는 것은 처리량이 아니라 증분"* (사용자 재정의) |
| `14` | 🔴 **원전 미참조 전수 조사** — 인용 census(원전 170파일 × 우리 54,356줄 → **인용 0건 74파일**) · §2 **`cluster_coordinator.py` 200줄이 인용 0회 = 「단일 writer」는 발명이 아니었다** · §3 열린 4건은 설계가 아니라 **이식**이었다 · §4 되먹임 5종 미참조 · §5 `@ms-contract`·부분 무효화 · §7 74파일 1차 분류(🔴 **§7.1 census 가 스스로 틀린 자리**) · §8 영향도 · §9 **M3 `locked` · M4 신설(대 4·중 17·소 45)** |
| `13` | **M3 대 1·대 2 완주 + 중 3.2** — 추론 파견(§1~§11) · 통합자(§12) · **3층 판정**(§13, 🔴 §13.2 층2 모델 실측·근거 없는 층2 는 의미 없다) · 🔴 **§14 주입은 계약을 재고 실물을 재지 않는다**(같은 교훈 4회) · 상태 화면(§15) · 🔴 **§16~§19 사용자 지적으로 되돌린 것 셋** — 로컬 파일 웹 UI · 읽기 전용 화면의 로그인 · **원전 3,700줄을 안 읽고 새로 쓴 뷰어**. 🔴 **§18 이 그 규칙(애매하면 묻는다)을 CLAUDE.md 에 넣은 기록** · §20 마감 |
