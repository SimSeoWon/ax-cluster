# 17. 부분 무효화 이식 — 변경분만 다시 만든다 (2026-08-15)

> 마일스톤 4 · 대 3 미이식 자산 재검토·이식 · **중 3.2 결정적 온톨로지 장치** (`#151`)
> — 소 3.2.2 (`#153`) · 🔴 **소 3.2.3 (`#195`)·소 3.2.4 (`#196`) 은 이 세션에 신설**했다
> (읽고 재다가 나왔다) · 소 3.2.1 (`#152`) 재범위.
> 원전: `watcher/ontology_invalidation.py` 190줄 (Phase η.7.2/.3, 2026-05-31) +
> `ontology_synthesizer` 스탬프 절(η.7.2 제안 2) + `ontology_refresh:527`(η.2.5).
>
> 🔴 **이 세션의 형태 하나: 이식할 때마다 그 아래 미이식이 하나씩 나왔다.** 부분 무효화를
> 옮기니 워터마크가 안 찍히는 것이 보였고, 그것을 고쳐 실증하니 색인이 안 깨어나는 것이
> 보였다. **장치는 사슬로 놓여 있고, 한 칸만 옮기면 그 칸이 거짓말을 한다.**

## §1 세션 시작 상태 (실측)

    세 클론        fa67c83 동일 · 작업트리 청결 (마스터 · Gitea · .43)
    유닛 7종       HTTP 3 + ax-indexer.path + timer 3 — 전부 active
    기계           .43 ✅ · .2 ✅ · .33 ✅ (지난 세션엔 꺼져 있었다)
    레드마인 M4    36/78 (version *마일스톤 4* = id 3)

**다음 작업은 사용자가 골랐다** — 후보 넷의 **입력 실재 여부를 먼저 재서** 함께 냈고
(`#153` LLM 0·입력 실재 / `#152` 태그 0건 / `#187` 도메인 15개만큼 LLM 비용 / 중 3.3 입력 0건),
사용자가 `#153` 을 지정했다.

## §2 착수 전 대조 — 무엇이 이미 있고 무엇이 없나

능력 단위로 맞췄다(이름 대조는 양방향으로 틀린다 — 마일스톤 4 함정).

| 원전 능력 | 우리 상태 |
|---|---|
| `changed_member_classes` (저장 `source_commit` ≠ 문서 `source_commit`) | ✅ **이미 있다** — `ontology/stale.py` 가 같은 판정식 |
| `L*/{actions,invariants}/*.yaml` 레이아웃 | ✅ 동일 |
| `write_domain_yaml(cleanup_stale=False)` 머지 모드 | ✅ `package.write(prune=False)` 로 있다 (⚠️ 결함 하나 — §5) |
| `determine_invalidation` · `plan_domain_refresh` | 🔴 **없다** |
| manifest `md_hash` 스탬프 | 🔴 **안 찍는다** (7개 중 3개 보유 = 전부 스냅샷 유산) |
| `_refresh_one_domain(changed_classes=set)` partial 분기 | 🔴 **없다** (full 만) |

🔴 **판정식을 다시 구현하지 않았다.** 원전 `changed_member_classes` 와 우리 `stale.compute`
는 같은 식이다. 두 벌을 두면 언젠가 갈라지고, 그때 어느 쪽이 맞는지 아무도 모른다.

## §3 🔴 신호원을 안 찍으면 판정기는 영원히 full 을 고른다

`plan_domain_refresh` 의 full 폴백 조건은 *"manifest `md_hash` 부재 또는 도메인 MD 변경"* 이다.
실측:

    md_hash 보유         7개 도메인 중 3개 — 🔴 전부 **받아온 스냅샷**이 찍은 것
    그중 현재 MD 와 일치   1개 (UiManagement) · 불일치 2개
    우리 package.write    🔴 **한 번도 안 찍었다**

즉 판정기만 옮기면 **모든 도메인이 영구히 full** 이다. 원전은 같은 사이클에서
`write_domain_yaml` 이 `md_hash` 를 찍게 했으므로(`ontology_yaml_dump.py:276`) 그 스탬프도
같은 커밋에 넣었다 — 마일스톤 4 의 *"가드를 만들면 깨울 주체를 같은 커밋에"* 와 같은 결이다.

⚠️ **해시식이 원전과 같은지 먼저 쟀다.** `UiManagement` 의 스냅샷 `md_hash` 와 우리
`domain_md.content_hash` 가 **같은 값**(`51bbaedc41ffa531`)이다. 달랐으면 안 바뀐 도메인을
*"MD 변경"* 으로 읽어 부분 갱신이 통째로 죽는다 — 조용히.

## §4 🔴 반사실 실측 — 원전 그대로 옮겼으면 스냅샷 37건이 지워졌다

원전 partial 은 무효화 yaml 을 **선삭제**한다. 우리 항목 82건을 실제로 판정해 보니:

| 도메인 | 변경 클래스 | 원전대로면 선삭제 |
|---|---|---|
| GlobalEventSystem | 1/9 | 1건 |
| MissionEditor | 3/13 | 6건 |
| MissionRuntime | 3/11 | 11건 |
| UiManagement | 8/9 | 19건 |
| | | 🔴 **합계 37건** |

**그 37건은 전부 `protected` 스냅샷이다.** `protected` 는 원전에 없는 **우리 개념**이고
(리포트 11 §20 — 우리 합성이 스냅샷보다 **얕았다**), 삭제하면 `package.write` 의
`locked_items()` 가 디스크에서 그것을 못 읽어 **잠금 보존 기제 자체가 무력화**된다.

→ **잠긴·보호 항목은 무효화 대상에서 뺀다**(`preserved` 로 센다). ⚠️ 이것은 「안 옮기는
판단」이 아니라 **기제는 그대로 옮기고 우리 잠금 규약을 끼워 넣은 것**이다.

**원전과 다르게 둔 것 하나 더**: 무효화 파일 삭제를 **추출 전 → 쓰기 직전**으로 옮겼다.
LLM 이 통째로 실패하면 원전은 대체물 없이 지워진 상태로 끝난다. 성공했을 때의 최종 상태는
같고 실패했을 때만 다르다 — 우리 규약(*"실패는 결과로 돌려준다"*)과 맞는 방향이다.

## §5 ⚠️ 이식이 옆 함수의 결함을 드러냈다 — `prune=False` 가 manifest 를 잃고 있었다

`package.write(prune=False)` 는 이번에 안 들어온 항목 **파일은 남기면서 manifest 목록은
이번 것만으로 다시 썼다.** 그러면 파일은 있는데 **색인에는 없다** — manifest 는 스스로
*"DB 인덱싱 단일 SSOT"* 라고 적어 둔 자리다. 부분 갱신이 그 인자를 처음 실제로 쓰면서 드러났다.
→ `prune=False` 일 때 디스크를 훑어 합친다(원전도 `cleanup_stale=False` 에서 디렉토리를 walk 한다).

🔴 **이 인자는 지금까지 아무도 쓰지 않았다** — 실 사용자가 생기기 전까지 결함이 조용했다는
뜻이고, 「주입은 계약을 재고 실물을 재지 않는다」의 사촌이다.

## §6 이식분과 배선 (같은 커밋 — `#123` 의 재발 방지)

    master/ontology/invalidate.py      신규 — 원전 190줄. 설계 결정 셋 그대로
                                       (scope=변경∪응집 · 보수적 무효화 · MD 해시 변경시 full)
    ontology/domain_md.content_hash    MD 원문 md5[:16] — 🔴 파서가 아니라 **원문**을 잰다
                                       (파서가 모르는 절의 변경이 해시에 안 잡히면 오판한다)
    ontology/package.write             md_hash 스탬프 + prune=False 의 manifest 보존
    ontology/synth.refresh_domain      `changed_classes=` partial 분기 (원전 `_refresh_one_domain`)
    ontology/synth.run                 stale walk 가 도메인별로 full/partial 을 정한다.
                                       🔴 사람이 도메인을 명시하면 **언제나 full** (원전 force 경로)
    ontology/__main__ status           판정과 **사유**를 사전에 보여 준다 (LLM 0 · 부작용 0)

🔴 **`#123` 은 이식이 아니라 배선이 없어서 한 마일스톤 내내 죽어 있었다.** 같은 실수를
반복하지 않으려고 판정기·스탬프·호출자·CLI 표면을 한 번에 넣었다.

## §7 실 구동 (LLM 0 — 판정만 돌렸다)

    ■ GlobalEventSystem → full (이유: 도메인 MD 변경 4d0e8d06≠17b3c0db)   변경 1 · 보존 16
    ■ MissionEditor     → full (이유: 도메인 MD 변경 8900e6da≠80cb6638)   변경 3 · 보존 22
    ■ MissionRuntime    → full (이유: manifest md_hash 부재/레거시)        변경 3 · 보존 22
    ■ UiManagement      → 🔵 partial (변경 8건: AMSHUD · UMSHUDWidget …)  변경 8 · 보존 22

🔴 **지금 데이터에서 무효화는 0 이다** — 항목 82건이 **전부 잠금·보호**라서다. 그래서 현재의
부분 갱신 이득은 **추출 scope 축소뿐**이고, 판정 출력이 그 사실을 그대로 말한다.
⚠️ **이 값을 「이식이 무의미하다」로 읽으면 틀린다** — 셋은 md_hash 가 없거나 낡아서 full 인
것이고, **이제 우리가 그 값을 찍으므로 다음 full 재합성부터 partial 이 걸린다**(원전의
레거시 폴백과 같은 성질: eventually-consistent).

## §8 테스트

    신규   master/test_invalidate.py   42/42  (원전 tests/test_eta_7_partial.py 16건 대응 +
                                              우리 것 둘: 잠금 보존 · prune=False manifest 보존)
    전체   2626 → **2668/2668 · 실패 파일 0개**

⚠️ **이 수치는 §10·§12·§14 에서 다시 움직인다** — 최종은 **2743/2743**.
그리고 그 사이에 §10.6 의 계측 결함 하나가 여기 숨어 있었다(스위트가 7분이었다).

## §9 레드마인

    #153  소 3.2.2  → **완료** (이식·배선·실측 노트)
    #195  소 3.2.3  → **신설·완료** — 워터마크 settle(η.7.2 제안 2 의 안 옮긴 절반).
                    🔴 분모가 78 → 79 로 움직였다. **읽어서 나온 작업**이고 그것이 정상이다
    #152  소 3.2.1  🔴 **재범위** — `@ms-contract` 태그가 미러 소스에 **0건**. 지금 이식하면
                    「입력 없는 배치」다. 전제가 바뀌면 되살린다 — **닫지 않는다**
    #196  소 3.2.4  → **신설·완료** — 재합성 후 도메인 색인 동기 배선(원전 η.2.5)
    #187  소 3.1.4  → **완료** — 레이어별 책임 서술(원전 η.7.4) + MCP 조회 창구.
                    ⚠️ 중 3.1 은 **7/8** — `#189` 는 머지 후 결정으로 남는다
    #194  중 3.7    한글 베이스 **머지 지점 8** 추가 — 무효화 언급 판정이 영문 식별자 토큰
                    전용이라 한글 별칭으로만 가리킨 yaml 은 무효화되지 않는다 (지금은 실해 0)

    M4  36/78 → **40/80**  (분모가 둘 늘었다 — `#195`·`#196` 둘 다 **읽어서** 나왔다)

## §10 소 3.2.3 (`#195`) — 워터마크 settle · **사용자가 이것을 먼저로 골랐다**

§9 에 「남은 것」으로 적어 둔 구멍을 **같은 세션에 닫았다.** 사용자가 순서를 물었고, 내
추천(①)이 채택됐다 — 근거는 *"방금 이식한 부분 무효화의 상류"* 였다. 변경 집합이 줄지
않으면 partial 이든 full 이든 **비용이 안 줄기 때문**이다.

### §10.1 🔴 DB 경로를 그대로 옮기면 아무 일도 안 일어난다

원전은 스탬프를 **둘 다** 찍는다 — 오브젝트 yaml + `class_ontology` DB. 그런데:

    원전 upsert_class_source_commit   "기존 row 부재면 no-op" (원전 주석 그대로)
    우리 class_ontology                🔴 **0행** · INSERT/UPDATE 하는 코드 **0건**

→ DB 절반은 옮겨도 **no-op** 이고, `#189` 가 그 표에 쓰는 것을 *"우리가 없앤 드리프트를
되살린다"* 로 판정해 뒀다. **안 옮긴다**(재범위는 닫지 않는다).
→ 신호원을 **오브젝트 yaml(우리 SSOT)** 로 바꾼다. 세션마다 반복된 그 형태다 —
**기제는 옮기고 신호원은 다시 고른다.**

### §10.2 🔴 신호원 교체가 판정을 **양방향으로** 고쳤다 (실측)

`_stored` 가 DB 만 보던 탓에 **오브젝트 yaml 112개 중 16개가 이미 갖고 있던 실 리비전을
한 번도 안 읽고 있었다.**

| 도메인 | 전 | 후 | 뜻 |
|---|---|---|---|
| UiManagement | 8/9 stale | 🔴 **정합(사라짐)** | 없는 결함 8건을 세고 있었다 |
| GlobalEventSystem | 1/9 | **5/9** | DB 만 보느라 4건을 **놓치고** 있었다 |
| MissionEditor · MissionRuntime | 3/13 · 3/11 | 그대로 | — |

⚠️ σ.9.0 의 `mtime`→git 과 **같은 계열**이고, 그때는 한쪽 방향(없는 결함 25건)만 틀렸는데
이번엔 **양쪽**이 틀렸다. 🔴 *"고쳤더니 수가 줄었다"* 만 보고 접었으면 늘어난 4건을 못 봤다.

### §10.3 🔴 원전과 다르게 둔 것 — **필드 하나만** 갱신한다

원전은 objects 를 통째로 다시 쓰며 스탬프를 얹는다. **그래도 되는 이유가 있었다** —
원전의 잠금은 `actions`/`invariants` **전용**이다(`load_locked_items(subdir)`). 우리는 스냅샷
오브젝트 112개가 **전부 `protected`** 라 통째로 쓰면 `merge_preserve_locked` 가 새 값을 버려
**스탬프가 영영 안 들어간다.**

→ 워터마크 필드만 제자리에서 올린다. 잠금의 목적은 **본문**(설명·근거·별칭) 보존이지 기계가
찍는 표식을 얼리는 것이 아니다 — 얼리면 그 도메인이 영원히 재합성 대상으로 남아 **보호가
의도하지 않은 비용**을 만든다. `package.HUMAN_FIELDS`(사람 필드는 이월한다)의 정확한 거울이다.

### §10.4 🔴 내가 틀렸다가 실측에 잡힌 것 — `-1` 스탬프를 「퇴행」으로 읽었다

처음엔 *"모르는 값으로 아는 값을 덮지 않는다"* 로 짜고 테스트까지 붙였다. **원전은 무조건
찍는다** — `o["source_commit"] = rev` 는 조건이 없고 `rev != "-1"` 은 **카나리 집계용**이다.

건너뛰면 `GlobalEventSystem` 의 5개가 **영원히** 재합성 대상으로 남는다. 그 5개는 오브젝트에
실 리비전이 있는데 **컨텍스트 문서가 리비전을 잃은** 상태 — 리포트 16 §12.7 이 확정한
**물려받은 σ.7 손상**이다. 🔴 이 필드의 뜻은 *"우리가 아는 가장 좋은 리비전"* 이 아니라
**"이 합성이 반영한 문서가 그때 말한 값"** 이다. 문서가 모른다고 하면 `-1` 이 사실이다.
⚠️ 문서 손상은 여기서 알릴 일이 아니다 — `context_audit` 의 `recent_but_empty` 가 그 자리다.

### §10.5 실증 (실 데이터 **복사본** · 실 트윈 무변경 · LLM 0)

    settle 전   GlobalEventSystem 5/9 · MissionEditor 3/13 · MissionRuntime 3/11
    settle      11건 갱신
    settle 후   🔴 **stale 0** — 전부 가라앉았다
    재호출      0건 (idempotent — 재시작해도 같은 답)

🔴 **트윈은 `.gitignore` 안이라 되돌릴 수 없다**(`git ls-files ModularStage/ontology` → 0).
그래서 실 트윈에는 손대지 않고 복사본에서 쟀다. ⚠️ **실 흐름에서는 재합성이 성공한 뒤에만**
찍힌다 — 합성 없이 settle 을 부르면 *"반영했다"* 는 거짓말이 된다(테스트로 고정: partial 은
scope 밖 클래스를 안 올린다).

테스트 21건 추가(42 → 63) · 전체 2668 → **2689/2689 · 실패 0**.

## §10.6 ⚠️ 내 **테스트**가 조용히 틀렸다 — `dry_run` 은 LLM 을 막지 않는다

배선 테스트에 *"🔴 LLM 없이 partial 분기가 도는지"* 라고 적고 `dry_run=True` 로 불렀다.
🔴 **`dry_run` 은 쓰기만 막는다** — 추출 레인은 그대로 돌아 **실제 노드를 두드리고 있었다.**

    test_invalidate.py 단독   🔴 **360초** (통과는 한다 — 그래서 안 보였다)
    전체 스위트               ~7분

추출 레인을 막아 네트워크 0 으로 만들었다(분기 판정은 추출 **전에** 끝나므로 계약은 그대로):

    test_invalidate.py   360초 → **0.24초**   ·   전체 스위트 → **28초**

🔴 **마일스톤 4 의 *"측정 도구 자신이 조용히 틀린다"* 가 이번엔 테스트에서 났다.** 초록색이라
아무도 안 본다는 점에서 더 나쁘다 — **테스트가 느리면 그 자체가 신호다.**

## §11 실 트윈 재합성 실증 (사용자 승인 2026-08-15) — 🔴 **이 절은 실행 *전에* 썼다**

트윈은 `.gitignore` 안이라 되돌릴 수 없다. 규약대로 실행할 것을 먼저 적는다.

    대상        MissionRuntime — 🔴 manifest md_hash **부재**라 full 로 돈다.
                그 한 번이 md_hash 를 찍으므로 **다음 사이클부터 partial 이 걸린다**
    백업        ModularStage/ontology 전체 → 스크래치패드 `ontology-before-refresh.tgz` (49KB)
                (git 이 없으니 손으로 되돌릴 수 있는 사본을 먼저 만든다)
    노드        bc250 = 35B 상주 ✅ · rtx3060 = 14b 상주 ✅ · inflight 0 (핀 유지 확인)
    규모        멤버 11 → 3조각 · 프롬프트 71,202자 · **요청 6건** (actions 3 + invariants 3)
    명령        python -m master.ontology refresh MissionRuntime
    기대        ① 조각별 추출 → 사실 게이트 → package.write(prune=True, full)
                ② manifest 에 md_hash 스탬프
                ③ 🔴 settle 이 워터마크를 올려 **stale 3/11 → 0**
                ④ 잠긴·보호 22건은 그대로 (merge_preserve_locked)
    되돌리기    tar xzf …/ontology-before-refresh.tgz -C ~/ax-cluster/ModularStage

### §11.1 실행 기록 — 🔴 **전부 기대대로, 그리고 보존을 바이트로 확인했다**

    소요        152초 (2m32s) · 요청 6건 · 3조각(35B ×2 레인 / 14b ×1)
    추출        actions 15 (5/5·5/5·5/5) · invariants 20 (7/7·6/6·7/7) · 사실 게이트 버림 0
    쓰기        작성 36 · 그대로 22 · 🔒 **잠금 보존 22** · 제거 0
    md_hash     🔴 `f3296fb84f00e162` — 현재 도메인 MD 해시와 **일치**
    settle      워터마크 **3건 갱신**
    stale       🔴 **3/11 → 사라짐** (`MissionRuntime` 이 stale 목록에서 빠졌다)

**보존 검증 — 백업과 바이트 단위 대조** (*"보존했다"* 를 세지 않고 **쟀다**):

    잠긴 actions·invariants 22건   동일 22 · 달라짐 0 · 🔴 **사라짐 0**
    오브젝트 yaml 11건             🔴 **워터마크 외 변화 0** (별칭·protected·confidence 그대로)

**다음 사이클 판정** — 이식의 목적이 실제로 달성됐는지:

    MissionRuntime → partial (변경 클래스 0건)      ← 🔴 **더 이상 full 폴백이 아니다**

전에는 *"manifest md_hash 부재/레거시 → full"* 이었다. 한 번의 full 이 스탬프를 찍었고,
이제 이 도메인은 **변경분만 다시 만드는 경로**에 올라섰다. 통짜 재합성 1회의 비용(152초 ·
요청 6건)이 그 전환의 대가였다.

⚠️ **트윈이 실제로 자랐다** — actions 12 → **27** · invariants 10 → **30**. 새로 쓴 35건은
합성물이라 `protected` 가 아니다(설계대로 — 보호는 받아온 스냅샷의 표식이고, 합성이 만든
것은 개선돼야 한다). 되돌릴 사본은 스크래치패드에 남아 있다.

## §12 소 3.2.4 (`#196`) — 재합성이 검색 색인을 깨우지 않고 있었다

§11 직후 파생물을 재다가 나왔다. **실증이 다음 미이식을 꺼낸 것**이고, 이 세션에 그 형태가
세 번째다(판정의 한쪽만 이식 · 신호원 미스탬프 · 이 건).

    실측       MissionRuntime 이 오늘 actions 12→27 · invariants 10→30 으로 자랐는데
               domain_index.json · bm25_domains.db 는 🔴 **닷새 전(8/10)** 것이었다
    원인       원전 `ontology_refresh.run()` 끝의 `try_sync_domain_index`(η.2.5)가 **미이식**.
               우리 색인을 깨우는 경로는 둘뿐이고 **둘 다 이 자리가 아니다** —
               `rebuild_all` 은 **컨텍스트 MD** 변화로만(색인기 로그가 그렇게 말한다),
               `staging.commit` 은 스테이징 세션을 쓸 때만
    이식        `synth.sync_index()` + `run()` 끝에서 **배치당 한 번** 호출 (원전과 같은 자리)

**다르게 둔 것 둘.** 원전 가드는 프로세스 안 전역(`_LAST_FINGERPRINT`)이라 재시작하면 잊는데
우리 `domain_index.sync` 는 **디스크에 지문을 남긴다** → 그냥 부르면 스스로 건너뛴다.
그리고 원전은 예외를 로그로 흘리지만 우리는 **값으로 돌려준다**(σ 기준) — 색인이 실패해도
재합성 결과는 이미 디스크에 있으므로 **배치를 죽이지 않는다.**

**실 구동** (LLM 0 · 파생물 재생성):

    색인 동기   도메인 7 · BM25 7 · 벡터 7 · 두 파일 모두 02:29 로 갱신
    🔴 확인     오늘 합성된 액션 `StatefulTaskComplete` 이 **BM25 도메인 색인에 실렸다**
                (전에는 검색이 그것을 못 봤다)

테스트 6건 추가(63 → 69) · 전체 **2695/2695 · 실패 0 · 27초**.

## 남은 것

- ✅ **실 트윈 실증은 이 세션에 했다** (§11 — `MissionRuntime`, 사용자 승인).
- 🕓 **나머지 두 도메인은 안 돌렸다** — `GlobalEventSystem`(5/9) · `MissionEditor`(3/13) 은
  둘 다 *"도메인 MD 변경"* 이라 full 이 필요하고, 각각 152초 규모다. **순서는 사용자가 정한다.**
  ⚠️ `GlobalEventSystem` 의 5건은 **문서가 리비전을 잃은** 부류(§10.4)라, 재합성해도 워터마크는
  `-1` 로 가라앉을 뿐 문서 손상은 안 고쳐진다 — 그쪽은 `context_audit` 의 자리다
- 🔴 **부분 갱신이 실제로 «변경분만» 도는 것은 아직 못 봤다.** 오늘 본 것은 판정(`partial`)까지고,
  변경 클래스가 실제로 생기려면 **소스 커밋 → 색인기가 컨텍스트 MD 재합성 → 워터마크 갱신**이
  일어나야 한다. 다음 실 커밋이 그 실증이다
- 중 3.2 는 `#152` 재범위 때문에 닫지 않는다 (재범위는 닫지 않는다는 규약)
- 지난 세션에서 넘어온 결정 대기: `#141` 2단계(가드에 `selftest/*`) · 중 3.3 생산자 복각 여부 ·
  `#194` 머지(복각 후) · `_archive` 처리 · 시소러스 「미션시스템」이 레거시를 가리키는 것이 의도인지

## §14 소 3.1.4 (`#187`) — 레이어별 책임 서술 (원전 η.7.4 270줄)

사용자가 다음 작업으로 골랐다. 착수 전에 **비용을 쟀다**(이슈가 요구한 대로): 입력이
**이름과 요약뿐**이라 도메인당 프롬프트 1,600~2,900자 · **도메인당 LLM 1콜** · 로컬 노드 →
비용 0. 원전이 그렇게 설계한 이유가 문서에 있다 — *"서술은 이미 추출된 항목 위의 한 단계 위
추상이라 본문 재독 비용이 0"*.

**이식분** `master/ontology/descriptions.py`. 원전이 정한 것을 그대로: full 전용 · stale 축
제외 · 비용 가드(전부 잠기면 LLM 0) · **환각 레이어 제거** · 프롬프트 문구 원본 유지.
**배선 셋을 같은 커밋에** — 오늘 얻은 교훈(*"누가 읽는지까지 따라가라"*) 그대로:
`refresh_domain` 끝(full 일 때만) · CLI `describe [--show]` · 🔴 **MCP `get_layer_description_tool`**.

### 🔴 실측이 사고를 하나 막았다 — 트윈에 원전 서술이 이미 있었다

`describe --show` 를 돌리자 **7건(3 도메인)이 이미 있었다.** `created_by: ontology-descriptions`
· created 2026-06-03/07 — **원전이 만든 것**이고 🔴 **전부 미잠금**이었다.

내 첫 이식은 `verified_by_user` 만 봤다(원전엔 그것뿐이니까). **그대로 돌렸으면 35B 초안이
원전 산출물 7건을 덮었다** — 리포트 11 §20 이 *"우리 합성이 스냅샷보다 얕다"* 로 측정해 둔
바로 그 사고다. 🔴 그리고 우리 `package.locked_items` 는 **이미 두 표식을 함께 본다**
(`verified_by_user` + `protected`). 서술만 다르게 두면 **같은 데이터가 파일 종류에 따라 다르게
취급된다** — 그것이 드리프트의 시작이다. → `is_locked` 로 맞추고, 사용자 결정으로 그 7건에
`protected` + 사유를 찍었다.

⚠️ **이번에도 「원전에 없는 우리 개념」이 이식의 안전 조건이었다** — §4 의 무효화 선삭제와
같은 형태이고, 이 세션에 두 번째다.

### 실 구동 (로컬 노드 · 38초 · 도메인 7)

    보호된 3 도메인   🔴 **LLM 0** — 비용 가드가 실 데이터에서 막았다
    빈 4 도메인       서술 7건 생성 (AlphaCore L3 · MissionRuntime L1·L3 ·
                      MissionSystemTools L3 · ProjectAlpha_UiView L1·L2·L3)
    🔴 환각 제거       MissionSystemTools 에서 **멤버 없는 레이어 2건 버림** — 원전 가드가 일했다
    보존 검증         백업과 본문 대조: 보호 7건 **전부 동일**
    소비 확인         실 데이터로 MCP 왕복 · `protected` 플래그가 응답에 실린다

⚠️ **서술은 BM25 도메인 색인에 안 실린다 — 원전도 안 싣는다**(원전 `domain_index` 히트 0).
원전은 **전용 라우트·도구**로 읽고, 리포트 14 §7.5 가 그 라우트를 「원전에만 있는 것」으로
표시해 뒀다. 그래서 이번에 MCP 창구를 같이 넣었다 — **없으면 만들어 둔 서술을 아무도 못 읽는다.**

⚠️ **중 3.1 은 이것으로 7/8 이다** — `#189` 가 남고 그 잔여 후보(`edit_domain_field`)는
**한글 베이스 머지 후 결정**으로 보류돼 있다(`#194` 머지 지점 7). 🔴 세션 중에 내가 이 칸을
*"중 3.1 마지막"* 이라고 말했는데 **부정확했다**(리포트 16 의 표현을 그대로 옮긴 것이다).

테스트 `test_descriptions.py` 47/47 신규 + MCP 계약 29→30종 · 전체 **2743/2743 · 실패 0**.

## §15 남은 두 도메인 재합성 (사용자 승인) — 🔴 **이 절도 실행 전에 썼다**

    대상       GlobalEventSystem(stale 5/9) · MissionEditor(3/13) — 둘 다 full
               (도메인 MD 가 바뀌어 md_hash 불일치)
    백업       ontology-before-refresh2.tgz (60KB)
    노드       bc250 = 35B 상주 ✅ · rtx3060 = 14b 상주 ✅ · inflight 0
    규모       멤버 9→2조각 · 13→3조각 · 프롬프트 101,108자 · **요청 10건**
    기대       ① 항목 재합성(잠금 44건 보존) ② md_hash 스탬프 ③ settle → stale 0
               ④ 🔴 **서술은 LLM 0** — 두 도메인 다 `protected` 다 ⑤ 끝에 색인 동기 1회
    되돌리기   tar xzf …/ontology-before-refresh2.tgz -C ~/ax-cluster/ModularStage

### §15.1 실행 기록

    (아래에 실측을 적는다)

---

## §13 세션 마감 (2026-08-15)

    M4          36/78 → **40/80** (분모 +2 — `#195`·`#196`, 둘 다 **읽고 재다** 나왔다)
    닫은 것      `#153` 소 3.2.2 · `#195` 소 3.2.3 · `#196` 소 3.2.4 · `#187` 소 3.1.4
    재범위       `#152`(태그 0건) — 🔴 닫지 않는다
    커밋         6개 (6dde4d4 · 6fd4d82 · 752b820 · bcf7cba · 3ee68b8 · 316a804)
                 · 세 클론 `316a804` 동기
    테스트       2626 → **2743/2743 · 실패 0** (스위트 ~7분 → **28초**)
    유닛         7종 전부 active · 색인기 정상
    실 변경      🔴 트윈 — `MissionRuntime` actions 12→27 · invariants 10→30 · 워터마크 3건 ·
                 레이어 서술 **7건 생성** · 원전 서술 **7건에 protected 표시**.
                 백업 둘: `ontology-before-refresh.tgz` · `ontology-before-describe.tgz`
                 (🔴 트윈은 git 밖이라 그것이 유일한 되돌리기다)

### 🔴 이 세션의 성격 — **한 칸을 옮길 때마다 그 아래 칸이 드러났다**

| 옮긴 것 | 그것이 드러낸 것 |
|---|---|
| 부분 무효화(`#153`) | manifest `md_hash` 를 **아무도 안 찍는다** → 판정기가 영원히 full |
| md_hash 스탬프 | `package.write(prune=False)` 가 **manifest 를 잃고 있었다** |
| 워터마크 settle(`#195`) | `_stored` 가 DB(0행)만 봐서 **실 리비전 16개를 안 읽었다** |
| 실 트윈 실증(§11) | 재합성이 **검색 색인을 안 깨운다**(`#196`) · 내 테스트가 **실 노드를 두드렸다** |
| 서술 이식(`#187`) | 트윈에 **원전 서술 7건이 미잠금으로** 있었다 — 돌렸으면 덮었다 |

⚠️ 지난 세션의 형태(*"기제는 옮기고 신호원·구성요소는 우리 것"*)가 이번에도 **셋** 나왔다 —
`md_hash`(우리 파서) · `stored`(DB→yaml SSOT) · 색인 지문(전역→디스크). 🔴 그리고 새로 하나:
**「원전의 이상해 보이는 선택」에는 대개 수렴 논거가 있다**(`-1` 스탬프).

### 🔴 내가 틀렸다가 잡힌 것 넷

    ① `-1` 스탬프를 「퇴행」으로 막았다      원전은 무조건 찍는다 — 안 찍으면 5건이 영원히 stale
    ② 「protected 를 다시 써야 한다」        DB 만 보면 될 줄 알았는데 그 표가 0행이라 안 됐고,
                                            결국 **필드 하나만** 갱신하는 쪽이 맞았다
    ③ 내 테스트가 실 노드를 두드렸다         `dry_run` 은 쓰기만 막는다 — 360초를 먹고 있었다
    ④ `find -printf` 문자열 정렬             `22:13` > `02:1x` 라 최신 파일을 못 찾았다(즉시 정정)
    ⑤ 서술 이식이 `verified_by_user` 만 봤다  우리 `protected` 를 빠뜨려 원전 산출물 7건을
                                             덮을 뻔했다 (`--show` 실측이 착수 전에 잡았다)
    ⑥ "`#187` 은 중 3.1 마지막 칸"            🔴 `#189` 도 열려 있다 — 닫으면 **7/8** 이다

🔴 **①②는 원전을 다시 읽어서, ③④는 수치가 이상해서 잡혔다.** 둘 다 *"결과가 극단적이면
도구를 먼저 의심하라"* 의 사례다.

### ✅ 이 세션에 지킨 규약

    순서는 사용자가 정한다        후보의 **입력 실재·비용을 재서** 내놓고 골라 받았다 (세 번)
    커밋은 승인 후                6번 전부 승인받고 실행
    실 트윈 변경은 승인 후        재합성 1회 · 서술 생성 1회 · 스냅샷 표시 — 계획을 **실행 전에**
                                 적고, **매번 백업 후** 실행 (트윈은 git 밖이다)
    애매하면 묻는다               스냅샷 서술 처리는 **셋 중 고르게** 물었다(덮으면 되돌릴 수 없다)
    보존은 세지 않고 잰다          백업과 바이트 대조 — 항목 22건 · 서술 7건 · 사라짐 0
    개선은 기록만                 배치화·`_archive` 처리 등은 손대지 않았다

### 🔴 다음 세션이 먼저 볼 것

    ①  부분 갱신이 **「변경분만」 도는 것은 아직 못 봤다** — 다음 실 소스 커밋이 그 실증이다
    ②  stale 두 도메인(`GlobalEventSystem` 5/9 · `MissionEditor` 3/13)은 안 돌렸다.
        각 152초 규모이고 순서는 사용자가 정한다
    ③  중 3.3 생산자 복각 여부 — 넷이 입력 0건으로 **그룹째** 막혀 있다 (가장 크게 푸는 결정)
    ④  `#189`·`#194` 는 **한글 베이스 머지 후** 결정 · `#141` 2단계는 결정 하나
