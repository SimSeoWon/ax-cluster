> Part of the AX Cluster plan. Index: [`../../PLAN.md`](../../PLAN.md).

# 마일스톤 4 — 원전 대조·재이식 (2026-08-13 착수)

> 🔴 **이 마일스톤은 새 기능이 아니라 정합성이다.** 원전(`~/AgentTest`, 돌아가는 시스템)을 참조하지
> 않고 만든 것을 대조해 좁혀진 요구를 되돌린다. 근거 전부 = **리포트 14**
> [`../../reports/14-origin-citation-audit.md`](../../reports/14-origin-citation-audit.md).
>
> 🔴 **마일스톤 3 은 `locked`(진행 정지)다.** 열린 9건은 M3 에 남아 있고 노트가 붙어 있다 —
> 거기서 진행하지 않는다.

## 🔴 최우선 규칙 — **리눅스 적용이 1번, 개선은 그 뒤** (사용자 2026-08-13)

> *"일단 리눅스 환경에 적용하는 것이 1번이야. 개선은 그 뒤라는걸 명심하면 좋겠어"*

이 규칙에는 따름정리가 하나 있고, 그것이 이 저장소의 다른 규칙과 **부딪히는 것처럼 보인다**:

    🔴 원전에 이미 있는 장치는 **재지 않고 옮긴다.**
       *"실사용 문제인지 먼저 측정한다(추측 금지)"* 는 **새로 만들 때**의 규칙이고,
       원전은 이미 값을 치렀다. **검증은 이식 후에 한다.**

⚠️ **그 구분을 처음에 틀렸다** — 소 3.5.1(`#164`)을 *"실사용 문제인지 먼저 측정"* 으로 적어 놨다가
정정했다. `#104`(BM25 원자성)도 M3 에 같은 모양으로 적혀 있는데 원전에 `search_log_analyzer.py`
696줄이 **있다** — 재기 전에 이식이 먼저다.

🔴 **이식 목록에 개선을 섞지 않는다.** 원전이 **강등·폐기**한 것을 되살리는 것도 개선이다.
분류된 개선 4건은 M4 에 남아 있지만 **대 1~3 이 끝나기 전에 진행하지 않는다**(각 이슈에 노트):

    #146  /debate 되살리기      원전 C.5 가 「전제 미충족」으로 강등해 뒀다
    #169  log 태깅 (η.12.3)     비개발 직군 확장 계열 = 마일스톤 밖
    #176  σ 감사 1회 실행       방법 이식(#175)은 포팅, 실행은 개선 활동
    #178  census 규칙 제도화    원전에 없는 우리 것 신설

## 🔴 이 작업의 정체 — OS·환경 변경이지 재설계가 아니다 (사용자 2026-08-13)

    원전   윈도우 + UE5 설치된 인프라 PC 에서 돌던 서버 데몬
    우리   리눅스 + Gitea 환경으로 **OS·환경만** 옮긴다

**원전 인프라 PC 1대** = `watch.exe`(RAG) + `task_queue` 데몬 + `auto_cleanup` +
🔴 **1차 통합 빌드 게이트** (`README.md:380`). **워커 PC N대** = `worker.exe` 데몬이
claim → base_commit fetch → `worker/<host>/<task_id>` checkout → `claude -p` → 디스크 검증 →
`git push --force-with-lease` → `submit_result` (`distributed-workers.md:31`).

🔴 **리눅스가 강제하는 변경은 「1차 통합 빌드 게이트」 한 스텝뿐이다.** UE5 가 없으니 그것만
`.2` 로 원격 위임한다. 나머지는 리눅스에서 그대로 돈다 — 실제로 지금 돌고 있다.

⚠️ **Gitea 조차 강제된 변경이 아니다.** 원전 `distributed-workers.md:351–357` 이 브랜치 보호 락을
**GitHub / Gitea / 베어 git** 셋으로 적고 *"현재 팀 환경에 맞는 방식은 구현 시점에 확정"* 이라
남겨 뒀다. 우리는 그 칸을 골랐을 뿐이다.

## 🔴 왜 이 마일스톤이 생겼나 — 셋이 아니라 패턴이었다

리포트 13 §18 이 잃은 것 셋을 적었다(웹 UI 를 로컬 파일로 · 읽기 전용 화면에 로그인 ·
온톨로지 뷰어를 229줄로 새로 씀). **그 셋은 사용자가 눈으로 본 화면에서 드러난 것이고, 화면이
없는 자리에서는 드러나지 않았을 뿐이다.**

인용 census(원전 170파일 × 우리 코퍼스 54,356줄) 실측: **인용 0건 74파일 23,710줄.**
⚠️ **그 숫자는 상한이지 손실량이 아니다** — 지표가 양방향으로 틀린다(리포트 14 §1·§7.1):

    인용됨 ≠ 읽음    원전 docs 8종은 전부 인용됐다. 그 인용이 "안 읽었다"는 문장이었다
    인용 0 ≠ 미이식  task_queue 는 파일명 9개가 동명으로 이식돼 있었다 (census 가 못 봤다)

## 🔴 가장 큰 발견 — 「쓰는 주체는 하나」는 발명이 아니었다

`mcp/master_orchestrator/cluster_coordinator.py` **200줄**, 우리 저장소 인용 **0회**:

    assign_attempt()     서버가 브랜치명을 배정 (워커는 구성 안 함)
    push_attempt()       워커가 ephemeral attempt 브랜치에 commit·push
    verify_and_merge()   🔴 **서버**가 검증 후 durable 에 merge·push
                         주석 원문: **"durable 단일 writer 보존"**
                         epoch 게이트: submit_epoch < current_epoch → zombie 거부
    cleanup_attempts()   ephemeral 일괄 제거 · durable 보존
    fake_worker()        worker_fn 주입 — 원격 PC·LLM 없이 드라이런

    원전       워커 → ephemeral attempt push  ·  **서버**가 verify → durable merge
    내 재설계  워커 → 텍스트 응답만            ·  통합자 `.2` 가 적용·빌드·커밋

같은 문장을 쓰면서 다른 것을 했다. **원전에서 워커는 쓴다 — ephemeral 브랜치에만 쓴다.**
원칙을 `.2` 로 옮기면서 **attempt 계층 · epoch fencing · fake 워커 주입까지 같이 버렸다.**

🔴 그리고 원전엔 **사용자 확정 설계 문서**가 있다 — `local_llm_distributed_workers_plan_v5.0.md`
463줄, 인용 0회. C.1 2-tier(사용자 2026-06-13 확정) · C.2 pull→push 서버분배(확정) ·
C.3 Git-as-feedback-bus(핵심 통찰) · C.4 인터페이스 동결(확정) · **C.7 리더 최종 통합·빌드
(토폴로지 확정 2026-06-21)** · κ.0 2층 구조.

⚠️ **C.7 이 우리 「통합자」의 원본이다** — 통합자 개념 자체는 원전에 있었다. 내가 없앤 것은 그
앞단이다. ⚠️ 그리고 **C.5 가 `/debate` 를 「전제 미충족」으로 강등**해 뒀다 — `#98` 은 그 강등을
모르고 되살리려는 칸이었다.

## 🔴 Flow Y 확정 (사용자 2026-08-14) — 소 1.1.1 정독이 갈림길을 드러냈다

원전은 이 갈림길을 **미확정으로 남겨 뒀다** (κ 미결정 사항, *"추론 요청 주체 (κ.2 핵심 갈림) …
**확정 필요**"*). 그리고 스스로 *"2층 물리 분리 강화는 **Flow Y** 를 가리킴"* 이라 적어 뒀다.

    Flow X  ②워커가 직접 노드에 추론 요청 (현 worker.py content-return)
    Flow Y  🔴 **마스터가 브로커** — git 정본서 소스 읽어 노드에 추론 요청 → **받은 diff 를
            task/attempt 브랜치에 commit** · ②는 pull → UE 컴파일/RunTests → 보고    ← **채택**

    ① 마스터 (.57)   판단·상태 전부 · 소스 조립 · 추론 라우팅 · **커밋**
    ② 워커 (윈도우)  pull → UE 빌드/RunTests → 보고. 🔴 **판단 안 함 · stateless**
    노드 (BC-250)    추론만 — 텍스트 in/out, 파일 없음
    요청자 (.33)     등재 · 리뷰 · `[FEEDBACK]`

🔴 **Flow X·Y 공통이라 방향과 무관하게 이식할 것** — 2-tier 브랜치 · **epoch fencing** ·
`cleanup_attempts` · `fake_worker` 주입 · reject 시 **누적**(전진 + `[FEEDBACK]` 1커밋).
갈리는 것은 **소 1.2.2(누가 attempt 에 push 하나)** 하나였고, 이제 답은 **마스터**다.

🔴 **빌드 게이트는 SSH 호출이 아니라 큐 잡이다** (κ.0): *"task_queue 잡으로 큐잉 → UE5 있는
윈도우 워커가 claim 하는 **CI 러너 패턴**"*. 소 1.3.1 을 그 형태로 재서술했다.

⚠️ **그리고 `.2` 통합자라는 특수 역할을 재검토해야 한다** — 원전 세션이 *"검증 전용 노드"* 를
발명·배선했다가 **전량 revert** 했다(*"워커는 homogeneous"*). 같은 부류일 수 있다.

## 🔴 커밋 권한 (사용자 2026-08-14)

    커밋·푸시   자율 실행 금지 → **매번 허락을 요구하고 승인 후 실행**
    절대 금지   `push --force` · `reset --hard` · 브랜치/태그 삭제 · `rebase` · 히스토리 재작성
                (승인이 있어도 안 한다 — 명령만 적어 준다)

원전은 더 강하다(*"사용자가 시켜도 금지, 묻지도 말 것"*). 🔴 **우리가 느슨한 것은 환경 때문이고
사용자가 직접 정했다** — 원전의 윈도우 박스엔 소스트리가 있고 sim-desktop 엔 없다.
⚠️ **파이프라인 코드가 커밋하는 것은 이 규칙과 무관하다**(통합자·워커의 설계된 동작).

## 작업 분류 — 대 4 · 중 17 · 소 45

🔴 **분자도 분모도 여기 적지 않는다**(version *마일스톤 4* 가 센다). **소분류가 작업 단위다.**

| 대 | 중 | 이슈 |
|---|---|---|
| **대 1 토폴로지 원복** | 1.1 원전 설계 문서 정독 · 1.2 2-tier 복원 · 1.3 빌드 게이트만 `.2` 위임 · 1.4 문서·라벨 정정 | `#117` `#121` `#127` `#130` |
| **대 2 열린 4건을 이식으로 전환** | 2.1 /review-work · 2.2 작업유형 템플릿 · 2.3 /cluster-selftest · 2.4 /tdd-dryrun 이식 + **/debate 재논의** | `#135` `#138` `#141` `#144` |
| **대 3 미이식 자산 재검토·이식** | 3.1 ①추정 이식분 실물 대조 · 3.2 결정적 온톨로지 장치 · 3.3 되먹임 7종 · 3.4 감사·복구·튜닝 · 3.5 운영 가드 · 3.6 나머지+개명 cascade | `#147` `#151` `#154` `#159` `#163` `#166` |
| **대 4 방법론·규범 회수** | 4.1 `AGENTS.md` 5조 · 4.2 silent failure 감사 · 4.3 원전 필독 목록 제도화 | `#172` `#174` `#177` |

### 🔴 여기서 다시 걸릴 것 (함정 목록)

- 🔴 **1.1 이 상류다.** 설계 문서를 안 읽고 1.2 를 하면 같은 실수를 반복한다. 이 마일스톤이
  존재하는 이유가 그것이다
- 🔴 **`#115` *"실패는 커밋하지 않는다"* 는 `8-git-authority.md` §8.4 *"실패도 커밋한다"* 와 이미
  모순이다.** 원전 기준으로는 §8.4 가 맞다 — ephemeral 이니까
- 🔴 **`infer.py`·`integrate.py` 를 지우지 않는다.** §8.4 규약(*"측정으로 뒤집힐 여지가 있는 동안
  되돌릴 수 있게"*)이 `runner.py`·`ax-work` 에 적용된 것과 같다. **양쪽을 나란히 둔다**
- 🔴 **소 3.5.1 은 측정이 먼저다.** `watch_state.py` 의 *"분산 작업 활성 검사 = Git 폴링 일시 중지"*
  가 우리에게 없고, `ax-indexer.path` 는 push 마다 깨어나며 통합자는 조각마다 커밋한다 →
  한 work 안에서 색인이 여러 번 돈다. ⚠️ **실사용 문제인지 재고 나서 고친다**(추측 금지)
- 🔴 **`2.4.2` 는 작업이 아니라 질문이다.** `/debate` 를 되살릴지는 사용자 판단이다
- ⚠️ **②(의도적 미이식)는 "안 읽어도 된다" 가 아니다** — `ax_incubating_workflow_plan_v1.0.md`
  320줄은 Redmine·Wiki 계층 매핑과 계측 4필드를 담아 **우리 레드마인 규약과 충돌할 수 있다**
- ⚠️ **404 는 두 가지다** — 라이브 감사 중 *"라우트 미등록"* 과 *"자원 없음"* 을 응답 **본문**까지
  읽어서야 갈랐고, 그 전에 오탐을 두 번 냈다(1세그먼트 경로 · 존재하지 않는 액션 이름)

## 사슬

    1.1 → 1.2 → 1.3        🔴 설계 정독이 상류. 게이트는 merge 안의 **한 스텝**이다
    1.2.5 → 2.3.2          fake_worker 드라이런이 /cluster-selftest 의 전제
    1.2 → 1.4              복원한 것으로 문서·라벨을 정정한다 (거꾸로 하면 또 어긋난다)
    대 3 · 대 4            사슬 밖 — 병행 가능. 🔴 **4.3(필독 목록 제도화)은 재발 방지라 늦출수록 비싸다**

## 진행 규칙

마일스톤 1~3 의 규칙을 그대로 쓴다 — 분자는 레드마인이 센다 · 조사 없이 라벨 붙이지 않는다 ·
줄 수는 작업량이 아니다 · **순서는 사용자가 정한다** · **애매하면 묻는다**.

**이번에만 다른 것 셋:**

- 🔴 **리눅스 적용이 1번, 개선은 그 뒤** — 맨 위 절이 이 마일스톤의 최우선 규칙이다.
  원전에 있는 것은 재지 않고 옮기고, 검증은 이식 후에 한다
- 🔴 **만들기 전에 원전을 읽고, 읽었다는 것을 인용으로 남긴다.** 이 마일스톤은 그 규칙이 없어서
  생겼다. 인용 census 가 재현 가능한 절차로 남는다(소 4.3.1 — 🔴 그 칸 자체는 **개선**이다)
- 🔴 **되돌리는 것이 목적이므로 지우지 않는다.** 두 토폴로지를 나란히 두고 측정으로 고른다
