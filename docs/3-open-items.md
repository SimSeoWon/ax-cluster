> Part of the AX Cluster plan. Index: [`../PLAN.md`](../PLAN.md).
> Section numbers are unchanged by the split — cross-references from other documents still resolve.

## 3. 미정/다음 단계

[중요] **이 문서는 「무엇으로 할지 안 정한 것」만 담는다** — *"아직 안 한 것"* 은 열린 마일스톤에
있다([`milestones/6-improvements.md`](milestones/6-improvements.md)). 섞이면 어느 쪽도
신뢰할 수 없다.

[주의] **아래 여섯 항목은 마일스톤 3 의 작업 칸이 됐고, 그 마일스톤은 2026-08-17 에 닫혔다**
(2026-08-23 갱신). 여기 남긴 것은 *결정 근거와 실측*이고, **진행 상태는 레드마인에서 읽는다**:
`§4.5 대규모 모듈 작업 세부`→중 1.2·1.3 · `층2 검증 모델 선정`→소 2.2.1(`#85`) ·
`BM25 원자성`→소 3.3.1(`#104`) · `클러스터 상태 웹 UI`→중 3.2 · `tool-calling 모델`→소 3.3.3(`#106`) ·
`BC-250 장시간 안정성`→소 3.3.2(`#105`). 뒤의 넷은 M3 마감 때 **M6 으로 이관돼 거기서 닫혔다**.

- [x] ~~마스터(192.168.0.57)↔BC-250 #1 실제 네트워크 stateless 호출 테스트~~ — **완료 2026-08-06**
      (`/api/tags` + `/api/generate` 정상, 60.6 t/s). 상세 = 리포트 24 §6
- [x] ~~BC-250 #1의 TTM/GTT 캡 상향~~ — **완료 2026-08-06**. GTT 3.73 → **15.10 GiB**,
      Ollama 인식 11.7 → **15.6 GiB**. 14B(8.9GB)·35B-A3B(13GB) 모두 100% GPU 적재 확인. 리포트 24 §3·§4
- [x] ~~추론 요청 주체(워커 직접 vs 마스터 브로커)~~ — **마스터 브로커로 확정 2026-08-06** (아래 §4)
- [~] **BC-250 #1 안정성 검증** — **단기(30분/65사이클) 통과 2026-08-06**: 성공률 100%,
      tok/s 26.57±0.27(1분기↔4분기 변화 0.00%), 커널 오류 0, GTT 7.89GB 고정(GPU 누수 없음).
      리서치 보고서가 경고한 고빈도 크래시 유형은 재현되지 않음. 상세 = 리포트 24 부록 B.
      **⬜ 장시간은 여전히 미검증** — 30분은 long-tail 을 보지 못한다. 특히 시스템 `MemAvailable` 이
      65사이클 동안 약 -620MB 단조 감소(부하 종료 후 소폭 회복)한 것이 미해결 관찰이라,
      무인 24시간 판단 전 수 시간 이상 재실행 필요.
      [중요] **그 「미해결 관찰」이 2026-08-15 에 현실이 됐다** — 온톨로지 배치 도중 OOM(03:04) →
      저널 정지(03:11) → **보드 자체 재부팅**(03:22) → SSH 배너 정지 ~10분. 재실측:
      요청마다 `available` 이 **약 60MB씩** 줄고 안 돌아오며(작은 요청 기준), 35B 를 8192 로
      상주시키면 여유가 **138~300MB** 뿐이다. 그것을 막으라고 있는 `UNLOAD_EVERY` 는
      [중요] **한 번도 안 돈다**(카운터가 레인 호출마다 리셋 — 실측 요청 14회·회수 0회).
      [주의] **단기 통과가 장기 안전을 뜻하지 않는다는 것이 값으로 확인됐다.**
      [중요] **그래서 열린 질문이 하나 커졌다 — 이 보드가 35B 를 계속 상주해야 하는가**(사용자 결정).
      상세 = 리포트 17 §16 · `machines/bc250-1.md`.
- [x] ~~**코드 생성 모델 후보 재평가 — `devstral:24b`**~~ — **탈락 확정 2026-08-08 (§9.4.1).**
      보드에 적재해 실측했다. tool-calling 관문에서 **3/4** — 프롬프트에 형식 지시가 붙으면
      도구 호출을 빠뜨리고 "하겠다"고 말만 하고 멈춘다(`temperature:0` 재현). 코드 품질은
      **측정하지 않았다** — 첫 관문에서 걸렸다. 아래 원 항목의 우려(에이전틱 특화가 우리 형태와
      어긋남)와 다른 이유로 걸린 셈이다. 참고용 원문:
> [중요] **마일스톤 2 의 작업 항목은 여기 두지 않는다** —
> [`milestones/2-twin-restoration.md`](milestones/2-twin-restoration.md) 에 있다.
> 이 문서는 **설계상 미결**(무엇으로 할지 안 정한 것)만 다룬다. 진행 항목(아직 안 한 것)과
> 섞이면 어느 쪽도 신뢰할 수 없게 된다.

- [ ] ~~(원 검토 내용)~~ `devstral:24b`(Q4_K_M 14GB, SWE-Bench Verified 46.8%, 128K ctx)가
      15.6GiB 에 들어가는 유일한 신규 후보. 단 ① SWE-Bench 는 대부분 Python 저장소라 UE5 C++·매크로/
      리플렉션 체계로의 전이가 불확실 ② Devstral 은 **에이전틱 특화**인데 우리 파이프라인은 골조의
      `[PSEUDO]` 본문만 채우는 제약 생성이라 강점이 발휘될 형태가 아님 ③ 14GB 라 여유가 1.6GB 뿐이라
      컨텍스트 확장 여지가 거의 없음(현 14B 는 6.6GB 여유).
      → 추측 대신 **`bench_quality2.py` UE5 6종 하네스로 동일 잣대 측정**할 것.
      참고: `qwen3-coder` 는 공식 태그 최소 19GB(30B-A3B Q4_K_M)라 **적재 불가**. 7B/14B 밀집 모델이
      존재하지 않으며, 서드파티 저양자화는 오늘 35B-A3B IQ2 에서 확인된 API 환각 리스크가 있어 비권장.
- [x] ~~추론 노드 장애 감지 방식~~ — **스트리밍 하트비트 + 마스터측 Python 래퍼 확정 2026-08-06** (아래 §6)
- [x] ~~원격 전원 재투입 수단(스마트 플러그/PDU)~~ — **확보 완료 2026-08-07** (§6.6).
      스마트 콘센트 도입 + BIOS `Restore on AC Power Loss`=`Power On` 실증(콘센트 재공급만으로 자동 부팅).
      **끄기·켜기 양방향 모두 원격 가능** — GPU 크래시 시 복구 경로가 열렸다
- [ ] **마스터 GPU 증설(GTX 1070 Ti)** — RAG 임베딩/리랭킹/LoRA 용 CUDA 확보 목적. 선결: PSU 용량 확인 + 드라이버 설치 (아래 §7)
- [~] [중요] **BC-250 #2 — 현재 Bazzite 게임 머신. #1 완성 후 추론 노드로 전환 의향**
      (사용자 확정 2026-08-08). **"미구성"도 "도착 대기"도 아니다 — 다른 용도로 가동 중이다.**
      [주의] 이 항목은 세션 중 두 번 잘못 적혔다(→"미구성, #1 완성 후 복제" →"주문 후 도착 대기").
      **최종 사실은 이것이다.**

      **성격이 바뀐다 — 셋업 작업이 아니라 "게임 머신을 내주는" 자원 배분 결정이다.**
      의향은 확인됐으나 확정은 아니므로, 계획은 **#2 를 조건부로만** 다룬다:
      가용성을 전제로 설계하지 않되, 붙었을 때 바로 쓰도록 경로는 열어둔다.

      **선결(#1 완성) 잔여:** 장시간 안정성 재검증 · §9.4.3 경로 착수 · `task_queue` 이식.

      [주의] **전환 시 확인할 기술 리스크** — 셋업 가이드
      (`sim@192.168.0.43:~/bc250-backup-staging/reports/18-second-board-setup-guide.md`)는
      **일반 Fedora 를 전제**할 가능성이 높은데 **Bazzite 는 rpm-ostree 기반 이미지형(immutable)**이다.
      #1 에서 필수였던 **TTM/GTT 캡 상향·커널 부팅 파라미터·헤드리스(multi-user.target) 전환**이
      이미지형 시스템에서 같은 절차로 되는지 **미확인**. 재설치(일반 Fedora)가 더 쌀 수 있다 — 전환 착수 전 판단할 것

      **#2 도착 전까지 성립하지 않는 것들 — 계획에서 전제로 쓰지 말 것:**
      | 항목 | 상태 |
      |---|---|
      | §4.5 "노드 수만큼 병렬" | [실패] 보드 1대 = 직렬. 클래스 단위 분할의 이득은 **충돌 방지**뿐 |
      | §6.5 추론 노드 페일오버 | [실패] 재할당할 곳이 없다. 감지는 되지만 복구 불가 |
      | §4.4 "모델을 보드별로 고정 배정" | [실패] `MAX_LOADED_MODELS=1` 그대로 |
      | §9.4.2 35B=드라이버 / 14b=코드생성기 | [완료] **해제 2026-08-08** — 2번째 엔드포인트가 보드가 아니라 **RTX 3060** 이었다. 왕복 ~100초 → **3.6초** |

      **→ "보드 1대, 모델 1개 상주"는 더 이상 전제가 아니다 (2026-08-08).** 추론 엔드포인트는
      **2개**다 — BC-250(35B 상주) + 보조 윈도우 PC 의 RTX 3060(14b 상주). 위 표의 나머지
      항목(§4.5 병렬·§6.5 페일오버)도 부분적으로 다시 봐야 한다: 14b 는 양쪽에 다 있어
      **폴백이 실재**하지만, 35B 는 3060 에 안 들어가 여전히 단일 장애점이다.
- [x] ~~마스터가 git 변경을 어떻게 감지하나~~ — **Gitea 이벤트 구동으로 확정 2026-08-07** (§5.4.2).
      Gitea 를 마스터가 직접 호스팅하므로 폴링할 이유가 없다. `watch.py` 의 `while True` 폴링 루프는
      이식 대상이 아니다. 상주 프로세스 자체는 여전히 필요(웹훅 수신·task_queue HTTP 서버·RAG).
- [x] ~~`process_lifecycle.py`·`network_firewall.py` 처리~~ — **폐기 확정 2026-08-07** (§5.4.3).
      리눅스 등가물로 고쳐 쓰지 않고 systemd 에 넘긴다.
- [x] ~~**마스터 실행 모델 — systemd vs Docker Compose**~~ — **systemd + venv 확정 2026-08-08** (§5.4.4).
      Compose 선호 규약의 **의도적 예외**다: 워크로드가 호스트 git·SSH 키·`agy`/`claude` CLI 의
      홈 디렉터리 인증·Gitea 훅에 묶여 있어 컨테이너에 넣으면 전부 다시 마운트해야 한다.
      서비스 3개가 그 형태로 가동 중이다
- [x] ~~오케스트레이션 주체·워커 풀 배치~~ — **확정 2026-08-07** (§2.0~§2.4).
      **오케스트레이터 2곳**(윈도우 Claude Code + 마스터), **마스터는 인프라지 작업장이 아니다**
      (파일 소유·빌드·코드 등록은 윈도우), **BC-250 은 추론 연산만 인계**.
      전제도 명시: AgentTest=팀 인프라 / AX=1인이 집의 여러 컴퓨터를 묶는 것
- [x] ~~**대규모 모듈 작업 세부** (§4.5)~~ — **답이 났다 (마일스톤 3 중 1.2·1.3·1.4, 닫힘)**:
      적용 순서는 **의존 순서**(분해가 파일 불가분까지 자른다) · 부분 실패 단위는 **조각**
      (조각마다 UE5 빌드 → 통과분만 커밋) · 적용·빌드·커밋의 주체는 워커가 아니라 **통합자**
      (중 1.3 = 추론 파견만). 아래는 그 결정 전의 근거다 — 적용 순서(의존 순서 vs 임의) · 부분 실패 처리 단위 ·
      적용 풀 구현(윈도우 측, task_queue 와 별개) · 분할 입도(현행 `/distribute` 는 8~20파일 클래스 패밀리)
- [x] ~~**능력 기반 라우팅** (`requires: ue5`)~~ — **구현·실측 완료 2026-08-08** (§5.2-C).
      task 의 `requires` ⊆ 워커가 claim 시 신고한 `capabilities` 일 때만 배정하는 **부분집합
      매칭, fail-closed**. 능력 미신고 워커(=구 워커)는 `requires` 빈 task 만 집는다.
      write·verify 잡 **양쪽 모두** 적용. 미지 태그는 **등록 시점에 거부**(오타 → 영구 기아 방지).
      실측: 마스터 워커는 우선순위 10 인 `ue5` 잡을 건너뛰고 우선순위 1 인 일반잡을 집었고,
      남은 `ue5` 잡에 대해 204 를 받았으며, `capabilities:["ue5"]` 워커가 그것을 집었다
- [x] ~~**마스터 선결 설치**~~ — `agy` [완료] (v1.1.10, 리포트 04) · `claude` [완료] ·
      ~~`bun`/`node`~~ → **마스터엔 불필요해졌다 (2026-08-08).** gjc 는 loopback 제약상
      파일을 소유한 윈도우 PC 에서 돌기 때문(§9). **`bun`/`node` 는 윈도우 PC 의 선결 조건으로 이동**
- [x] ~~**층2 검증 모델 선정**~~ — **실측으로 확정 (소 2.2.1 = `#85` 완료)**: 표본 7개(전부 이
      프로젝트에서 실제로 났던 실패)에서 `agy`·`claude:sonnet`·`claude:opus` 가 **검출 4/5 · 오탐 0
      동점** → 싼 쪽부터 **`agy` → `claude` 체인**(코드의 기존 기본값이 이제 실측 뒷받침을 받는다).
      14b 는 3/5, **35B 는 오탐 2 로 탈락** — 정상 코드를 막는 게이트가 더 나쁘다. 그리고 차이를
      만든 것은 모델이 아니라 **grounding** 이었다. 아래는 그 판정 전의 후보 목록이다 — `agy models` 목록(gemini-3.6-flash 계열 · claude-sonnet-4-6 ·
      gpt-oss-120b 등) 중 UE5 환각 API 검출률로 결정. 리포트 02 의 `bench_quality2.py` 하네스 재활용 가능
- [x] ~~git 조작 권한 범위~~ — **확정 2026-08-07** (§8). 사람 세션은 되돌리기 어려운 것만 금지
      (`push --force`·`reset --hard`·브랜치 삭제·`rebase`), 파이프라인 워커는 작업 브랜치 자율 +
      `main` 직접 push 금지. **완료 신호를 믿지 말고 산출물을 검증할 것**(§8.3)
- [~] **팀 전용 컴포넌트 선별** (§5.2-D) — **1인 + N워커 구도로 판단이 내려졌다 (2026-08-09)**:
      | | |
      |---|---|
      | 작성자별 분리 (`reviews/<작성자>/`) | [중요] **덜어낸다** — 미는 주체가 하나다 |
      | durable merge CAS · 모델이 머지 권한 (`verify_worker.py` 314줄) | [중요] **불필요해진다** — durable 이 오래 살고 머지는 끝에 사람이 한 번(§8.4). 재설계가 아니라 삭제 대상 |
      | attempt 브랜치 격리 · lease · epoch fencing | **그대로 필요** — 워커 머신이 2대라 충돌은 실재 |
      | Redmine | [완료] **채택** — 작업 항목 추적에 쓴다(`docs/10-references.md` §10.1) |
      | `/review-work` · AgentWiki · zip 패키징 | ⬜ 아직 미판단 |
- [ ] **`mcp/local_llm_runner/` 처리** — §6.4 추론 래퍼와 역할 중복. 재사용/대체 결정 후 래퍼 착수
- [x] ~~**이벤트 큐 설계**~~ — **완료 2026-08-08** (§5.4.5-a). 디렉토리 스풀
      (파일 1개 = 이벤트 1개, 임시→rename). 영속 · `flock` 단일 소비자 ·
      `project_id` 합치기 · at-least-once · 거부는 `rejected/` 에 보존.
      `master/events/`, 테스트 38건. **`task_queue` 와 분리 유지**.
      [주의] 원형(`_regenerate_queue.jsonl`)의 append 방식은 **채택하지 않았다** —
      소비 시점 경합으로 이벤트가 유실된다
- [x] ~~**이벤트 큐 소비자(색인기)**~~ — **완료·가동 중** (2026-08-08, 확인 2026-08-09).
      `ax-indexer.path`(inotify) → `ax-indexer.service`. 실전 1건 처리 확인:
      `Sim/ModularStage: bc4b38f4→bc4b38f4 · 재색인 961건`. 변경분 → 그래프 → 컨텍스트 합성 →
      재색인이 한 줄로 돈다
- [x] ~~**Gitea `post-receive.d/` 훅 설치**~~ — **완료** (확인 2026-08-09):
      `/var/lib/gitea/gitea-repositories/sim/modularstage.git/hooks/post-receive.d/ax-index` [완료] ·
      `ax-project-id` [완료] · 스풀 4개 디렉토리 `group=gitea` setgid [완료].
      `python -m master.events.install_hook` 로 점검한다 ([주의] `AX_PROJECTS_ROOT` 필요)
- [x] ~~**워크숍 측 체크아웃 등록**~~ — **완료 2026-08-08, 확장 2026-08-09** (§5.5.4).
      스키마에 [중요] **`role`** 이 추가됐다 — `driven`(닿을 수 있나) ≠ `role`(시켜도 되나).
      | 호스트 | driven | role | 경로 |
      |---|---|---|---|
      | `.2` (`janus`) | ssh | `worker` | `E:\trunk\ModularStage` |
      | `.43` (`sim`) | ssh | `worker` | `/home/sim/trunk/ModularStage` (2026-08-09 클론) |
      | `.33` (`user`) | ssh | **`requester`** | `C:\Users\USER\Documents\ModularStage` |
      [중요] `.33` 은 SSH 가 열려 있어도 **`drivable_hosts` 에서 빠진다.** 사람이 편집 중인
      트리에 일감을 파견하지 않는다 (`docs/8-git-authority.md` §8.4)
- [x] ~~**BM25 원자성 확인** (§5.4.6)~~ — **문제가 아니었다 (소 3.3.1 = `#104` 완료, M6 이관분)**.
      걱정의 전제가 **원전 기준**이었고 우리 이식은 세대 이중화(`bm25_a`/`b` + 마커 원자 교체)를
      이미 넣어 뒀다. 재색인 2회와 경합시킨 **62,591 질의에서 오류 0**(리포트 19 §9).
      원래 적어 둔 우려는 이랬다 — 더블 버퍼가 벡터/태그캐시만 덮고 BM25 는 제자리 갱신이다
- [x] ~~기존 "서버 모드"와의 중복도 판정~~ — **완료 2026-08-07** (§5.4.7). 계층별로 갈린다:
      `context_search --serve`·`task_queue` 는 **거의 그대로 이식 가능**(`sys.platform` 분기 0건 / 2건 모두
      리눅스 분기 존재), `watcher/` git 감시는 **재설계**, BC-250 추론 브로커는 **신규**.
      [주의] 서버 모드는 **RAG 전용**이지 중앙 오케스트레이션이 아니다 — git 감시는 여전히 `watch.exe` 폴링
- [x] ~~작업자(윈도우) ↔ 마스터 경로 — AgentTest `task_queue` 를 리눅스 마스터로 이관~~ —
      **완료.** `ax-task-queue`(8101) 가동 중이고, 요청자→워커 한 바퀴가 **1분 29초** 실측됐다
- [x] ~~gajae-code를 이 오케스트레이션에 실제로 연결하는 방법 — WebSocket SDK로 마스터가 외부 컨트롤러 역할~~ —
      [중요] **그 가설은 성립하지 않는다 (2026-08-08).** SDK WebSocket 은 **loopback 전용**
      (`ws://127.0.0.1:<port>/?token=`, discovery 파일 `<repo>/.gjc/state/sdk/<sessionId>.json`, 오토큰 401),
      Coordinator MCP 도 stdio + 로컬 discovery 파일 의존이라 원격 불가(SSH stdio 터널 필요),
      `--mode rpc`/`rpc-ui`/`bridge` 는 **제거됨**. → **gjc 는 파일을 소유한 윈도우 PC 에서 돈다.**
      마스터는 대신 **Ollama API 호환 브로커**로 gjc 의 모델 백엔드가 된다(§9). 상세 = 마스터 리포트 06 §3
- [x] ~~[중요] **에이전틱 executor 용 tool-calling 모델 확보**~~ — **`gemma4:e4b` 로 확정
      (소 3.3.3 = `#106` 완료, M6 이관분 · 사용자 2026-08-17)**: 4관문 4/4 ×3회 + 한글 3/3,
      30사이클 연속에서 `#105` 형 누적 없음(~-5MB/사이클). **이미 `.43` 에 상주 중인 모델이라
      추가 비용 0** 으로 공석이 찼다. 폴백 `qwen3:8b`(`.2`). 실측 표는
      [`../machines/llm-fitness.md`](../machines/llm-fitness.md) 「Tool-calling」 절.
      아래는 그 자리를 비게 만든 실측이다 — `qwen2.5-coder:14b` 는 **구조화된
      `tool_calls` 를 못 낸다**(BC-250 실측 3/3, `temperature:0`: 호출 JSON 이 `content` 에 문자열로 나옴).
      `/api/show` 가 `capabilities:['completion','tools','insert']` 를 광고하는데도 그렇다 — **광고와 실제가 다르다.**
      `35B-A3B IQ2_M` 은 정상 방출. 에이전틱 하네스를 쓰려면 executor 를 14b 로 못 채운다 (§4.4 · §9.3)
- [x] ~~온톨로지 기반 디지털 트윈 설계 — 어떤 데이터를 온톨로지화할지~~ —
      **범위 확정 2026-08-08 (사용자 지시)**: **"RAG 부터 디지털 트윈 전반 모두 소스 부분에
      한정"** (§5.2-E). 대상은 `Source/**`·`Config/**`, **`Content/**`(uasset, 워킹트리의 93%)와
      에셋·레벨·블루프린트 트윈은 범위 밖**이다. 모듈은 **검색 코어 + 온톨로지 둘 다** 이식한다 —
      온톨로지가 곧 소스의 트윈이므로 빼면 트윈이 아니다.
      실측: 확보한 온톨로지 247개가 **이미 전부 소스 파생**(C++ 심볼·코드 모듈)이라 이관 비용 0.
      검색↔온톨로지는 서로 import 0건이라 **단계를 나눌 수 있다** (결합은 라우트 등록부 한 곳).
      [중요] 다만 **"나눌 수 있다" 를 "온톨로지는 나중" 으로 읽으면 안 된다** — 목표 ①이 온톨로지
      문서 자동 작성이다. 줄 수로 단계를 가르지 않는다 (§5.2-E ②).
      [주의] 감사·위키·웹UI 제외는 **이 결정 때문이 아니다** — §5.2-D(팀 전용 선별) 기준으로 별도 판단.
      **격리 규약은 확정됐다 (§5.5, 2026-08-08)**: `ax-cluster/projects.yaml`(추적 목록+`active`) + 프로젝트명 디렉토리(gitignore) · 프로젝트당 하위 디렉토리 · **한 번에 하나만 마운트** ·
      다른 프로젝트 요청은 거부 — [중요] **형태는 `200 + ok:false + 사유`** (§12.5-c 확정
      2026-08-22, 배선 완료). 태그가 **없어도** 쓰는 표면은 거절한다. UE5 프로젝트는 지금 둘
      (ModularStage · NS)이고 **동시에 1개만 마운트한다**
- [x] ~~Unreal MCP(5.8+) 연동 방식 — 윈도우 UE5 PC 쪽에서 어떻게 노출/호출할지~~ —
      **확정 2026-08-07: 노출하지 않는다.** 에디터 내장 MCP 서버는 **loopback 전용이고
      non-loopback `Origin` 헤더를 거부하며 인증 계층이 없다**(Epic 공식: "not safe to expose
      beyond the local machine"). **마스터가 LAN 으로 붙는 것은 불가능** →
      **윈도우 PC 의 Claude Code(오케스트레이터 ①)가 로컬 `127.0.0.1:8000/mcp` 로 붙는다.**
      마스터가 에디터 조작이 필요하면 `requires: ue5` 잡으로 큐잉해 윈도우가 claim(§5.2-C).
      AgentTest 에 이미 배선돼 있다(`unreal_mcp_port` config opt-in, `ue-editor-operator` 에이전트).
      상세 = 마스터 리포트 05.
      [완료] **전제조건(5.8+) 충족 — `sim/modularstage` 가 UE 5.8.1 로 올라갔다 (2026-08-08).**
      브랜치 `upgrade/ue5.8`, 최근 커밋 `MCP 추가` (§5.5.3)
- [x] ~~[중요] **추론 노드에 소스를 실어야 한다 — 매니페스트가 문서 발췌뿐이다**~~ —
      **해소 2026-08-10** (아래 근거는 남긴다: 어느 소비자가 왜 못 읽었는지가 설계 판단이었다)

      두 규칙이 충돌한다: 원본 `rag-system.md` 는 *"모든 판단은 소스 코드 기준, RAG 는
      참조만, 반드시 Read 로 직접 확인"* 인데, 우리 설계는 *"추론 노드는 파일을 만지지
      않는다"*(§2)다. **원본은 소비자가 Claude Code 라 파일을 직접 읽을 수 있어 충돌이
      없다.** 우리 노드는 못 읽으므로 **마스터가 소스를 프롬프트에 넣어야** 한다.

      | | 소스를 보나 |
      |---|---|
      | 컨텍스트 MD 합성 (`context_synth`) | [완료] 본문 4,000자를 넣는다 |
      | **코드 생성** (`work/manifest.py`) | [실패] **문서 발췌만** — 거꾸로다 |

      [중요] 볼 수 없는 노드에게 **지도만 주고 코드를 짜라고** 시키고 있다. 층2가 환각 API 를
      잡아내는 이유의 일부일 수 있다(미확인 — 재볼 것).

      [완료] **소 2.3.1 이 끝나며 실측으로 확인됐다 (2026-08-09)** — 규범을 붙이자 생성 코드가
      실재 멤버 4/4 를 쓰고 invariant 도 지켰지만, `CurrentStep`(실제 `Step`) ·
      `EMissionTaskExecutorState`(실제 `EMissionExecutorState`) 처럼 **한 글자 차이**가 남았다.

      [중요] **그런데 전제가 절반 바뀌었다** — 워커가 소스 클론을 갖게 됐다(2026-08-09).
      | 소비자 | 소스를 읽나 | 상태 |
      |---|---|---|
      | **워커의 Claude** (`get_manifest` → 직접 작업) | [완료] | **해소** — `ax-work` 스킬이 *"헤더를 열어 정확한 선언을 뽑아 모델에 넣어라"* 를 지시 |
      | **`get_code`** (마스터 → 브로커 → 로컬 모델) | [실패] | **그대로** |

      → **재정의**: *"매니페스트에 싣기"* 가 아니라 **"생성 시점에 싣기"**. 매니페스트는
      *1회 수집 → 다회 소비* 되는 저장물이라 소스를 넣으면 **필요 없는 소비자도 매번 비용**을
      낸다. 예산 실측: 현재 생성 프롬프트 1,692tok + 대상 헤더 1,131tok = **2,824tok**
      (`num_ctx` 8192, 여유 5,368tok). [중요] 오늘 환각난 이름이 그 헤더에 **전부 있다**.
      [완료] **해소 2026-08-10 — 레드마인 #26 완료.** `work/declarations.py` 가 생성 시점에
      헤더 선언을 싣는다(클래스당 1,500자·합 6,000자 상한, 잘리면 프롬프트 본문에 명시).
      실측: 지어낸 식별자 3 → 1, 남은 하나는 **요청받은 신규 함수**라 실질 환각 **2 → 0**.

- [x] ~~🆕 **클러스터 상태 웹 UI**~~ — **완료 (마일스톤 3 중 3.2 = 3/3, 2026-08-13)**, 이후 M6 에서
      **v2 `#216` · v3 `#221`** 로 확장됐다(work 중심 목록 · 로그 열람 · 파이프라인 스텝퍼).
      `http://192.168.0.57:8103` · 하드웨어만 스냅샷(120초 가드), 큐·브로커는 서빙 순간 주입.
      원래 요청은 이랬다 — 머신 구성도 + SSH 로 받아온 실시간 상태 (사용자 요청 2026-08-08)
      *"머신들 구성 모습과 SSH로 상태 받아올 수 있으니까 상세 정보 표기하면 좋겠네.
      [뭘 시작했고, 온도, 전력량, cpu, gpu 상태 등]"*

      [중요] **무엇을 실제로 읽을 수 있는지 먼저 재봤다 (2026-08-08 실측)** — 나중에 추측으로
      설계하지 않기 위해서다. **세 머신 전부 나온다:**

      | 머신 | 명령 | 실측값 |
      |---|---|---|
      | **BC-250** (.43) | `/sys/class/drm/card*/device/hwmon/hwmon*/temp1_input`<br>`…/power1_average` | **55.0°C · 32.24 W** |
      | | `sensors` · `/proc/loadavg` · `/proc/cpuinfo` | 팬 0 RPM(팬리스) · load 0.00 · `AMD BC-250` |
      | | ⬜ `gpu_busy_percent` | **빈 값 — 이 보드에선 안 읽힌다** |
      | **`.2`** (RTX 3060) | `nvidia-smi --query-gpu=…--format=csv,noheader` | `RTX 3060, 42, 17.41 W, 0 %, 9959/12288 MiB` |
      | **`.33`** (RTX 3080) | 같은 명령 | `RTX 3080, 33, 6.32 W, 0 %, 2509/10240 MiB` |

      [주의] **함정**: 윈도우 SSH 는 `cmd.exe` 라 `| head` 가 없다(`'head' is not recognized`).
      파이프 없이 부르고 **파이썬에서 자르라.** 실측으로 걸린 지점이다.

      **"뭘 시작했고"** 는 이미 있는 것으로 대부분 된다 — 브로커 `/health`(노드별 healthy·
      상주 모델·inflight) · Ollama `/api/ps` · task_queue `/tasks` · `systemctl is-active`.
      새로 만들 것은 **수집·표시 계층**뿐이다.

      **설계 메모**: [중요] SSH 폴링은 **읽기 전용**이고, `.33` 은 사람의 기계라 게스트 규칙이
      적용된다(`machines/win-main-33.md`). 폴링 주기를 짧게 잡지 말 것 — BC-250 은 이 세션에
      **부하로 커널이 멈춘 전례**가 있다(리포트 10 §8). 소 2.4.2(도메인 뷰어 웹 UI)와 **같은
      웹 표면을 공유**하는 것이 자연스럽다 — 둘 다 마스터에 화면을 하나 세우는 일이다.

- [x] [중요][중요] ~~**워커 통제 배선**~~ — **4/5 배선 완료 2026-08-09.** 아래 실측은 착수 근거로 남긴다.
      (사용자 지시: *"이건 작업 꼭 해야 한다고 명기해두고"*)

      [완료] **끝난 것** — 번들 배달(`master/client/`, sha256 대조) · 워커 git 인증(§8.5) ·
      **러너**(`master/work/runner.py`, 큐→워커 e2e **71초 실측**) · **찌꺼기 정리**
      (`master/work/cleanup.py`). 아래 "고칠 방향" ⑴⑵ 도 여기 들어간다 — 매니페스트가
      기준 커밋을 싣고, [중요] 그 절은 **파견 시점에 다시 푼다**(등록 시점 값은 durable 브랜치가
      등록 이후 만들어지므로 **정상 흐름에서 반드시 낡는다**). ff-only 는 스킬이 못박는다.

      [중요] **결정이 하나 뒤집혔다**: 워커에 큐 토큰을 주지 않고 **마스터가 중개한다.** 실측
      결과 두 워커 모두 8101 이 **401**, AX MCP 등록 **0건**이라 *"MCP 에 헤더가 박혀 있다"* 는
      스킬의 전제가 틀려 있었다. 리스 하트비트(1200초)도 마스터가 대신 친다 — 워커의 Claude
      에게 맡기면 잊고, 잊으면 다른 워커가 같은 태스크를 집는다.

      ⬜ **남은 것은 요청자 `.33` 한 칸** — 작업 등록 + durable `task/<id>` 생성. 마스터는
      push 를 못 하므로(§2.1) 요청자가 해야 하는데 기계가 꺼져 있다. 오늘은 `.2`/`.43` 이 대행.

      **실측** (`janus@192.168.0.2`, `E:\trunk\ModularStage`, 2026-08-09):

      | 확인 | 결과 |
      |---|---|
      | `.claude/skills/` | **전임(AgentTest) 스킬 12종이 그대로 있다** — `distribute`·`review-work`·`cluster-selftest`·`refresh-ontology`·`manage-domain`·`audit-context` … |
      | `.claude/agents/` | 전임 에이전트 12종 (`code-writer`·`code-validator`·`build-integration` …) |
      | `.mcp.json` 의 `C:\Users\USER\Documents\ModularStage` | [중요] **MISSING** — 계정이 `USER` 다. 이 머신은 `janus` |
      | `.claude/mcp/*.exe` | [중요] **하나도 없다** (`context-search`·`log-analyzer`·`crash-analyzer`) |
      | `~/.claude.json` 의 `mcpServers` | [중요] **비어 있다** — 마스터 8101·8102·8103 이 **등록돼 있지 않다** |

      → 그 폴더에서 Claude Code 세션을 시작하면 **전임 프로젝트의 스킬 12종을 로드하고, 죽은
      MCP 6종을 물고, 우리 마스터는 모르는 상태**로 시작한다. **에러 없이 조용히 틀리는**
      이 저장소의 반복 실패 유형이다.

      **제어 표면 두 개** — 원본에 이미 있다(재발명 금지): `watcher/setup.py` 의
      `_merge_skills()`·`_merge_managed_mcp()`·`_deploy_hook_scripts()`,
      `skill_templates.py` 독스트링 *"watch.exe 가 init 시 `.claude/skills/<name>/SKILL.md` 로 배포"*.
      **행동 통일 = 스킬 · 능력 = MCP.**

      [중요] **우리 환경에서 다른 것 둘:**
      ① **데몬을 두지 않는다** — 원본이 `watch.exe` 폴링을 쓴 이유는 팀원 PC 에 접근 경로가
      없어서였다. `.2`·`.33` 둘 다 SSH 가 열려 있으니 **마스터가 밀어넣는다** (§5.5.4 가 같은
      논리로 이미 데몬을 기각했다).
      ② **배달은 fail-closed + 드리프트 검사** — 지금 `C:\Users\janus\CLAUDE.md` 가 손복사
      사본 + sha256 수동 대조인데 그게 이 메커니즘의 원시 버전이다(§7.7 이 "사본은 갈라진다" 를
      **알려진 예외**로 적어 뒀다). 배달 후 **되읽어 대조**하는 것까지가 한 세트다(§8.3).

      **소스 최신화도 같은 항목이다 (사용자 지적 2026-08-09).** 트윈은 미러의 **특정 커밋**
      기준으로 지어진다(워터마크가 커밋 해시). 워커 사본이 그 커밋이 아니면 매니페스트가 준
      참조 클래스·파일 경로·동결 시그니처가 안 맞는데 **에러가 안 난다.**

      | | 커밋 (2026-08-09 실측) |
      |---|---|
      | Gitea bare (정본) · 마스터 미러 · `last_indexed_commit` · `.2` 체크아웃 | 넷 다 `bc4b38f` — **지금은 우연히 맞아 있다** |
      | `master/work/manifest.py` 의 커밋 필드 | [중요] **0건** — 어긋나도 감지할 수단이 없다 |

      고칠 방향: ⑴ 매니페스트에 **`twin_commit`** 을 싣는다(컨텍스트 MD 의 `source_commit`
      스탬프와 같은 개념의 한 층 위) ⑵ 워커는 시작 전 그 커밋까지 **`fetch` + ff-only 로만**
      따라잡는다 — [중요] `reset --hard`·`checkout`·`git clean` 금지, **추적 파일이 더러우면 차단**
      (`workshop_check.py` 재사용), 못 따라잡으면 진행이 아니라 **fail-closed 차단**.
      [중요] **`.33` 은 예외 — 사람의 메인 작업 PC 다. 자동 갱신 대상이 아니라 드리프트 보고만 한다**
      (`machines/win-main-33.md` 게스트 규칙).

      **선결:** 전임 `.claude/` 일습을 **먼저 아카이브**해야 한다. 안 하면 우리 번들이 죽은
      스킬 12종 위에 병합된다.

      **자리:** 지금 README 스텁뿐인 **`client/`** 가 정본이 놓일 곳이다.
      상위 레이어(대 2 / `/distribute`)와 같은 차선이라 **중 1.3 이후**에 착수한다 —
      사용자 지시(*"지금은 마스터 서버의 작업을 하니까 1.3 을 진행하자"*).

- [x] ~~마스터 쪽 오케스트레이션 언어(Python vs Go) 최종 결정~~ — **Python 확정 2026-08-06** (아래 §5)

---
