# 18. 셀프테스트 2단계 이식 — 원전 능력 4개와 만료된 전제 둘 (2026-08-16)

> 마일스톤 4 · 중 2.3(`#141`). 근거 = 원전 `mcp/master_orchestrator/cluster_selftest.py` 416줄
> 정독 · `#142` 대조 결과.

## §1 세션 시작 상태 (실측)

    서비스     ax-task-queue · ax-broker · ax-projects · ax-indexer.path · ax-status.timer
               · ax-batch.timer — 전부 active
    기계       .2 UP · .33 UP · .43 UP (🔴 BC-250 이 8/15 OOM 이후 복귀 — uptime 3h16m)
    추론       bc250=35B 상주 · rtx3060=14b 상주 · 둘 다 healthy · 🔴 여유 1,155MB (조건 그대로)
    저장소     마스터·GitHub·BC-250 셋 다 `485244a` 동기 · 트리 깨끗
    마일스톤   M4 = **40/80** (레드마인 version *마일스톤 4*)

사용자가 오늘 작업으로 `#141`(중 2.3 /cluster-selftest 2단계)을 골랐다.

## §2 🔴 사용자가 정한 M4/M5 분리 (2026-08-16)

> *"작업이 이식작업이었지만 이전 작업을 무시한 상태로 진행된 것이 너무 많아서 마일스톤4가 긴급
> 잡힌거야. 그러다보니 작업된 것과 이식해야 할 것의 머지 작업이 필요할텐데. 1순위로 이식을 먼저
> 하고 다음 마일스톤 5를 잡아서 두 버전간 더 좋은 것이 뭘지 어떻게 머지할지 결정하는거야."*

    M4   순수 복각. 우리가 먼저 만든 것과 부딪혀도 **고치지 않고 표시만** 남긴다
    M5   「우리 것 ↔ 원전 것」 중 어느 쪽이 나은지 판정하고 합치는 방법을 정한다

🔴 **M5 목록을 언제 쌓을지도 사용자가 정했다 — 「M4 종료 때 한 번에」.** 내 추천은
「지금부터 한 곳에 모으자」였고 기각됐다. ⚠️ 되짚는 방식은 M4 가 생긴 원인과 같은 모양이라
놓칠 위험이 있다고 적어 두었으므로, **대신 이식마다 리포트에 머지 지점을 명시**해 되짚기
비용을 낮춘다. 이 리포트의 **§9** 가 그 자리다.

## §3 🔴 착수 전 정독이 뒤집은 전제 둘

`#141` 노트는 이 칸을 *"가드가 막으니 ⓐ/ⓑ/ⓒ 중 결정이 필요하다"* 로 적어 뒀다. 원전 416줄과
우리 595줄, 그리고 **가드에 직접 입력을 넣어** 잰 결과 그 전제가 둘 다 틀렸다.

### §3.1 가드는 이 경로에 아예 없다

`run_live` 의 git 은 **한 줄도 마스터에서 돌지 않는다** — 전부 `_ssh` 로 작업장에서 돈다.
그리고 작업장 `.43` 클론에는 **pre-push 훅이 없고** `origin` 이 곧장 Gitea 다(실측).

    가드 직접 입력 실측 (rc 가 아니라 **누가 막았는지**를 봤다 — 리포트 16 §2 의 교훈)
    selftest/<slug>       push    🔴 거부   ← 마스터에서 밀 때만 해당
    attempt/selftest-…/…  push    ✅ rc=0
    도달 불가 attempt/*   삭제    🔴 거부   ← 원전 8단계 cleanup 이 하는 일
    도달 불가 task/<id>   삭제    🔴 거부

🔴 **그래서 ⓐ/ⓑ/ⓒ 는 브랜치명 문제가 아니라 「잔재를 어떻게 하나」 문제였고, 그것은 하니스를
Flow Y(마스터가 push)로 옮길 때 비로소 생긴다.** 지금 형태의 2단계에는 가드 결정이 **필요 없다.**
⚠️ 리포트 14 §15 가 *"증명 안 한 것: 마스터가 쓰기 주체"* 라고 적어 둔 칸이 정확히 이것이다.

### §3.2 ⑤ C.7 통합 머지는 상류가 미이식이다

원전 7b 는 *"실 leader 가 쓰는 `_integrate_push_durables` 를 **그대로** 태워"* 통합 정합성을
본다(머지만·push 없음). 그 헬퍼는 `executor_review_finalize.py`(중 2.1 `#135`)에 있고
**우리 코퍼스 grep 0건 = 미이식**이다. 🔴 여기서 머지를 **새로 쓰면 검사의 뜻이 바뀐다** —
*"실 leader 경로가 합류시킨다"* 가 아니라 *"셀프테스트가 자기 머지를 돌렸다"* 가 된다.
따라서 **`#135` 가 상류**이고 오늘 옮기지 않는다. ⚠️ per-task durable 을 올리는
`verify_and_merge`(push 함)와는 **다른 능력**이다 — 이름이 비슷하다고 대응으로 보지 말 것.

## §4 이식한 것 넷 (`#142` 대조가 특정한 5개 중 4개)

| 능력 | 원전 근거 | 우리 구현 | 🔴 다르게 둔 것 |
|---|---|---|---|
| ① 폴링 중 워커 수 | step 0 · 사용자 2026-07-06 | `count_polling` + 사전 점검 배선 | **신호원 교체** (§5) |
| ② `force_backend` 태깅 | step 5 · 사용자 2026-07-05 | `build_specs` + `task_data.force_backend` | 기본 레인이면 **안 싣는다** |
| ③ 백엔드별 격리 검증 | step 7 `[{b}] write verified` | 레인별 `by_backend` 집계·단정 | 레인 ≥2 일 때만 단정 |
| ④ 진짜 병렬 판정 | step 7 구간 겹침 | `overlapping_pair` + 조건부 단정 | skip 여부를 **결과에 기록** |

②의 소비 주체를 같이 넣었다 — 🔴 **찍기만 하고 읽는 데가 없으면 죽은 이식**(리포트 17 §3)이라
하니스의 `work_fn_cmd` 가 backend 를 받도록 했다. 원전에서 이 필드를 읽는 것은 워커 데몬이고,
우리 `worker/` 는 아직 README 뿐이라 그 데몬이 생기면(`#128`) 같은 필드를 읽으면 된다.

⚠️ `_call_work_fn` 은 **인자 수를 세서** 옛 4인자 콜러도 받는다. `TypeError` 를 잡아 재시도하는
방식은 **쓰지 않았다** — 콜러 *안에서* 난 `TypeError` 까지 삼켜 조용한 실패가 된다(σ 부류).

## §5 🔴 원전 판정식을 글자로 옮기면 우리 큐에서 거짓말을 한다

원전은 *"등록 워커(대부분 stale)가 아니라 **실제 폴링 중**(`last_seen` 있음) 워커 수"* 로
병렬 가능성을 판정한다. 그대로 옮기고 실 큐에 대 보면:

    등록 4명 · 원전식(존재로 셈) = **4명**
      192.168.0.2            경과 167시간
      192.168.0.43           경과 170시간
      probe-HV0I6DL          경과 194시간
      selftest-192.168.0.43  경과  66시간
    🔴 실제 폴링 데몬 = **0개** (`worker/` 는 README 뿐)

원인은 전제 차이다 — 원전은 상주 워커 데몬이 heartbeat 를 계속 찍고, 우리
`worker_last_seen` 은 claim/heartbeat 때 찍히고 **만료되지 않는다**(`logic_claim.py`).
리포트 16 §12.7 의 σ.9.0 `mtime` 과 **같은 부류**다: 기제는 옮기고 신호원은 다시 고른다.

🔴 **임계값은 발명하지 않고 이 저장소에 이미 있는 것을 재사용했다** — `reclaim.py` 의
`_VERIFY_CAPABLE_RECENT_SEC = 300`("최근 접촉"에 대한 우리 답). 새 숫자를 만들면 같은 뜻의
임계값이 둘이 되어 조용히 갈라진다.

**그것이 바꾼 판정**: 원전식이면 폴링 4명 → `polling >= 2` 성립 → **병렬 단정이 켜진다** →
직렬로 도는 하니스가 매번 실패 → **없는 결함**. 신선도로 세면 0명 → skip(로그) → 정상.

## §6 실증 계획 — 🔴 **이 절은 실행 *전에* 썼다**

유닛 55/55 는 **계약만 재고 실물을 안 잰다**(리포트 13 §14). 사용자 승인(2026-08-16)을 받아
**배선만 실증**한다 — LLM 0.

    대상        작업장 `.43`(sim · /home/sim/trunk/ModularStage) · 실 Gitea · 실 큐
    파라미터    backends=["probe_lane_a","probe_lane_b"] · tasks=1(레인당) → probe 2개
                via_queue=True · keep=False · work_fn_cmd=None(스크립트 편집 = LLM 0)
    🔴 레인명   실제 백엔드 이름을 **쓰지 않는다** — 아무 백엔드도 안 돌았는데 큐 기록에
                `force_backend=claude` 가 남으면 그 기록이 거짓말을 한다
    부작용      실 Gitea 에 브랜치 5~6개 생성 → `finally` 에서 삭제 · 큐에 work 1건(종결 처리)
    되돌리기    하니스의 `finally` 가 원격·로컬·worktree·큐를 정리하고 **흔적 0을 단정**한다

**보려는 것 넷**: ⓐ `force_backend` 가 큐까지 실려 claim 응답에 돌아오는가 · ⓑ `by_backend`
집계가 레인별로 갈리는가 · ⓒ 병렬 단정이 **skip 경로**를 타는가(폴링 0) · ⓓ 정리 후 흔적 0.

## §7 라이브 런 — 🔴 **첫 판이 통과했는데, 그 통과가 아무것도 증명하지 않았다**

첫 실행은 **30개 단정 전부 통과 · ok=True · 흔적 0** 이었다. 그런데 결과를 보다가 알아챘다:

    by_backend = {'probe_lane_a': {'ok': 1, 'total': 1}, 'probe_lane_b': {'ok': 1, 'total': 1}}

이 집계는 **하니스 자신의 로컬 dict**(`backend_of`)로 센 것이다. 🔴 *"내가 아는 값을 내가 안다"*
일 뿐 **`force_backend` 가 큐까지 갔는지는 아무것도 말하지 않는다.** NONCE 증명이 값을 주입하지
않고 디스크에서 읽게 만든 것과 **정확히 같은 논리**인데, 내가 그 논리를 옆 칸에 적어 놓고
밟았다 — 리포트 13 §14 *"주입은 계약을 재고 실물을 재지 않는다"* 의 **다섯 번째** 재발이다.

큐가 돌려준 claim 응답으로 판정하는 단정을 넣고 다시 돌렸더니 **실패**했다:

    🔴 force_backend 가 **큐를 거쳐** claim 응답으로 돌아왔다
       기대={0:probe_lane_a, 1:probe_lane_b}  실측={0: None, 1: None}

## §8 그 실패가 드러낸 것 — 🔴 **원전 task_queue 의 능력 하나가 미이식이었다**

    등재   register_task 가 task_data 를 **MD 본문의 json 블록**에만 쓴다 (logic.py:202)
    meta   claim 이 돌려주는 레코드에 task_data 가 **없다** (logic.py:166-201)
    복원   재기동 시 프론트매터만 파싱한다 — 본문 블록을 읽는 코드가 0건
    결과   🔴 워커는 force_backend 를 **볼 방법이 없다**

원전은 이 자리에 근거를 박아 뒀다 — *"task_data 는 free-form dict → 확실히 저장·**claim 응답에
전달**(top-level 은 모델이 drop)"*(`cluster_selftest.py:223`). ⚠️ **리포트 14 의 인용 census 는
이것을 못 봤다** — `task_queue` 를 *"파일명 9개가 동명으로 이식됨"* 으로 분류했고, census 는
**파일 안의 능력 단위를 못 센다.** 마일스톤 함정 *"대조는 이름이 아니라 함수·능력 단위로"* 가
파일 **내부**에도 적용된다는 실례다.

**고친 것 셋** (사용자 승인 2026-08-16 — 재기동 포함):

    persistence.py   TaskIndex.task_data map 신설 · parse_task_data(body) · rebuild 가 되읽음
    logic.py         register_task 가 메모리에도 보관 (본문 기록과 짝)
    logic_claim.py   claim 응답에 task_data 포함 (후방호환 — 필드 추가)

🔴 **재기동 복원을 반드시 쟀다** — 등재 직후만 재면 *"메모리에만 있고 재기동하면 사라지는"*
상태를 통과시키고, 그러면 큐를 한 번 내렸다 올린 순간 워커가 **조용히 다른 백엔드를 쓴다.**
`test_task_data.py` 6건이 그 축(본문 파싱·claim 전달·재기동·claim 이 본문을 안 지움)을 잰다.
⚠️ `_save_task` 가 기존 본문을 다시 읽어 보존하는 것을 **확인하고** 나서 이 구조를 골랐다.

**재기동·재증**: `pkexec systemctl restart ax-task-queue` → works 13 · tasks 14 그대로 복원 →
라이브 런 **33/33 · 흔적 0**:

    ✅ 🔴 force_backend 가 **큐를 거쳐** claim 응답으로 돌아왔다
       실측={0: 'probe_lane_a', 1: 'probe_lane_b'} 어긋남={}
    [parallel] 병렬 단정 skip — 폴링 0명 / probe 2개 (원전 조건 미충족 · 직렬 검증만)
    [cleanup] 원격 5건·worktree 2개 제거 (남음=0)

🔴 **레인명에 실제 백엔드 이름을 쓰지 않은 것이 여기서 값을 했다** — 아무 백엔드도 안 돌았는데
큐 기록에 `force_backend=claude` 가 남았다면 **그 기록이 다음 세션에 거짓말**을 했을 것이다.

## §9 M5 머지 지점 (이 세션에서 나온 것)

사용자 결정대로 **M4 종료 때 한 번에** 모으되, 되짚기 비용을 낮추려고 여기에 명시한다.

| # | 우리 것 | 원전 것 | 판단 시점 |
|---|---|---|---|
| 1 | 하니스가 git 을 **작업장에서** 돌린다 (`_ssh`) | 인프라 PC(서버)가 직접 push | 🔴 Flow Y 로 옮길지 = `#127`/`#128` 계열 |
| 2 | 삭제 기준 = **도달 가능성** (증거 보존) | work 종료 시 **일괄 삭제** | 하니스를 마스터로 옮기면 충돌한다 (§3.1) |
| 3 | 통합자 `.2` (`integrate.py`) | C.7 리더 통합 (`_integrate_push_durables`) | `#135` 이식 후 |
| 4 | 폴링 판정 = **신선도 300s** | `last_seen` **존재** | 폴링 데몬(`#128`) 생기면 재검토 |
| 5 | `force_backend` 소비자 = 하니스 | 소비자 = 워커 데몬 | `#128` 이 생기면 같은 필드를 읽는다 |

## §10 세션 마감 (2026-08-16)

    M4          40/80 → **40/80** (🔴 `#141` 은 닫지 않았다 — ⑤ C.7 이 `#135` 상류 대기)
    분모         변동 없음. ⚠️ 큐 결함은 `#141` 이식이 성립하기 위한 조건이라 새 칸을 만들지
                 않고 같은 이슈에 기록했다 — 판단이 갈릴 수 있는 자리다
    이식         능력 4개 (폴링 워커 수 · force_backend · 백엔드별 격리 · 병렬 판정)
    부수 수정    큐 3파일 — task_data 가 claim 응답까지 가도록 (원전 능력 미이식분)
    테스트       2750 → **2780/2780 · 실패 0** (신규 30건: selftest 24 · task_data 6)
    라이브 런    2회 (LLM 0) — 첫 판이 구멍을 드러냈고, 고친 뒤 33/33
    실 변경      🔴 실 Gitea 에 브랜치 5개 생성 → 전부 삭제(흔적 0 단정 통과) ·
                 `ax-task-queue` 재기동 1회 (사용자 승인)
    정정         모듈 머리말의 만료된 전제 **2건** (가드 위치 · ⓐ/ⓑ/ⓒ 의 정체)

### 🔴 이 세션이 확인한 것 — **정독이 질문 자체를 바꿨다**

`#141` 은 *"가드가 막으니 브랜치명을 정해 달라"* 로 적혀 있었다. 원전 416줄과 우리 595줄을
읽고 가드에 **직접 입력을 넣어** 재니, 그 질문은 **지금 형태에서는 존재하지 않았다.** 사용자에게
ⓐ/ⓑ/ⓒ 중 고르라고 물었으면 **없는 결정을 시키는 것**이 됐다.

### 🔴 내가 틀렸다가 잡힌 것

    ① by_backend 집계로 "동작한다" 할 뻔했다   내 로컬 dict 를 읽는 집계였다 — 큐 응답으로 고쳤다
    ② `#141` 노트의 ⓐ/ⓑ/ⓒ 를 그대로 믿을 뻔    전제(마스터가 push)가 현행 코드와 달랐다
    ③ ⑤를 "새로 쓰면 되지" 로 볼 뻔했다        원전은 **실 leader 함수를 태운다** — 새로 쓰면
                                               검사의 뜻이 바뀐다. `#135` 가 상류다

### 🔴 다음 세션이 먼저 볼 것

    ①  `#141` 은 **⑤ 하나 남았고 `#135`(중 2.1) 가 상류**다. 순서는 사용자가 정한다
    ②  `force_backend` 를 읽는 **워커 데몬**은 아직 없다(`#128`) — 지금 소비자는 하니스뿐
    ③  병렬 단정은 폴링 데몬이 생기기 전까지 **항상 skip** 이다. skip 이 사실의 기록이다
    ④  M5 머지 지점 5건이 §9 에 있다 (사용자 결정: 수집은 M4 종료 때 한 번에)
