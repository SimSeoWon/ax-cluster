> Part of the AX Cluster plan. Index: [`../../PLAN.md`](../../PLAN.md).

# 마일스톤 2 — 디지털 트윈 복원 (2026-08-08 착수)

> 🔴 **안 된 것과 사슬만 담는다.** 완료분은 [`1-infrastructure.md`](1-infrastructure.md) 로
> 보낸다 — 열린 마일스톤 하나만 읽는다.
>
> **200줄 상한** (150 → 170 → 180 → **200**, 사용자 지시 2026-08-09: *"상한을 늘려. 더 줄일
> 수 없으면"*). 본문이 산문이 아니라 **분류표 + 결정 로그**라 깎으면 다음 세션이 같은 조사를
> 다시 한다. 🔴 **올리는 순서는 정해져 있다 — 완료된 소분류를 먼저 지우고, 그래도 넘치면
> 올리고 이유를 여기 적는다.** 실제로 2026-08-09 에 상한을 맞추려다 같은 실수를 네 번 했다
> (줄 수를 안 재고 "맞췄다" 고 커밋). **깎기보다 올리기가 싼 순간이 있다.**

## 🔴 성격 — 문서는 **지도지 정답이 아니다** (사용자 확정 2026-08-09)

> *"이 프로젝트는 완벽하게 정답을 찾는 문서가 아니다. 모든 문서는 **소스코드라는 원천**을
> 바탕으로 참조하거나 찾거나 이해하기 쉽도록 하는 **지도·네비게이션** 역할이다."*

원본 `rag-system.md` 첫 절과 같다 — *"RAG 는 진실의 원천이 아니라 네비게이션이다.
실제 소스코드가 항상 최신이고, RAG 는 '어디를 봐야 하는지' 안내하는 메타데이터 인덱스"*.

**판단 기준이 이것이다**: *"이걸로 맞는 자리를 찾을 수 있나"* — *"증명되게 옳은가"* 가 아니다.
근사(역방향 include 의 basename 매칭 등)는 그 기준에서 **허용된다.** 다만 **없는 것을 있다고
하면 틀린 자리로 보내므로** 사실 게이트는 그대로 둔다 — 그건 정확성이 아니라 항법의 문제다.

## 목표 (사용자 확정 2026-08-08) — **머리는 바꾸지 않는다**

1. **온톨로지 문서를 자동으로 작성한다** — 커밋마다 컨텍스트 문서 → 연관 클래스 관계 그래프
   → 검색용 BM25 → 최종적으로 **사용자의 수동 명령과 대화형으로 휴리스틱이 포함된** 온톨로지
2. **그 트윈 데이터를 코드 작성 에이전트에게** 관련된 것만 추출해 전달한다

🔴 목표 ①의 마지막 절이 핵심이다. 온톨로지는 전자동이 **아니다** — `자동 승급 2026-06-01
영구 비활성`, 미배정 클래스도 *"노출만, 자동 편입 0"*, 지시가 *"도메인 클래스 추가는
지금처럼 명시적으로만"*. 휴리스틱은 그 자리를 메우는 **LLM 0 추천기**다.

## 산출물 — 4종 (5종이 아니다)

| | 산출물 | 생산자 | LLM |
|---|---|---|---|
| ① | 컨텍스트 문서 (**태그·related_classes·category 포함**) | `watcher/context.py` | 🔴 예 |
| ② | 관계 그래프 — 상속(tree-sitter) + `#include` | `class_graph_parse` · `dependency` | 아니오 |
| ③ | 온톨로지 문서 (도메인 yaml) | `ontology_synthesizer` + **사람** | 🔴 예 |
| ④ | 시소러스 (`aliases`) | 🔴 **사람이 등록** + `build_thesaurus_index` | 아니오 |

**태그는 별도 산출물이 아니다** — `context.py` 프롬프트가 ①의 프론트매터로 쓰게 한다.

## 파이프라인 (`watch.py` 커밋 처리 순서, 실측)

```
커밋 → process_commit  컨텍스트 MD + source_commit 스탬프 + 색인 upsert   🔴 LLM
     → 의존/상속 그래프 증분 · path_sync · [헤더변경시] declared_contract  LLM 0
     → 시간 게이트(평일 10시 이후 첫 폴링) → ontology_refresh.run         🔴 LLM
```

🔴 **컨텍스트 MD 가 온톨로지 재합성의 신호원이다.** Phase η.8 이 dirty 플래그를 없애고 MD 의
`source_commit` 갱신 → `compute_stale_domains` 가 "저장값 ≠ 문서값" 으로 stale 판정한다.
**문서가 없으면 온톨로지는 자기가 낡은 줄 모른다.** LLM 을 쓰는 것만 게이트 뒤다.
**메인은 대 1**(데이터 생성). 대 2 는 생성 뒤에 붙고, 위 계층은 「🕓 예정」에만 남긴다.

## 선행 조건

✅ **tree-sitter** 0.26.0 + `tree-sitter-cpp` 설치, UE 매크로 전처리 리눅스 실측 확인.
🔴 **소스 44%가 CP949** (실측 723/1,654) — `master/source_text.py` 를 거쳐 읽는다. 그래프는
영향 없지만(식별자 ASCII) **컨텍스트 합성은 치명적**이다(한글 주석이 최고 신호원).

---

## 작업 분류 — 대 2 · 중 9 · 소 43

🔴 **진척(분자)은 여기 적지 않는다 — 레드마인이 계산한다** (사용자 결정 2026-08-09):
`http://192.168.0.57:8080` → 버전 **「마일스톤 2 — 디지털 트윈 복원」** → 중분류가 부모 이슈,
소분류가 서브태스크다. **이 문서가 남기는 것은 사슬·함정·실측 근거** — 이슈가 담지 못하는 것들.

⚠️ **왜 옮겼나.** 손으로 적은 분자가 **실제로 두 번 어긋났다.** ⑴ 중 1.3 이 `7/10` 인데 남은
목록은 6개였다(합 13) ⑵ 중 1.3 이 5→8 로 올랐는데 **대 1 의 분자가 15 에 멈춰 있었다**(실측
18). 🔴 **작업이 는 게 아니라 세는 법이 틀렸던 것이다.** 사람이 세는 한 계속 어긋난다.

### 대 1. 트윈 데이터 생성 (목표 ①) · 🔴 **이번 마일스톤 메인**

**중 1.1 관계 그래프** ✅ 5/5 · **중 1.2 컨텍스트 문서** ✅ 5/5 — 상세는 `../../CLAUDE.md` 코드 표.

**중 1.3 온톨로지 문서** ✅ **13/13** (`master/ontology/`, 테스트 250)

✅ `yaml_io` · `stale`(워터마크 2개) · `layers`(구조 우선, git 은 강등만) · `collect`(후보 **제안**)
· `hierarchy` · **`verify_facts`**(호출 관계 사실 게이트) · `package`(🔴 검수 잠금 보존)
· ✅ **`1.3.3` 재합성(LLM)** — `domain_md`·`contexts`·`prompt`·`parse`·`synth`·`__main__`

✅ 마지막 5개 (2026-08-09) — **전부 사람이 부른다. 자동 편입 0**
`create`(씨앗 MD, 덮지 않음·이름 안 고침) · `edit`(**편집이 곧 잠금**) · `unassigned`(노출만) ·
`lock/unlock`(잠금 가시성) · `sync`(경로·`declared_methods`)

🔴 **1.3.7 은 절반만 있었다** — `package` 가 `verified_by_user` 를 *보존*했지만 **거는 경로가
없어 死코드였다**(실측). 🔴 **1.3.6 은 규모가 곧 설계였다** — 1,806 중 배정은 111(6%)뿐이라
평평한 목록은 프로젝트 전체를 쏟는 것이다. 인접(266, 근거·도메인 포함)과 고립(1,429, 규모 순
표본)으로 가른다. 고립 상위에 `UWNDInventory`·`FPSMapEditEdMode`·`UUserAccount` 처럼
**통째로 도메인이 없는 서브시스템**이 보인다 — 도메인이 모자라다는 신호다.
🔴 **1.3.9 는 잠금과 충돌하지 않는다** — 경로·선언은 *기계의 사실*이라 잠긴 항목에도 넣되
그 두 필드 말고는 건드리지 않는다. 그래프에 없는 클래스는 **지우지 않고 보고**한다.

🔴 **페이로드를 조각내 두 노드에 나눠 던진다**(사용자 2026-08-09) — 창을 못 늘리는 것과
**데이터를 못 보는 것은 다른 문제**다(단일 요청이면 `ProjectAlpha` 67개 중 8개만 보인다).
예산은 실측이다(2.79~2.85자/tok · `num_ctx` 8192 → 발췌 7,000자).
🔴 `plan → dry → refresh` **순서로 쓴다.** 실측·상세 = 리포트 11.

🔴 **범위는 active 도메인 멤버십만** (원안 1,799 → 24, **36배 절감**). **폐지된 것 이식 금지**
— Obsidian `.md` · 검수 큐 · `ontology_artifacts`. 나머지 결정 규칙은
[`master/ontology/__init__.py`](../../master/ontology/__init__.py) — 코드 옆이 읽히는 자리다.

🔴 **사실 게이트는 모호하면 봐준다** — 엔진 클래스, 괄호 없는 `Class::Member`(실측 오탐).
*정상 문서를 막는 게이트가 통과시키는 게이트보다 나쁘다.* 현 데이터 228건 대조 6건 지적.

⚠️ **후보 폭발**: 다른 도메인/하위 문서 소속을 뺀 뒤에도 **`GlobalEventSystem` 은 178** —
온 프로젝트가 include 하는 허브다. **하위 문서로 갈라야** 풀린다(소 1.3.10 이 그 수단).

**중 1.4 시소러스 · 한글 어휘** ✅ **5/5** · 🔴 **실측 근거가 생겼다 (2026-08-09)**

| 소 | | 함정 |
|---|---|---|
| 1.4.1 | `aliases` 필드 + 등록 경로 | 사람이 등록 |
| 1.4.2 | **재합성 이월** | 🔴 안 하면 다음 합성에 사라진다 |
| 1.4.3 | 인덱스 빌더 + fingerprint | — |
| **1.4.4** | 한글 어휘 등록 | ✅ **별칭 10개**(두 단어 구체 용어). 🔴 단독 일반명사는 뺐다. ⚠️ MD 태그 전파도 막았다 — 앞서 *"6/7 에 tags 없음"* 은 **awk 오독**이었고 실제론 전부 있다 |
| **1.4.5** | alias → 색인 tags 채널 | ✅ 가중치 3.0 채널로 올라간다 |

🔴 **근거** — 「미션 태스크 실행」에 `MissionEditor` 0.4883 / `MissionRuntime` 0.4778(2% 차).
⑴ **tags 채널(가중치 3.0)에 한글이 0개**라 한글 질의에서 최고 가중치 채널이 안 걸리고
⑵ `MissionRuntime` 이 더 많이 갖고도 body 가 길어 정규화에 깎였다. 증거는
`MissionSystemTools` — body 에 「태스크」 0회인데 **한글 태그 4개만으로** 상위권이다.
🔴 **가중치를 만지지 말 것**(리포트 10 §9.2: 눈으로 보며 세 번 손댔고 한 번은 나빠졌다) —
**데이터를 늘리는 1.4.4/1.4.5 만 안전하고, 순위 튜닝은 정답 집합 전에 하지 않는다.**

### 대 2. 트윈 소비 배선 (목표 ②)

**중 2.1 도메인 색인** ✅ **3/3** — 별도 DB·별도 컬렉션, `rebuild_all` 배선.
🔴 **노이즈 3중 차단** — 무관 질의는 한글·영문 둘 다 0건. 상세 = 리포트 11.

**중 2.2 검색 확장** ✅ **2/2**

| 소 | | |
|---|---|---|
| 2.2.1 | 시소러스 질의 확장 | ✅ `context_search/expand.py` — 🔴 부분 문자열(조사) · 짧은 별칭 차단 · 개수 상한 |
| 2.2.2 | 질의 → 도메인 **결정적** 추론 | ✅ `context_search/infer.py` — 🔴 출처는 패키지 yaml(`class_ontology` 표는 **행 0**) · 모르면 비운다 |

**중 2.3 코드 작성 grounding** ✅ **2/2** — `work/norms.py`, 테스트 20. 규범의 본체는
**invariants**(액션은 "무엇을 하는가", invariant 는 "무엇을 지켜야 하는가"). 도메인 ≤2 ×
대상 클래스와 얽힌 액션만 = *"관련된 것만"*. 🔴 잘랐으면 본문에 적는다.

✅ **실측**(같은 지시·모델, 매니페스트만 교체): 규범 없음은 **없는 멤버**를 지어냈고, 규범
있음은 실재 멤버 **4/4** + `ExecutorTickOnlyWhenRunning` invariant 준수. 🔴 **그래도 환각은
남았다** — `CurrentStep`(실제 `Step`) 같은 **한 글자 차이**. 규범은 *무엇을 지킬지*를 주지만
**정확한 철자는 헤더에만 있다** → `../3-open-items.md` 의 *"노드에 소스를 실어야"* 가 실측이 됐다.

**중 2.4 건강성·가시성** ✅ **3/3**

| 소 | | |
|---|---|---|
| 2.4.1 | drift 감사 | ✅ `ontology/drift.py` — 🔴 커밋 차이가 아니라 **근거 파일 변경**으로 좁힌다 |
| 2.4.2 | 도메인 뷰어 | ✅ `ontology/view.py` — **자립형 HTML 한 장**(서비스 안 띄운다). 🔴 manifest 를 믿지 않고 파일을 훑는다 |
| 2.4.3 | 상태 익스포트/임포트 | ✅ `master/transfer.py` — **두 벌**(compat·full) + README. 🔴 임포트는 덮어쓰기가 아니라 **병합** |

**중 2.5 워커 파견 배선** ✅ **5/5** — 목표 ②의 *마지막 한 뼘*. 트윈을 실제로 **먹이는** 자리다.

| 소 | | |
|---|---|---|
| 2.5.1 | 워커 번들 (config·스킬·CLAUDE.md, sha256 대조) | ✅ `client/` |
| 2.5.2 | 워커 git 인증 (Gitea SSH 키) | ✅ 양쪽 push 확인 |
| 2.5.3 | **러너** — claim → 기준 재해석 → scp → `claude -p` → 마커 → submit | ✅ `work/runner.py`, e2e 71초 |
| 2.5.4 | 브랜치·부산물 찌꺼기 정리 | ✅ `work/cleanup.py` — 병합된 것만, 원격은 안 건드림 |
| 2.5.5 | 요청자 `.33` 배선 — Gitea SSH 키 · durable 브랜치 생성 | ✅ ①~⑥ 한 바퀴 **1분 29초** 실측 |

🔴 **워커에 큐 토큰을 주지 않는다 — 마스터가 중개한다.** 실측: 두 워커 모두 8101 이 **401**,
AX MCP **0건**이라 스킬의 전제가 틀려 있었다. 하트비트(리스 1200초)도 마스터가 대신 친다 —
워커의 Claude 에게 맡기면 잊고, 잊으면 **다른 워커가 같은 태스크를 집는다.**

🔴 **판정은 fail-closed 마커** — 마지막 세 줄 `ATTEMPT`/`HEAD`/`RESULT`. 관대함은 **BLOCKED
쪽으로만** 연다(`RESULT: BLOCKED — 사유` 는 받고, `DONE — …` 은 안 받는다).

**사슬**: `1.2 → 1.2.5`(색인 트리거) · `1.2 → 1.3`(stale 신호원) · `1.1 → 1.3.3`(합성이 그래프를
읽는다) · `1.3 → 2.1 → 2.2/2.3`. 상류 **1.1·1.2**, 종점 **2.3.1**.

---

## 🕓 예정 — 상위 레이어 (이 마일스톤 밖, 폐기 아님)

트윈을 **먹는 쪽**이다. 이제 온톨로지가 색인되지만 **`attach_norms`(2.3.1)가 없어** 매니페스트가
아직 규범 자리에 `"_미구현_"` 을 싣는다 — 그 위에 상위 레이어를 세우면 안 된다. 이름만 남긴다.

🕓 `/distribute` · `/review-work` · 작업유형 템플릿 · `/cluster-selftest` · `/tdd-dryrun` ·
`/debate` · 팀 기능. 착수 전 `distributed-workers.md`(425줄) 필독.

🔴🔴 **워커 통제 배선은 「예정」이 아니라 「필수」였다** → **중 2.5 로 올렸다(4/5).** **끝났다(5/5).** 사람의 등록부터 워커의 제출·복귀까지 한 줄로 이어진다.

🔴 **골조 생성 부재** — `register_work` 는 골조를 **인자로 받는다**(사람이 쓰는 전제). 원본은
`generate_skeletons` 가 만든다. §4.5 "골조 → 동결 → 병렬 본문" 의 첫 단계가 비었다.

## 진행 규칙 (마일스톤 1 의 실패에서)

1. **"완료" 는 영역 전체에만.** 부분이면 분모를 같이 적되, 🔴 **수치는 레드마인에서 읽는다**
   — 손으로 적은 분자는 두 번 어긋났다(위 ⚠️)
2. **조사 없이 라벨 붙이지 않는다** · **소스보다 문서를 먼저 읽는다** · **줄 수를 작업량으로
   쓰지 않는다**(1,341줄이 "삭제 후 재스캔" 이었다)
3. 🔴 **품질은 결정적 게이트로 검증한다** — LLM 에게 자기 검증을 맡기지 않는다
4. 🔴 **순서는 사용자가 정한다** — 사슬만 적고 임의로 고르지 않는다
5. **완료된 소분류는 즉시 지워** 마일스톤 1 로 보낸다 — 200줄 상한을 그렇게 지킨다
6. **머리(목표·산출물)는 바꾸지 않는다** — 바뀌면 마일스톤 자체가 바뀐 것이다

## 미독 인수인계 문서

`data-schema.md`(206) ✅ · `ontology.md`(94)·`rag-system.md`(187) 부분. **미독: `README.md` 842 ·
`watcher-internals.md` 543 · `runbook.md` 280 · `extending.md` 251** (+ 예정분 425).
위 분류는 `watch.py` 실측이라 유효하다. 🔴 **중 1.2·1.3 착수 전 `watcher-internals.md`** —
컨텍스트 MD 생성·LLM 폴백·서킷브레이커의 내부 동작이 거기 있다.
