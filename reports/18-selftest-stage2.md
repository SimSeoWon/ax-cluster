# 18. 셀프테스트 2단계 이식 — 원전 능력 4개와 만료된 전제 둘 (2026-08-16)

> 마일스톤 4 · 중 2.3(`#141`). 근거 = 원전 `mcp/master_orchestrator/cluster_selftest.py` 416줄
> 정독 · `#142` 대조 결과.

## §1 세션 시작 상태 (실측)

    서비스     ax-task-queue · ax-broker · ax-projects · ax-indexer.path · ax-status.timer
               · ax-batch.timer — 전부 active
    기계       .2 UP · .33 UP · .43 UP ([중요] BC-250 이 8/15 OOM 이후 복귀 — uptime 3h16m)
    추론       bc250=35B 상주 · rtx3060=14b 상주 · 둘 다 healthy · [중요] 여유 1,155MB (조건 그대로)
    저장소     마스터·GitHub·BC-250 셋 다 `485244a` 동기 · 트리 깨끗
    마일스톤   M4 = **40/80** (레드마인 version *마일스톤 4*)

사용자가 오늘 작업으로 `#141`(중 2.3 /cluster-selftest 2단계)을 골랐다.

## §2 [중요] 사용자가 정한 M4/M5 분리 (2026-08-16)

> *"작업이 이식작업이었지만 이전 작업을 무시한 상태로 진행된 것이 너무 많아서 마일스톤4가 긴급
> 잡힌거야. 그러다보니 작업된 것과 이식해야 할 것의 머지 작업이 필요할텐데. 1순위로 이식을 먼저
> 하고 다음 마일스톤 5를 잡아서 두 버전간 더 좋은 것이 뭘지 어떻게 머지할지 결정하는거야."*

    M4   순수 복각. 우리가 먼저 만든 것과 부딪혀도 **고치지 않고 표시만** 남긴다
    M5   「우리 것 ↔ 원전 것」 중 어느 쪽이 나은지 판정하고 합치는 방법을 정한다

[중요] **M5 목록을 언제 쌓을지도 사용자가 정했다 — 「M4 종료 때 한 번에」.** 내 추천은
「지금부터 한 곳에 모으자」였고 기각됐다. [주의] 되짚는 방식은 M4 가 생긴 원인과 같은 모양이라
놓칠 위험이 있다고 적어 두었으므로, **대신 이식마다 리포트에 머지 지점을 명시**해 되짚기
비용을 낮춘다. 이 리포트의 **§9** 가 그 자리다.

## §3 [중요] 착수 전 정독이 뒤집은 전제 둘

`#141` 노트는 이 칸을 *"가드가 막으니 ⓐ/ⓑ/ⓒ 중 결정이 필요하다"* 로 적어 뒀다. 원전 416줄과
우리 595줄, 그리고 **가드에 직접 입력을 넣어** 잰 결과 그 전제가 둘 다 틀렸다.

### §3.1 가드는 이 경로에 아예 없다

`run_live` 의 git 은 **한 줄도 마스터에서 돌지 않는다** — 전부 `_ssh` 로 작업장에서 돈다.
그리고 작업장 `.43` 클론에는 **pre-push 훅이 없고** `origin` 이 곧장 Gitea 다(실측).

    가드 직접 입력 실측 (rc 가 아니라 **누가 막았는지**를 봤다 — 리포트 16 §2 의 교훈)
    selftest/<slug>       push    [중요] 거부   ← 마스터에서 밀 때만 해당
    attempt/selftest-…/…  push    [완료] rc=0
    도달 불가 attempt/*   삭제    [중요] 거부   ← 원전 8단계 cleanup 이 하는 일
    도달 불가 task/<id>   삭제    [중요] 거부

[중요] **그래서 ⓐ/ⓑ/ⓒ 는 브랜치명 문제가 아니라 「잔재를 어떻게 하나」 문제였고, 그것은 하니스를
Flow Y(마스터가 push)로 옮길 때 비로소 생긴다.** 지금 형태의 2단계에는 가드 결정이 **필요 없다.**
[주의] 리포트 14 §15 가 *"증명 안 한 것: 마스터가 쓰기 주체"* 라고 적어 둔 칸이 정확히 이것이다.

### §3.2 ⑤ C.7 통합 머지는 상류가 미이식이다

원전 7b 는 *"실 leader 가 쓰는 `_integrate_push_durables` 를 **그대로** 태워"* 통합 정합성을
본다(머지만·push 없음). 그 헬퍼는 `executor_review_finalize.py`(중 2.1 `#135`)에 있고
**우리 코퍼스 grep 0건 = 미이식**이다. [중요] 여기서 머지를 **새로 쓰면 검사의 뜻이 바뀐다** —
*"실 leader 경로가 합류시킨다"* 가 아니라 *"셀프테스트가 자기 머지를 돌렸다"* 가 된다.
따라서 **`#135` 가 상류**이고 오늘 옮기지 않는다. [주의] per-task durable 을 올리는
`verify_and_merge`(push 함)와는 **다른 능력**이다 — 이름이 비슷하다고 대응으로 보지 말 것.

## §4 이식한 것 넷 (`#142` 대조가 특정한 5개 중 4개)

| 능력 | 원전 근거 | 우리 구현 | [중요] 다르게 둔 것 |
|---|---|---|---|
| ① 폴링 중 워커 수 | step 0 · 사용자 2026-07-06 | `count_polling` + 사전 점검 배선 | **신호원 교체** (§5) |
| ② `force_backend` 태깅 | step 5 · 사용자 2026-07-05 | `build_specs` + `task_data.force_backend` | 기본 레인이면 **안 싣는다** |
| ③ 백엔드별 격리 검증 | step 7 `[{b}] write verified` | 레인별 `by_backend` 집계·단정 | 레인 ≥2 일 때만 단정 |
| ④ 진짜 병렬 판정 | step 7 구간 겹침 | `overlapping_pair` + 조건부 단정 | skip 여부를 **결과에 기록** |

②의 소비 주체를 같이 넣었다 — [중요] **찍기만 하고 읽는 데가 없으면 죽은 이식**(리포트 17 §3)이라
하니스의 `work_fn_cmd` 가 backend 를 받도록 했다. 원전에서 이 필드를 읽는 것은 워커 데몬이고,
우리 `worker/` 는 아직 README 뿐이라 그 데몬이 생기면(`#128`) 같은 필드를 읽으면 된다.

[주의] `_call_work_fn` 은 **인자 수를 세서** 옛 4인자 콜러도 받는다. `TypeError` 를 잡아 재시도하는
방식은 **쓰지 않았다** — 콜러 *안에서* 난 `TypeError` 까지 삼켜 조용한 실패가 된다(σ 부류).

## §5 [중요] 원전 판정식을 글자로 옮기면 우리 큐에서 거짓말을 한다

원전은 *"등록 워커(대부분 stale)가 아니라 **실제 폴링 중**(`last_seen` 있음) 워커 수"* 로
병렬 가능성을 판정한다. 그대로 옮기고 실 큐에 대 보면:

    등록 4명 · 원전식(존재로 셈) = **4명**
      192.168.0.2            경과 167시간
      192.168.0.43           경과 170시간
      probe-HV0I6DL          경과 194시간
      selftest-192.168.0.43  경과  66시간
    [중요] 실제 폴링 데몬 = **0개** (`worker/` 는 README 뿐)

원인은 전제 차이다 — 원전은 상주 워커 데몬이 heartbeat 를 계속 찍고, 우리
`worker_last_seen` 은 claim/heartbeat 때 찍히고 **만료되지 않는다**(`logic_claim.py`).
리포트 16 §12.7 의 σ.9.0 `mtime` 과 **같은 부류**다: 기제는 옮기고 신호원은 다시 고른다.

[중요] **임계값은 발명하지 않고 이 저장소에 이미 있는 것을 재사용했다** — `reclaim.py` 의
`_VERIFY_CAPABLE_RECENT_SEC = 300`("최근 접촉"에 대한 우리 답). 새 숫자를 만들면 같은 뜻의
임계값이 둘이 되어 조용히 갈라진다.

**그것이 바꾼 판정**: 원전식이면 폴링 4명 → `polling >= 2` 성립 → **병렬 단정이 켜진다** →
직렬로 도는 하니스가 매번 실패 → **없는 결함**. 신선도로 세면 0명 → skip(로그) → 정상.

## §6 실증 계획 — [중요] **이 절은 실행 *전에* 썼다**

유닛 55/55 는 **계약만 재고 실물을 안 잰다**(리포트 13 §14). 사용자 승인(2026-08-16)을 받아
**배선만 실증**한다 — LLM 0.

    대상        작업장 `.43`(sim · /home/sim/trunk/ModularStage) · 실 Gitea · 실 큐
    파라미터    backends=["probe_lane_a","probe_lane_b"] · tasks=1(레인당) → probe 2개
                via_queue=True · keep=False · work_fn_cmd=None(스크립트 편집 = LLM 0)
    [중요] 레인명   실제 백엔드 이름을 **쓰지 않는다** — 아무 백엔드도 안 돌았는데 큐 기록에
                `force_backend=claude` 가 남으면 그 기록이 거짓말을 한다
    부작용      실 Gitea 에 브랜치 5~6개 생성 → `finally` 에서 삭제 · 큐에 work 1건(종결 처리)
    되돌리기    하니스의 `finally` 가 원격·로컬·worktree·큐를 정리하고 **흔적 0을 단정**한다

**보려는 것 넷**: ⓐ `force_backend` 가 큐까지 실려 claim 응답에 돌아오는가 · ⓑ `by_backend`
집계가 레인별로 갈리는가 · ⓒ 병렬 단정이 **skip 경로**를 타는가(폴링 0) · ⓓ 정리 후 흔적 0.

## §7 라이브 런 — [중요] **첫 판이 통과했는데, 그 통과가 아무것도 증명하지 않았다**

첫 실행은 **30개 단정 전부 통과 · ok=True · 흔적 0** 이었다. 그런데 결과를 보다가 알아챘다:

    by_backend = {'probe_lane_a': {'ok': 1, 'total': 1}, 'probe_lane_b': {'ok': 1, 'total': 1}}

이 집계는 **하니스 자신의 로컬 dict**(`backend_of`)로 센 것이다. [중요] *"내가 아는 값을 내가 안다"*
일 뿐 **`force_backend` 가 큐까지 갔는지는 아무것도 말하지 않는다.** NONCE 증명이 값을 주입하지
않고 디스크에서 읽게 만든 것과 **정확히 같은 논리**인데, 내가 그 논리를 옆 칸에 적어 놓고
밟았다 — 리포트 13 §14 *"주입은 계약을 재고 실물을 재지 않는다"* 의 **다섯 번째** 재발이다.

큐가 돌려준 claim 응답으로 판정하는 단정을 넣고 다시 돌렸더니 **실패**했다:

    [중요] force_backend 가 **큐를 거쳐** claim 응답으로 돌아왔다
       기대={0:probe_lane_a, 1:probe_lane_b}  실측={0: None, 1: None}

## §8 그 실패가 드러낸 것 — [중요] **원전 task_queue 의 능력 하나가 미이식이었다**

    등재   register_task 가 task_data 를 **MD 본문의 json 블록**에만 쓴다 (logic.py:202)
    meta   claim 이 돌려주는 레코드에 task_data 가 **없다** (logic.py:166-201)
    복원   재기동 시 프론트매터만 파싱한다 — 본문 블록을 읽는 코드가 0건
    결과   [중요] 워커는 force_backend 를 **볼 방법이 없다**

원전은 이 자리에 근거를 박아 뒀다 — *"task_data 는 free-form dict → 확실히 저장·**claim 응답에
전달**(top-level 은 모델이 drop)"*(`cluster_selftest.py:223`). [주의] **리포트 14 의 인용 census 는
이것을 못 봤다** — `task_queue` 를 *"파일명 9개가 동명으로 이식됨"* 으로 분류했고, census 는
**파일 안의 능력 단위를 못 센다.** 마일스톤 함정 *"대조는 이름이 아니라 함수·능력 단위로"* 가
파일 **내부**에도 적용된다는 실례다.

**고친 것 셋** (사용자 승인 2026-08-16 — 재기동 포함):

    persistence.py   TaskIndex.task_data map 신설 · parse_task_data(body) · rebuild 가 되읽음
    logic.py         register_task 가 메모리에도 보관 (본문 기록과 짝)
    logic_claim.py   claim 응답에 task_data 포함 (후방호환 — 필드 추가)

[중요] **재기동 복원을 반드시 쟀다** — 등재 직후만 재면 *"메모리에만 있고 재기동하면 사라지는"*
상태를 통과시키고, 그러면 큐를 한 번 내렸다 올린 순간 워커가 **조용히 다른 백엔드를 쓴다.**
`test_task_data.py` 6건이 그 축(본문 파싱·claim 전달·재기동·claim 이 본문을 안 지움)을 잰다.
[주의] `_save_task` 가 기존 본문을 다시 읽어 보존하는 것을 **확인하고** 나서 이 구조를 골랐다.

**재기동·재증**: `pkexec systemctl restart ax-task-queue` → works 13 · tasks 14 그대로 복원 →
라이브 런 **33/33 · 흔적 0**:

    [완료] [중요] force_backend 가 **큐를 거쳐** claim 응답으로 돌아왔다
       실측={0: 'probe_lane_a', 1: 'probe_lane_b'} 어긋남={}
    [parallel] 병렬 단정 skip — 폴링 0명 / probe 2개 (원전 조건 미충족 · 직렬 검증만)
    [cleanup] 원격 5건·worktree 2개 제거 (남음=0)

[중요] **레인명에 실제 백엔드 이름을 쓰지 않은 것이 여기서 값을 했다** — 아무 백엔드도 안 돌았는데
큐 기록에 `force_backend=claude` 가 남았다면 **그 기록이 다음 세션에 거짓말**을 했을 것이다.

## §9 M5 머지 지점 (이 세션에서 나온 것)

사용자 결정대로 **M4 종료 때 한 번에** 모으되, 되짚기 비용을 낮추려고 여기에 명시한다.

| # | 우리 것 | 원전 것 | 판단 시점 |
|---|---|---|---|
| 1 | 하니스가 git 을 **작업장에서** 돌린다 (`_ssh`) | 인프라 PC(서버)가 직접 push | [중요] Flow Y 로 옮길지 = `#127`/`#128` 계열 |
| 2 | 삭제 기준 = **도달 가능성** (증거 보존) | work 종료 시 **일괄 삭제** | 하니스를 마스터로 옮기면 충돌한다 (§3.1) |
| 3 | 통합자 `.2` (`integrate.py`) | C.7 리더 통합 (`_integrate_push_durables`) | `#135` 이식 후 |
| 4 | 폴링 판정 = **신선도 300s** | `last_seen` **존재** | 폴링 데몬(`#128`) 생기면 재검토 |
| 5 | `force_backend` 소비자 = 하니스 | 소비자 = 워커 데몬 | `#128` 이 생기면 같은 필드를 읽는다 |

## §10 세션 마감 (2026-08-16)

    M4          40/80 → **40/80** ([중요] `#141` 은 닫지 않았다 — ⑤ C.7 이 `#135` 상류 대기)
    분모         변동 없음. [주의] 큐 결함은 `#141` 이식이 성립하기 위한 조건이라 새 칸을 만들지
                 않고 같은 이슈에 기록했다 — 판단이 갈릴 수 있는 자리다
    이식         능력 4개 (폴링 워커 수 · force_backend · 백엔드별 격리 · 병렬 판정)
    부수 수정    큐 3파일 — task_data 가 claim 응답까지 가도록 (원전 능력 미이식분)
    테스트       2750 → **2780/2780 · 실패 0** (신규 30건: selftest 24 · task_data 6)
    라이브 런    2회 (LLM 0) — 첫 판이 구멍을 드러냈고, 고친 뒤 33/33
    실 변경      [중요] 실 Gitea 에 브랜치 5개 생성 → 전부 삭제(흔적 0 단정 통과) ·
                 `ax-task-queue` 재기동 1회 (사용자 승인)
    정정         모듈 머리말의 만료된 전제 **2건** (가드 위치 · ⓐ/ⓑ/ⓒ 의 정체)

### [중요] 이 세션이 확인한 것 — **정독이 질문 자체를 바꿨다**

`#141` 은 *"가드가 막으니 브랜치명을 정해 달라"* 로 적혀 있었다. 원전 416줄과 우리 595줄을
읽고 가드에 **직접 입력을 넣어** 재니, 그 질문은 **지금 형태에서는 존재하지 않았다.** 사용자에게
ⓐ/ⓑ/ⓒ 중 고르라고 물었으면 **없는 결정을 시키는 것**이 됐다.

### [중요] 내가 틀렸다가 잡힌 것

    ① by_backend 집계로 "동작한다" 할 뻔했다   내 로컬 dict 를 읽는 집계였다 — 큐 응답으로 고쳤다
    ② `#141` 노트의 ⓐ/ⓑ/ⓒ 를 그대로 믿을 뻔    전제(마스터가 push)가 현행 코드와 달랐다
    ③ ⑤를 "새로 쓰면 되지" 로 볼 뻔했다        원전은 **실 leader 함수를 태운다** — 새로 쓰면
                                               검사의 뜻이 바뀐다. `#135` 가 상류다

### [중요] 다음 세션이 먼저 볼 것

    ①  `#141` 은 **⑤ 하나 남았고 `#135`(중 2.1) 가 상류**다. 순서는 사용자가 정한다
    ②  `force_backend` 를 읽는 **워커 데몬**은 아직 없다(`#128`) — 지금 소비자는 하니스뿐
    ③  병렬 단정은 폴링 데몬이 생기기 전까지 **항상 skip** 이다. skip 이 사실의 기록이다
    ④  M5 머지 지점 5건이 §9 에 있다 (사용자 결정: 수집은 M4 종료 때 한 번에)

---

# (같은 세션 2부) 중 2.1 `#135` — 리더 검수 흐름 이식

사용자가 이어서 `#135` 를 골랐다: *"이제 리더[TD, 팀장등]이 없으니 작업 요청자 [.33] 가
진행하면 될 거 같아."*

## §11 [중요] 그 매핑은 이미 우리 코드에 적혀 있었다

새 판단이 아니다 — 마스터의 `pre_push_hook.sh:84` 가 거부 메시지로 이렇게 못박아 뒀다:
*"main 을 옮기는 것은 사람의 자리다 — 요청자(.33)에서 한다."* `.33` 은 UE5 를 갖고 있어
검증 빌드가 가장 빠르고(`ax-request` §7), 사람이 거기 앉아 있다. 원전의 리더(TD·팀장) 자리가
그대로 옮겨온다.

**사용자가 정한 것 셋** (2026-08-16):

    빌드 위치       `.33` 로컬 (원전 그대로). `.2` 위임은 **마스터에 UE5 가 없어서** 강제된
                    것이고 여기엔 해당이 없다
    Redmine 경로    [중요] **마스터 경유** — 키가 컨테이너 DB 에만 있다. `.33` 에 비밀을 안 둔다
    자동화 경계     시뮬레이션·빌드·요약은 자동 / main 머지·push 는 승인 후 /
                    [중요] **브랜치 삭제는 안 한다 — 명령만 적어 준다**

## §12 [중요] 반사실 실측이 설계를 강제했다 — 사람의 트리

원전 `review_work_impl` 은 `git stash push -u` → `checkout main` → … → `stash pop` 을 하고
**pop 실패는 로그만 남긴다**(`executor_review_finalize.py:191`). 착수 전에 `.33` 을 실측했다:

    .33  브랜치 main · [중요] M Content/Blueprints/BP_MissionPrefab_C_0/BP_MissionPrefab_C_0.uasset

그리고 픽스처에 원전 방식을 그대로 태워 봤다:

    stash 직후        사람 파일 존재 = **False** (stash@{0} 로 들어갔다)
    시뮬레이션 머지 후  브랜치 = review/w1 · 사람 파일 존재 = **False**

원전에서 그 트리는 **서버의 것**이라 잃을 것이 없었다. 우리에게 그 자리는 사람이 편집 중인
트리이고 `.uasset` 은 바이너리라 되살릴 수 없다. → **worktree 로 옮겼다.** [중요] 이것은
「안 옮기는 판단」이 아니라 **옮기되 우리 규약을 끼워 넣는 판단**이다(리포트 17 §4 의
`protected` 와 같은 결). 기제(시뮬레이션 → 빌드 → 요약 → 상태 flip)는 원전 그대로다.
[주의] 그리고 우리 `ax-request` §7 이 **이미 같은 것을 요구하고 있었다** — 읽고 시작해서 알았다.

## §13 이식한 것

| 원전 | 우리 | [중요] 다르게 둔 것 |
|---|---|---|
| `_integrate_push_durables` (C.7) | `integrate_durables` | 그대로 — 머지만·push 없음·통째 중단. **`#141` ⑤ 의 상류가 이걸로 풀린다** |
| `review_work_impl` | `review_work` | worktree · 빌드 주입 · Redmine 본문만 생성 |
| `reject_work_impl` | `reject_work` | 브랜치 **안 지운다** (반려분은 구조적으로 전부 보존) |
| `finalize_work_impl` | `finalize_work` | [중요] `confirm=False` 기본 · `HEAD:main` push(브랜치 전환 없음) |
| `_cleanup_all_work_branches` | `plan_branch_cleanup` | [중요] **계획만** — 도달 가능성으로 가른다(`#125`) |
| `_redmine_update_issue` | `redmine.update_issue` | 실패를 `False` 로 삼키지 않고 **사유 문자열** |
| — | `/api/v1/redmine/note` | 마스터 경유(토큰 필수). 원전엔 없던 자리 — 키 위치가 달라서 |
| 스킬 `/review-work` | `ax-review` SKILL.md | 요청자 역할에 등록 |
| — | 번들 **파이썬 페이로드** | [중요] 로직이 두 벌이 되지 않게 모듈째 배달 |

## §14 [중요] 실측이 내 가정을 반박한 것 둘

**① 「피드백」 상태는 없다.** 반려 기본 상태를 그렇게 적어 뒀는데 이 Redmine 의 실제 목록은
**신규(1)·진행(2)·해결(3)·검토(5)·완료(4=닫힘)** 이다. 그대로 뒀으면 상태 변경이 **영원히
조용히 건너뛰어졌을** 것이다(코드가 사유는 남기지만 아무도 안 읽는 사유는 없는 것과 같다).
[중요] 반려에 해당하는 상태가 **아예 없으므로** 기본을 「상태 변경 안 함」으로 뒀다 — 없는 것을
억지로 고르면(예: 「검토」) **반려된 일감이 「검토 중」으로 보인다.** 승인은 「해결」이다
(「완료」로 닫지 않는다 — 원전과 같은 방향).

**② 재기동 확인이 너무 빨랐다.** 큐 재기동 직후 `livez` 를 8회 순식간에 돌려 전부 실패했고
*"서비스가 안 올라왔다"* 로 읽었다. 저널을 보니 **정상 기동**이었고 내 루프가 바인딩 전에
끝난 것이었다 — 계측 오류를 서비스 장애로 오진할 뻔했다(*"결과가 극단적이면 도구를 먼저
의심하라"*의 또 다른 사례).

## §15 실증 (라이브)

    [중요] fail-closed        토큰 없이 POST /api/v1/redmine/note → **401**
    [중요] 마스터 경유 갱신    #141 에 코멘트 등재 → 응답 ok=true, **이슈에서 실물 확인**(7건 중 최근 2건)
    [중요] 없는 상태 이름      `Rejected` → 코멘트는 남고 상태만 건너뜀 + **그 사실을 문장으로 반환**
    [중요] 배달본 단독 임포트  의존 폐포 3파일 + `__init__` 로 `from axwork import review` 성공
                          큐 클라이언트 부재 시 맨 ImportError 가 아니라 **할 일을 말한다**
    음성 확인             원전 방식(stash+checkout)을 픽스처에 태워 우리 단정 6개가 실제로 걸림

## §16 세션 마감 (2026-08-16 · 2부 포함)

    M4          40/80 → **40/80** ([중요] `#141`·`#135` 둘 다 닫지 않았다)
    테스트       2750 → **2892/2892 · 실패 0** (이 세션 신규 **142건**)
    커밋         5개 — 048a829 · bb70b90 · da89609 · 23edf76 · (이 커밋)
    서비스       ax-task-queue 재기동 2회 (둘 다 승인) · 전 유닛 active
    실 변경      실 Gitea 브랜치 5개 생성→삭제(흔적 0) · Redmine #141 코멘트 3건
    [중요] 남은 것   `#135`: 빌드 함수(`.33` UE5)를 `build_fn` 에 꽂기 · 실 `.33` 배달·실증
                 `#141`: ⑤ C.7 단정을 셀프테스트에 붙이기 (상류가 풀렸다)

### [중요] 다음 세션이 먼저 볼 것 (갱신)

    ①  `integrate_durables` 가 생겼다 → `#141` ⑤ 를 붙이면 그 이슈가 닫힌다
    ②  `#135` 는 **실 배달·실증이 남았다** — `python -m master.client deliver` 로 `.33` 에
        스킬+페이로드를 보내고 한 바퀴 돌려야 「된다」가 증명된다
    ③  `.33` 빌드 함수는 아직 없다 — `layer3_verify` 의 로그 파싱 계약을 재사용할 것
        ([중요] SSH/배치 종료 코드는 양방향으로 틀린다)
    ④  M5 머지 지점이 §9 의 5건에서 늘었다: 원전 일괄 삭제 ↔ 우리 도달 가능성 ·
        원전 silent 실패 ↔ 우리 사유 문자열 · 원전 config 키 ↔ 우리 마스터 경유

---

# (같은 세션 3부) `#141` ⑤ C.7 — 상류가 풀려 붙였고, 중 2.3 을 닫았다

## §17 실 leader 함수를 그대로 태운다

원전 7b 의 핵심은 머지 자체가 아니라 **누가 머지하느냐**다: *"실 leader review/finalize 가
쓰는 `_integrate_push_durables` 를 그대로 태운다."* 그래서 이 칸은 `#135` 가 상류였고,
2부에서 `work/review.py::integrate_durables` 가 생기면서 풀렸다.

    실행 위치   [중요] 마스터 정본 클론 **옆** 임시 worktree (`ax-c7-<ts>`)
                원전은 서버 트리에서 돈다. 우리 서버는 마스터이고, 정본은 색인기 전제상
                `main` 에 앉아 있어야 하므로 거기서 머지하지 않는다
    remote      `gitea-write`(SSH) — `origin` 은 bare 경로라 `gitea` 그룹이 필요하고
                그 실패는 *"저장소가 아니다"* 로 **원인을 감춘다**(§5 가 밟았던 함정)
    push        [중요] **없다.** 머지 정합성만 본다 (원전: *"main 무접촉"*)

[주의] **머리말의 「아직 못 옮기는 것 하나」 절을 지우지 않고 「풀렸다」로 고쳐 썼다** — 만료된
근거를 그대로 두면 다음 세션이 그것을 사실로 읽는다(이 세션에 같은 부류를 이미 두 번 고쳤다).

## §18 실증 — 라이브 (LLM 0)

    ok=True · 단정 **36/36** · c7_integrated=True
    [완료] C.7 통합 전용 트리 생성 (정본 미접촉)
    [완료] [중요] C.7 리더 통합 머지 (feature + durable 2건, 충돌 0) — merged=2 skip=[]
    [완료] [중요] 통합 브랜치에 전 probe 합류 (2/2 NONCE)
    [완료] [중요] 정리 후 작업장 로컬/원격 흔적 0

**착수 후 확인 — 마스터 정본이 무사한가** (C.7 이 그 옆에 트리를 세웠으므로):

    브랜치 main · HEAD b1ba6af (안 움직임) · 더티 0건 · `ax-c7-*` 잔재 0개
    [중요] 색인 게이트도 정상 — 작업 중 *"색인 일시 중지 — 진행 중 분산 작업 1건"* 을 찍었다
    큐 활성(in_progress/ready) work **0건** — 하니스가 work 를 종결했다

[주의] **「전부 통과」라 도구를 의심했다.** 통합 함수 자체는 `test_review.py` 가 음성 경로
(충돌 → 통째 중단 · verified 인데 durable 부재 → 중단)까지 재고 있고, 여기서 새로 잰 것은
**배선**이다 — *"실 경로가 합류시키고 각 NONCE 가 살아남는가"*. 합류 검사는 통합 트리에서
파일을 **다시 읽어** 세므로, 머지가 조각을 조용히 덮으면 수가 줄어든다.

## §19 마감 — [중요] 중 2.3 (`#141`) 닫음

    하위     #142 소 2.3.1 대조 [완료] · #143 소 2.3.2 드라이런 배선 [완료]
    이 세션  2단계 능력 **5/5** (폴링 워커 수 · force_backend · 백엔드별 격리 · 병렬 판정 · C.7)
    M4       40/80 → **41/80**
    테스트    2750 → **2898/2898 · 실패 0** (이 세션 신규 148건)

[중요] **`#141` 이 처음 물었던 질문(ⓐ/ⓑ/ⓒ 브랜치명)은 끝내 답하지 않았다** — 그 질문 자체가
전제가 틀려 존재하지 않았기 때문이다(§3.1). 하니스를 Flow Y 로 옮길 때 다시 생긴다.

---

# (같은 세션 4부) `#135` 잔여 — `.33` 검수 빌드 + 실 배달

## §20 빌드 함수 — [중요] 판정을 두 벌로 만들지 않는다

`review_work(build_fn=…)` 에 꽂을 것을 새로 쓰지 않았다. 위험은 빌드가 되느냐가 아니라
**판정이 갈라지는 것**이라, 셋을 전부 공유한다:

    명령      layer3_verify.build_build_command   (`run_build_on_workshop` 과 **같은 것**)
    판정      layer3_verify.parse_build_log       ([중요] 종료 코드를 보지 않는다)
    경로 유도  skeleton_gate.build_bat_from_editor ([주의] `_build_bat_from` 은 여기에 위임)

새로 만든 것은 **전송 하나**뿐이다 — `run_build_local` (로컬 `cmd /c`). 마스터는 UE5 가 없어
`.2` 로 위임하지만 요청자는 **자기 디스크의 검수 트리**를 갖고 있어 자기 자신에게 SSH 로 붙을
이유가 없다(사용자 결정: *".33 로컬 (원전 그대로)"*).

[중요] **`local_build_fn` 은 엔진을 못 찾으면 만들 때 죽는다.** 검수를 절반 돌린 뒤
*"빌드는 못 했지만 통과"* 로 끝나는 것이 최악이라 착수 전에 드러낸다.

### [중요] 경로를 짐작했으면 한쪽이 반드시 틀렸다 (프로브 실측)

    .2   C:\Program Files\UE_5.8\…              ← Epic Games **없음**
    .33  C:\Program Files\Epic Games\UE_5.8\…   ← **있음**

두 배치가 실제로 공존한다. 그래서 유도 규칙은 짐작이 아니라 **프로브가 찾아 둔 값에서
되짚는다**. 실증에서 유도된 `Build.bat` 이 `.33` 디스크에 **실재**함까지 확인했다
(`os.path.isfile` = True — 문자열 조작이 아니라 실물).

## §21 배달 — [중요] 저장소 배치를 미러링한다

첫 판은 평면(`axwork/`)이었다. 그러면 `build_local` 의 `from ..layer3_verify import …` 가
죽고, 남는 선택지는 **소스에 try/except 이중 임포트를 심는 것**뿐이다 — 배달 때문에 저장소
코드를 더럽히는 짓이다. → `master/` → `axmaster/` 로 **배치를 그대로** 옮겼다.

    .ax/lib/axmaster/__init__.py · layer3_verify.py
    .ax/lib/axmaster/work/__init__.py · branch_names · coordinator · review · skeleton_gate · build_local

[주의] 배달되는 `work/__init__.py` 는 저장소의 것이 **아니다** — 저장소 쪽은 manifest·generate 를
끌어와 폐포가 커진다. 배달본은 「손대지 말 것」만 적힌 최소 표식이다.

## §22 실 배달 실증 (사용자 승인 2026-08-16)

    배달 전 .33   main · M Content/…/BP_MissionPrefab_C_0.uasset (사람 작업)
    배달          .2 / .33 / .43 셋 다 [완료] 해시 대조 통과
    [중요] 배달 후    **글자 하나 같다** — 같은 브랜치, 같은 미커밋 파일 하나뿐
    .ax/          `.git/info/exclude` 에 있어 워킹트리를 더럽히지 않는다

**배달본 실행 확인** (`.33` 에서, 읽기 전용 · 빌드 안 돌림):

    config ue5_cmd  C:\Program Files\Epic Games\UE_5.8\…\UnrealEditor-Cmd.exe
    유도 build_bat  C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat
    실재 여부       **True**
    임포트          axmaster.work.{review,build_local} · axmaster.layer3_verify 전부 OK
    판정 계약        성공로그 True / 실패로그 False — 마스터와 같은 파서

## §23 [중요] 실 배달이 드러낸 **보고의 구멍** 둘

**① `deliver` 출력이 페이로드를 안 찍었다.** 파일 8개가 **실제로 갔는데** 출력에 없어서,
확인하려면 원격 디렉토리를 직접 뒤져야 했다. [중요] *"배달했다고 믿지 않는다"* 는 **사람이 읽는
출력에도** 적용된다 — 안 보이는 산출물은 다음 세션에 **없는 것으로 읽힌다.** `plan` 과
`deliver` 양쪽에 찍게 고쳤다.

**② `check` 가 페이로드를 안 쟀다.** 스킬은 문서라 낡아도 사람이 읽다 눈치채지만, 파이썬
모듈은 **낡은 채로 조용히 돈다** — `.33` 이 옛 review 로직으로 검수하면 그 판정이 거짓말이
된다. 해시 대조를 넣고 **음성 확인**까지 했다: 배달본 한 줄을 일부러 망치니

    [중요] 192.168.0.33 [requester]: … [중요] payload 불일치/없음 1개: work/build_local.py

재배달로 복구 → `payload 6개 일치`.

## §24 마감 (4부)

    테스트    2898 → **2934/2934 · 실패 0**
    [중요] 남은 것 `#135` 의 **실 검수 1회**(실 work 을 골라 시뮬레이션+빌드까지)는 아직이다 —
              지금은 배달·임포트·경로·판정 계약까지 실증됐고, 그 위에서 도는 한 바퀴가 남았다

---

# (같은 세션 5부) 순수 이식 9건 — 이식 7 · 대조로 끝난 2

## §25 사용자가 다시 못박은 순서 (2026-08-16)

> *"1. 계속말하는데 **이식이 먼저**야. 2. 그 뒤 **실제 작동 검증**이고 3. 가장 마지막이 **개선**이야"*

[주의] 이 발화가 최우선 규칙에 **중간 단계 하나를 명시**했다. 기존 문장은 *"리눅스 적용이 1번,
개선은 그 뒤"* 였는데, 그 사이에 **실 작동 검증**이 있다. 그래서 `#197`(실 검수 1회)은 이식
9건보다 **뒤**, 개선 3건(`#146`·`#169`·`#178`)보다 **앞**이다.

## §26 [중요] 37건은 37개의 작업이 아니었다

사용자 지적(*"아직 수정해야 할 작업이 37건이 있는데??"*)으로 열린 목록을 갈라 봤다:

    중분류 컨테이너  12   하위가 다 닫히면 닫는 칸 (그 자체는 작업이 아니다)
    실증             1    #197
    소분류           24   실제 작업 칸

그리고 그 24를 우리가 적어 둔 규칙으로 다시 갈랐다 — 순수 이식 **9** · 결정/상류 대기 8 ·
재범위 4(닫지 않음) · 개선 3(M4 에서 진행 안 함).

### [중요] 그 점검이 **안 닫힌 것 둘**을 찾아냈다

    #117 중 1.1  하위 #118·#119·#120 전부 완료 · done_ratio 100% · 그런데 status=신규
    #159 중 3.4  하위 5건 전부 완료 · done_ratio 100% · 그런데 status=신규

[주의] **중 1.1 은 마일스톤 문서가 `[완료]` 로 적어 뒀는데 레드마인은 열려 있었고**, 중 3.4 는
**양쪽 다 놓쳤다**(문서의 「닫힌 중분류」 목록에도 없었다). [중요] 분자를 레드마인이 세게 한 이유가
이건데 **반대 방향으로 샜다** — 문서가 앞서고 레드마인이 뒤처지는 형태다.

## §27 이식한 것 일곱

| 이슈 | [중요] 이식하며 바꾼 것 |
|---|---|
| `#182` 카나리 | **절반만 돼 있었다.** 판정 기준을 코드에 대 보니 진짜 자리가 원전과 **달랐다** — 우리 재색인 실패는 self-heal 되고, 삼켜지는 곳은 `rebuild_all` 의 도메인 색인이었다 |
| `#183` UE 금지 3종 | 동결이 못 막는 **상속 변경·모듈 이동**을 이유째 |
| `#139` 태스크 템플릿 | [중요] **층3 의 전제** · 검색 채널 분리 불변식을 **AST 테스트**로 못박음 |
| `#140` 웹 표면 | 404 를 **자원 부재**로 구분 — 라이브로 본문이 바뀐 것을 확인 |
| `#145` TDD 드라이런 | 판정 **4교훈** + 컴파일 규약. 입력(등록된 템플릿)은 아직 없다 |
| `#167` 개명 cascade | [중요] **DB 는 안 옮긴다** — 옮기면 원천 제거한 드리프트가 되살아난다 |
| `#171` agy 프로브 | [중요] **신호원 교체** — 원전은 PTY, 우리는 **산출물 검증**(SUCCESS 라면서 파일 안 씀) |

## §28 대조로 끝난 둘 — [중요] 「안 옮기는 판단」에도 근거가 있다

    #168  audit_drift·list_stale·refresh_ontology 는 이미 있고,
          cleanup_inactive_domain·cleanup_null_classifications 는 **전부 DB 연산**이라
          우리 0행 표에 지울 것이 없다
    #170  redmine 갱신·test filters·queue URL 은 **오늘 채워졌고**, 계층 계산은 우리가
          **`#include` 그래프**로 하며(신호원이 다르다), `_is_protected_branch` 는
          우리 `pre-push` 훅이 git 계층에서 **더 강하게** 막는다

두 칸 다 **재범위는 닫지 않는다** — 전제(DB 를 SSOT 로 쓴다 / 훅을 못 쓴다)가 바뀌면 되살린다.

## §29 마감 (5부)

    M4        44/81 → **55/81** (이식 7 + 대조 2 + 안 닫혀 있던 2)
    테스트     2945 → **3086/3086 · 실패 0** (이 세션 누적 신규 **336건**)
    커밋       6개 추가 (6481160 · e12cac4 · 397d957 · 4bb87dc · e0e7bf3 · 이 커밋)
    재기동     ax-projects 1회 (웹 표면 확인)

### [중요] 이 회차에 내가 틀렸다가 잡힌 것

    ① grep 이 두 번 헐거웠다      `version.py` 가 "drift" 로, `sqlite_util.py` 가 "금지" 로 잡혔다
    ② 불변식 테스트를 문자열로 쟀다  모듈 독스트링이 불변식을 *설명하며* 쓴 단어를 「호출」로
                                  셌다 → **AST 로 바꿨다**
    ③ 재기동 확인이 또 빨랐다      서비스는 정상인데 루프가 바인딩 전에 끝났다(두 번째)

### [중요] 다음 세션이 먼저 볼 것

    ①  **실 작동 검증 단계다** (사용자 순서 ②) — `#197` 실 검수 1회 · agy 프로브 라이브 1회
    ②  태스크 템플릿 **등록**은 사람의 자리다 — 그것이 있어야 `#145` 드라이런에 입력이 생긴다
    ③  개선 3건(`#146`·`#169`·`#178`)은 **가장 마지막**이다
    ④  남은 26건 = 컨테이너 10 + 결정/상류 대기 8 + 재범위 5 + 개선 3
