"""컨텍스트 문서 합성 — 마일스톤 2 의 중 1.2.

소스 변경 → 컨텍스트 MD. **LLM 을 쓰는 첫 산출물**이고, 이것이 없으면 트윈이 자라지 않는다:

- 벡터/BM25 색인(소 1.2.5)의 트리거가 컨텍스트 digest 변화다 — 문서가 안 바뀌면 색인은
  스냅샷에 고정된다
- 🔴 온톨로지 재합성(중 1.3)의 stale 판정이 이 문서의 `source_commit` 을 한쪽으로 쓴다.
  **컨텍스트 문서가 없으면 온톨로지는 자기가 낡은 줄 모른다** (Phase η.8)

`md.py` 는 순수 함수(정제·게이트·스탬프), `prompt.py` 는 프롬프트 조립, `synth.py` 가
오케스트레이션이다.
"""
